# Con CRM — Conecta Imóveis

CRM interno de atendimento a leads da imobiliária **Conecta Imóveis** (região Petrolina/Juazeiro, Brasil).
Foco exclusivo: **atendimento** e **relatório de produtividade individual** por corretor. Sem cadastro de imóveis, contratos ou financeiro — não adicione esses módulos sem pedido explícito.

Este arquivo é o contexto do projeto. Leia-o antes de agir. Fale português com o usuário (Ali).

## Estado atual (o que já existe)

- **Frontend** (`frontend/index.html`): protótipo React completo e funcional, **rodando 100% no navegador com dados de exemplo em memória** (nada persiste — reseta ao recarregar). É um arquivo único, autossuficiente (React embutido), pronto pra hospedar como site estático. Já foi publicado no Netlify pelo usuário.
  - Código-fonte legível em `frontend/src/app.jsx`. O `index.html` é a versão **compilada** desse JSX (ver "Build do frontend").
  - **Responsivo** (feito): dois cortes de largura em `useIsMobile` / `useIsCompact` — até 760px é uma tela por vez com navegação inferior; até 1024px a ficha do lead vira botão em vez de painel fixo.
- **Backend** (`backend/`): Node/Express + SQLite (better-sqlite3). Ainda **não hospedado**. Cadastro de corretor com confirmação por e-mail está pronto e testado ponta a ponta; Meta e Uazapi ainda não ligados.
  - **Cadastro** (feito): `POST /auth/register` cria a conta como `pendente` + token de convite (7 dias) → e-mail via Resend (`services/mail.js`) → `/definir-senha?token=...` → `POST /auth/set-password` ativa. Sem `RESEND_API_KEY`, o link é devolvido na resposta e impresso no log — o fluxo funciona mesmo sem provedor de e-mail contratado.
  - **Páginas públicas**: `backend/public/cadastro.html` e `definir-senha.html`, servidas pelo próprio backend (HTML puro, sem build). O link que a ADM manda é `https://URL/cadastro?c=ADM_CODE`.
  - **`src/bootstrap.js`** roda a cada start: cria a org a partir de `ADM_CODE` e, se `ADM_EMAIL`/`ADM_PASSWORD` estiverem no .env, cria a conta ADM. Por isso **não é preciso rodar `seed` em produção** — `seed` é só para os usuários fictícios de teste.

## Papéis (3)

- **ADM** (Ali): painel da equipe, relatórios por atendente, e conecta o número único da Conecta via Uazapi.
- **SDR** (ex.: Camila): faz a **catraca** (distribuição) E **também atende** como um corretor. Importante: quando a SDR faz o primeiro atendimento, ela precisa poder **repassar o lead para o corretor da vez** (rodízio) — o lead NÃO fica preso na conta dela.
- **Corretor**: atende seus leads, avança no funil, vê a própria produtividade e marca disponibilidade.

## Regras de negócio importantes (não quebrar)

- **Número único da Conecta**: um só WhatsApp para todos (via Uazapi). NÃO é conexão individual por corretor. Cada mensagem que sai é **assinada com o nome do corretor** (prefixo `*Nome:*`) para o lead saber com quem fala.
- **Lead novo cai direto na atendente**: todo lead que entra (Meta ou WhatsApp) já nasce com dono — a atendente da vez, por rodízio próprio (`orgs.atendente_ptr`, ver `services/catraca.js`). Aqui **não** se olha disponibilidade: ninguém atende antes dela, então o lead não pode ficar parado por falta de prontidão marcada. Só o gestor vê e controla essa catraca (`GET /distribution/atendentes`).
- **Catraca manual (corretores)**: só recebe lead quem se prontificou no dia (campo `available`). A SDR transfere manualmente (um a um) ou por rodízio ("próximo disponível"). Quem está indisponível não entra na fila. Esta regra vale para os **corretores** — não confundir com a catraca das atendentes acima.
- **Caixa da atendente x supervisão**: a atendente enxerga a imobiliária inteira (é supervisora), mas a tela "Atender" abre no escopo `meus` — o que está com ela mais a fila. Sem isso, o lead que ela acabou de repassar continuava na tela dela. "Toda a equipe" fica a um clique.
- **Finalizar atendimento** (`POST /leads/:id/finalizar`): tira a conversa da caixa de entrada **sem mexer na etapa do funil** — encerra o atendimento, não o negócio. Se o cliente responder, reabre sozinho (webhook da Uazapi). `?finalizados=1` lista os encerrados.
- **Catraca numerada, e o botão diz o nome** (25/08/2026, `services/rodizio.js`, `GET /distribution/rodizio`): a tela mostra a ordem da vez (1º, 2º, 3º…) com "é a vez" em quem recebe o próximo, e o botão de repasse escreve **"Passar para Marina"** em vez de "corretor da vez". Antes de mostrar, a fila precisou parar de mudar sozinha — dois defeitos: (1) a vez era `contador % quantos estão disponíveis`, e como essa lista muda o dia inteiro, alguém marcar prontidão reordenava a fila de todos **sem ninguém ter recebido lead**; (2) `/next` sorteava entre corretores E atendentes e `/handoff` só entre corretores, mas os dois avançavam o MESMO `distribution_ptr`. Agora a memória é **quem foi o último a receber** (`orgs.rodizio_ultimo`) e o próximo é o primeiro disponível depois dele na roda — entrar ou sair da disponibilidade não desloca mais ninguém. A atendente saiu de `/next` (a regra do repasse já dizia que nunca volta para ela). **Escolher um corretor a dedo também move a vez**: sem isso, quem foi escolhido na mão continuava sendo o próximo e levava dois leads seguidos. Quem não se prontificou aparece na lista **sem número**, senão o gestor vê seis corretores na equipe e dois na catraca sem entender o sumiço. Teste: `npm run teste:rodizio`.
- **Repasse da SDR**: `POST /distribution/handoff` — passa o lead para o próximo CORRETOR disponível (rodízio), ou para um corretor específico. Nunca de volta pra SDR.
- **Funil com 11 etapas** (nesta ordem): `Lead, Atendimento, Pasta, Aprovação, Agendamento, Visita, Proposta, Venda, Perdido, Recaptação, Transferido por ligação`.
- **A palavra-chave RECOMENDA a etapa; ela não move mais nada** (26/08/2026, pedido do Ali; `sugerirEtapa` em `routes/messages.routes.js`, colunas `leads.sugestao_etapa/sugestao_de/sugestao_em`, `POST /leads/:id/sugestao-etapa`): o avanço automático saiu. O motivo não é a regra errar muito — é que **ela e a gestão escreviam no MESMO lugar**: o funil andava pela palavra, alguém corrigia na mão, a palavra aparecia de novo na mensagem seguinte e empurrava outra vez. No fim ninguém sabia dizer, olhando o relatório, qual etapa era leitura de gente e qual era palpite de regex — e número que ninguém reconhece não sustenta reunião.

  A leitura continua (é de graça e instantânea) e vira **recomendação**: um cartão âmbar na ficha e no popup do funil, dizendo de → para e **a palavra que disparou** ("alguém falou em *documentação*"). Sem a palavra escrita, "mude para Pasta" não dá para conferir. Dois botões: **Confirmar** grava com `motivo='mao'` (é o que faz a etapa contar como confirmada por pessoa no score) e **Não é isso** apaga a recomendação sem tocar na etapa — não é "depois eu vejo", é dizer que a leitura errou.

  **Recomendação velha some sozinha**: `sugestao_de` guarda a etapa de quando a leitura foi feita; se o lead andou desde então, ela é descartada (409 na rota). Confirmar uma leitura feita sobre outro estado moveria o lead para trás sem ninguém pedir.

  As palavras continuam em `GATILHOS` (`services/stages.js`), nesta ordem, valendo a **mais adiantada** da conversa: atendimento → Atendimento; documentação/documentos → Pasta; aprovação → Aprovação; visita/agendar → Agendamento; "o que achou do imóvel" → Visita; fechar/proposta → Proposta; contrato → Venda. Etapas manuais (Perdido/Recaptação/Transferido) ficam fora. **A "Reanálise da base" também parou de mover**: aplicar agora grava a recomendação em cada lead, para ser confirmada uma a uma — o gestor não tinha como conferir 300 mudanças antes de aplicar. O `PALAVRA_ETAPA` do `app.jsx` continua sendo o espelho dos GATILHOS, e o texto da ficha mudou junto: enquanto dizia "diga a palavra e o lead vai", ensinava um automatismo que não existe mais. Teste: `npm run teste:etapa-recomendada`.

- **A conversa SEMPRE abre no fim** (17/08/2026, `chatRef` virou um ref de função em `app.jsx`): a rolagem para o rodapé só acontecia quando o `selId` mudava. Só que abrir a ficha e voltar, ou sair para outra tela e voltar ao atendimento, **remonta o painel da conversa** — e div recém-montada nasce com a rolagem no topo, com o mesmo lead selecionado, então o efeito não fazia nada. O corretor caía na primeira mensagem, de meses atrás. Agora o `ref` é uma função: o React a chama no instante em que o elemento entra na tela, que é exatamente o momento certo, e ela vale para todo caminho de volta (não só os dois conhecidos). O `.current` continua existindo (`Object.defineProperty`) para o resto do código que já usava. A regra de NÃO puxar quem subiu para ler continua de pé — o ref só dispara na montagem.
- **Sem temperatura é um estado válido** (14/08/2026): todo lead do WhatsApp nascia `MORNO` — não era leitura de ninguém, era o padrão da coluna, e o funil inteiro ficou morno. Lead novo agora entra com `priority = NULL`, e a tela mostra "sem temperatura". No frontend, `prioDe(p)` substituiu todo acesso direto a `PRIO[...]`: com `prio` nulo o app quebrava a tela inteira.
- **Arrumar a base** (14/08/2026, `services/lote.js`, tela em "Base de leads" → Arrumar a base; só ADM): duas operações em massa, as duas com **prévia antes de aplicar**. (1) **Tirar o "Morno"** — apaga só essa marcação, Quente e Frio não são tocados. (2) **Reposicionar o funil com a IA** — lê a conversa de cada lead e grava a etapa com `motivo='ia_lote'`. Ficam **de fora**: lead com a atendente ou sem dono (não é atendimento de corretor — pedido do Ali por causa da Vanessa), sem conversa, com venda registrada e em etapa manual. Roda **em blocos** com barra de avanço e botão de parar: são centenas de conversas e uma requisição só estouraria o navegador. Lead já lido nas últimas 12h não é relido — parar no meio e continuar não paga duas vezes. O **custo estimado aparece antes do botão**. `ia_lote` conta como confirmação humana no score (o gestor autorizou a leitura sabendo o que ela faria). Teste: `npm run teste:lote`.
- **Os números do ranking passaram a ser DA PESSOA** (14/08/2026, `services/score.js` → `primeirasRespostas`, `temposDeResposta(ids, userId)`): `leads.first_resp_at` guarda a primeira resposta de QUALQUER pessoa, e na Conecta quem fala primeiro é a atendente, que depois repassa. O corretor aparecia com o tempo dela — ganhando nota por trabalho que não fez, ou levando a culpa — e a agilidade dele não existia no relatório. Agora os dois tempos contam **de `assigned_at`** (quando o lead ficou com ele) e só olham **as mensagens que ele escreveu**. `atendidos` também. Teste: `npm run teste:tempo`.
- **Primeiro atendimento fora do expediente** (16/08/2026, `services/robo.js` + `services/ia.js` → `atenderPrimeiroContato`; tela em Configurações → Fora do expediente, ligar só ADM): **é o ÚNICO lugar do CRM em que a IA fala com o CLIENTE** — todo o resto (resumo, etapa, temperatura) é leitura para gente de dentro. Nasce DESLIGADO: robô conversando no WhatsApp da imobiliária não pode começar porque uma versão nova subiu. A missão é estreita — **acolher e colher**.

  **Comprar ou alugar é a PRIMEIRA pergunta** (16/08/2026, `camposDa()`): são cinco campos nos dois casos, mas não os mesmos. Compra: renda, entrada, situação, CPF, prazo (os do formulário do Meta). Aluguel: renda, **orçamento**, situação, **garantia**, prazo — sem entrada, sem CPF, sem falar em simulação. Antes só existia o conjunto da compra, e ele perguntava "quanto tem de entrada?" para quem tinha acabado de dizer que queria alugar. A contagem "3 de 5" na conferência usa a lista do caso; na ficha, que só tem os campos da compra, orçamento e garantia entram na **situação** — jogá-los em `entrada`/`cpf` seria escrever mentira num campo com nome.

  **"Responda antes de perguntar"** é a primeira regra do prompt: reagir ao que a pessoa disse, responder o que ela perguntou, e só então fazer UMA pergunta. Emendar pergunta por cima do que o cliente falou é o que faz parecer robô — muito mais do que qualquer palavra.

  **Quando ele fala** — as três regras são do Ali: (1) **fora do expediente**, e são duas perguntas em ordem: *hoje tem expediente?* (`orgs.robo_dias`, padrão seg–sex) — no sábado e no domingo não tem, e aí ele atende **o dia inteiro**, sem olhar a hora; *se tem, estamos fora dele?* — a janela **18:00→09:00** atravessa a meia-noite (`a<b ? entre : a||b` — errar isso deixa o robô mudo a noite toda sem erro nenhum aparecer). Assim sexta 18h → segunda 9h vira um bloco contínuo sem nenhum caso especial. Às 09:00 a atendente assume, **e isso preserva o tempo de resposta DELA**, que é o número do relatório. Na tela marcam-se os **dias em que a EQUIPE trabalha**, não os do robô — invertido de propósito, senão a pergunta vira "marco sábado para ele atender no sábado?"; (2) só lead **sem corretor** — fila, com a atendente ou com o gestor são a mesma situação: ninguém tem esse atendimento no nome ainda. Lead repassado a corretor é o único caso em que ele se cala por causa do dono; (3) só quem está **sem retorno** (última mensagem é do cliente, mesma definição do `alerta.js`). Mais uma minha: **gente entrou, robô saiu para sempre** (`robo_parado`).

  **O que ele nunca faz**: valor de parcela/entrada/juros, dizer que aprovou, marcar visita, prometer ligação, e **dizer que a imobiliária tem imóvel num bairro/faixa de preço** (16/08/2026, primeiro teste real do Ali: ele respondeu "a gente tem opções bacanas por lá" para um bairro específico — nem sempre tem, e a pessoa cobra isso na segunda). Quando o cliente cita um bairro, ele anota em `situacao` e responde no espírito de "a gente encontra o imóvel ideal pra você". E o texto passa por `palavraProibida()` antes de sair — a resposta pode estar ótima e conter "podemos agendar sua visita", que move o lead duas etapas às 3h da manhã. Barrado não envia: a conversa fica parada para a atendente, que é onde ela estaria sem o robô. **`advanceStage` não roda enquanto o robô está atendendo.**

  **A última mensagem é sempre uma despedida** (16/08/2026): antes ele emudecia no teto, e o cliente ficava falando sozinho depois de "e qual a sua renda?". Agora a IA recebe quantas mensagens ainda cabem — faltando 2 ela começa a fechar, na última o aviso é explícito e o `encerrar` é forçado no código, tenha ela obedecido ou não. E o motivo mostrado na ficha separa as quatro causas de ele estar fora da conversa (gente respondeu / ele se despediu / bateu o teto / foi conferido): `podeAtender` devolve um `robo_encerrado` neutro e quem atribui a causa é `estadoNoLead`. **Gente x robô se distingue pela ASSINATURA (`ASSINATURA_ROBO`), não por ter autor** — mensagem digitada no WhatsApp Web não tem `from_user_id`, e a Vanessa cairia do lado do robô.

  **Não carimba `first_resp_at`** — senão um lead atendido só por robô apareceria no relatório como respondido por gente.

  **Atraso de 8–20s antes de responder**, e ele faz duas coisas: resposta em 2 segundos às 23h grita "robô"; e o `Set atendendoAgora` + a **reconferência depois da espera** impedem que duas mensagens seguidas do cliente virem duas respostas, e que o robô atropele a Vanessa que respondeu nesses segundos.

  **O disparo no webhook é SEM `await`** (`uazapi.webhook.js`). Webhook lento é webhook que a Uazapi desiste de chamar — e aí para de entrar lead. `atender` nunca lança.

  **A lista de conferência é par obrigatório, não extra**: a conversa que o robô atendeu tem a última mensagem da imobiliária e por isso SAI da fila de "cliente esperando". Sem ela, o robô trocaria "ninguém respondeu" por "ninguém percebeu que faltava responder". `GET/POST /config/robo/conferir`. Teste: `npm run teste:robo` (ele testa sobretudo **quando o robô fica calado**).

  **Religar num lead** (`POST /leads/:id/robo`, só supervisão; cartão "Atendimento da IA neste lead" na ficha): o robô sai da conversa quando gente responde, quando ele se despede ou quando a atendente confere — e até 16/08/2026 isso não tinha volta. Agora a gestão e a atendente religam num atendimento específico, e **religar zera `robo_msgs`** (religar e deixar mudo no teto seria o mesmo botão que não faz nada, com outra roupa). O cartão mostra o **estado real**, não só o botão: as regras gerais continuam valendo depois de religar, então ele diz o que aconteceria se o cliente escrevesse agora e por quê — e o motivo mostrado **prefere a causa do lead à do relógio** ("alguém já respondeu" em vez de "agora é expediente", que muda sozinha às 18h). O corretor não recebe o cartão: o servidor devolve `robo: null` para ele, e a permissão fica num lugar só.

  **A equipe ensina o robô a falar** (17/08/2026, tabela `robo_ensino`, `GET/POST/PATCH/DELETE /config/robo/ensino`; seção em Configurações → Fora do expediente): linhas curtas escritas pela ATENDENTE dizendo como a Conecta fala — tratamento, o que explicar sobre um programa, o que sempre perguntar. Entram no prompt a cada resposta. **A atendente edita, o gestor também; ligar o robô continua só do ADM** — saber como se fala com o cliente é o trabalho de quem atende, decidir que existe um robô falando não é. Desligar guarda o texto (testar uma orientação e voltar atrás não pode custar o que foi escrito). Teto de 30 linhas, porque cada uma entra em TODA mensagem e vira dinheiro em toda conversa.

  **O ensino entra DEPOIS das proibições, e o prompt diz que não as contraria** — é texto que alguém de fora do código escreve e a IA lê como instrução; na outra ordem, bastaria escrever "pode falar o valor da parcela" para a trava mais importante cair. O teste 28 compara as posições no pedido e quebra se a ordem inverter.

  **Limitação conhecida:** áudio. A mensagem de voz chega como o rótulo "Áudio" — a IA vê que chegou algo e não ouve o conteúdo. O prompt manda pedir por escrito. Transcrição é outro serviço e outro custo.
- **Temperatura só existe quando alguém a coloca** (16/08/2026, `services/lote.js` → `rodarTemperaturaIA`, colunas `leads.priority_por` / `priority_em`): o lead nascia **MORNO** por padrão da coluna — não era leitura de ninguém, e era o morno de ninguém que enchia o Kanban e o relatório. Agora **nenhum caminho de entrada marca temperatura**: nem o WhatsApp, nem o Meta (a nota de corte do formulário saiu de `stages.js`; as respostas continuam em `qual_json` e aparecem na ficha). Ela passa a ter duas origens, e as duas ficam gravadas em `priority_por`: `mao` (o corretor na ficha) e `ia` (a análise que o gestor pede). No card do funil e na catraca, **sem temperatura = sem pastilha** — escrever "sem temperatura" num card de 160px gasta a linha mais visível para dizer que não se sabe.
- **Análise de temperatura por IA, um corretor por vez** (16/08/2026, tela "Base de leads" → Arrumar a base; `GET/POST /leads/lote/temperatura-ia[/:corretorId]`, só ADM): o gestor escolhe **um nome**, vê quantos atendimentos seriam lidos e quanto custa, e manda rodar. A IA lê cada conversa daquele corretor e responde quente/morno/frio olhando **o que o cliente fez**, não o que a imobiliária escreveu (`services/ia.js` → `temperaturaDaConversa`; na dúvida entre dois, escolhe o menos quente). Por corretor porque a pergunta do Ali não é "como está a base", é "como esta pessoa está atendendo" — e assim a leitura fica ao lado do nome de quem atendeu. Ficam de fora os mesmos de sempre: quem está com a atendente (a SDR não entra, e a recusa explica por quê), sem conversa, com venda registrada ou em etapa marcada na mão. **Aqui a IA ESCREVE** — é a única coisa em que ela escreve, porque temperatura não vira cobrança em reunião e o corretor corrige na ficha. Etapa continua sendo sugestão. Teste: `npm run teste:lote`.
- **Lead sem temperatura não é "morno" na conta** (16/08/2026, `services/score.js` → `GRUPOS`/`grupoDe`): o score tinha três grupos e jogava o nulo no do meio. Com a base inteira sem temperatura, a "conversão dos mornos" virava a conversão da base com outro nome. Agora existe o grupo **SEM**, e a recomendação de direcionamento para um lead sem temperatura cai no desempenho da semana — que é a resposta honesta: "não sei como este perfil converte, mas sei quem está atendendo bem agora".
- **Bloco de IA que falha inteiro para o laço** (16/08/2026): as rodadas em lote devolvem `marcados`/`lidos` **separado de** `analisados`, e a tela para quando um bloco inteiro falha, mostrando o motivo. Antes, chave de IA inválida enchia a barra de progresso e terminava com "a IA leu 4 atendimentos" — sem ter lido nenhum. Junto: conversa que falhou **não fica marcada**; o motivo costuma ser a instalação, e marcar deixava o lead 12h fora da fila mesmo depois de o gestor consertar a chave.
- **Visita só conta quando uma pessoa confirma** (14/08/2026): `Agendamento`/`Visita` são etapas que a palavra-chave move sozinha; enquanto a equipe não usa as palavras, o número descreve o palpite da regra. O score passou a usar `visitas_confirmadas` — leads cuja etapa atual tem uma linha em `lead_etapas` com `motivo` em `mao`/`ia`. A tela mostra `confirmadas/total` e uma tarja âmbar dizendo quantas vieram da regra automática. **O histórico começou em 13/08/2026**, então o número nasce zero para todo mundo — zero honesto em vez de um número que ninguém reconhece.
- **Botão que some no iPhone** (14/08/2026): `-webkit-overflow-scrolling:touch` estava em 24 containers. No iOS essa propriedade legada faz `position:fixed` se comportar como `absolute` — a folha que abre por cima fica **presa dentro da área que rola**, e o botão do WhatsApp (popup do funil) e o Imprimir (relatório de reunião) saíam da tela. Removida: desde o iOS 13 a rolagem por inércia é o padrão e ela não faz mais nada além do estrago. Junto: as folhas usam a classe `.tela-cheia` (`height:100vh;height:100dvh` — no iPhone `100vh` é a tela COM a barra do navegador escondida, maior que a área visível), o popup para **acima** da barra de navegação (`usarAlturaDaBarra` mede a barra, que muda de tamanho com a faixa do iPhone) e o relatório ganhou um **segundo botão de imprimir no fim do documento** — ação importante com um caminho só é ação que some quando esse caminho falha.
- **Celular: o que estava fora do alcance** (14/08/2026): a tabela do score tinha 696px de largura numa tela de 375 — **6 das 10 colunas não existiam no celular**, sem nada indicando que dava para arrastar. Virou um cartão por corretor. Junto: `Metric` encolhe a fonte do número em vez de cortar (`R$ 400.000` saía como `R$ 400.00`, e quem bate o olho lê quarenta mil), a faixa de corretores quebra em linhas em vez de cortar o último ao meio, o cartão do corretor empilha (o botão "Relatório para reunião" ia de 174×30 para 313×41) e as pílulas de período passaram de 22–26px para ~34px. **Abaixo de ~32px o dedo erra** — é a régua usada aqui.
- **Lead repassado sobe na caixa do corretor** (13/08/2026, coluna `leads.assigned_at`): a ordem usava a data de ENTRADA do lead, então um lead de junho repassado agora afundava atrás de leads antigos — só por ser antigo — e o corretor não via justamente o que acabou de receber. Agora sobe ao topo o que chegou na mão dele nas últimas 24h, com o selo **"novo com você"**, junto com quem está esperando resposta (as duas coisas somam). `assigned_at` é carimbado em toda atribuição e zerado ao devolver para a fila.
- **A notificação desligava sozinha no celular do corretor** (17/08/2026): eram três buracos, e o primeiro era nosso. (1) **`atualizarConHub()` desregistrava o service worker** — e desregistrar o SW **destrói a inscrição de push junto**. Como esse é o botão "Atualizar agora" do aviso de versão nova, cada publicação desligava o aviso de lead de quem atualizasse. Pior: não adiantava nada, porque o nosso `sw.js` não guarda um único arquivo em cache. Agora usa `registration.update()`. (2) O navegador **troca a inscrição sozinho** e avisa só o service worker (`pushsubscriptionchange`); sem alguém reinscrever, o endereço velho vira 410 e o servidor apaga. O `sw.js` agora reinscreve e chama `POST /push/trocar` — **sem login, porque o service worker não tem o token**: a prova de posse é conhecer o endereço antigo, e o serviço só TRANSFERE uma inscrição existente, nunca cria uma. (3) **Rede de segurança**: ao abrir a tela, se `Notification.permission==="granted"` e não há inscrição, o app refaz em silêncio — a pessoa já autorizou, e ninguém descobre sozinho que parou de receber aviso. É o que conserta os aparelhos já quebrados. Teste: `npm run teste:push`.
- **O repasse diz se o corretor vai mesmo ser avisado** (13/08/2026): `POST /distribution/{transfer,next,handoff}` devolvem `aviso: {push, motivo}` — `sem_push_no_servidor` (falta VAPID) ou `corretor_sem_notificacao` (ele nunca ativou). A tela mostra uma tarja âmbar. Antes o repasse respondia "ok" nos dois casos e a atendente passava o lead achando que alguém tinha sido chamado; **lead entregue a quem não sabe que recebeu fica parado exatamente como se não tivesse sido entregue**. O disparo do push continua fora do fluxo da resposta — o que é imediato é a conferência "há chave VAPID? este corretor cadastrou aparelho?".
- **Barra lateral larga e agrupada** (25/08/2026, a partir de uma referência que o Ali mandou): no PC a barra era uma tira de 76px só com ícones, e metade do menu ficava atrás de um "Mais" — justamente onde sobra largura de tela. O gestor precisava de dois cliques para chegar em Relatórios, Base de leads e Equipe, e não descobria que existiam sem abrir o "Mais". Agora tudo aparece de uma vez, com o nome escrito e separado por seção (**Principal, Ferramentas, Gestão, Configurações**), mais a imobiliária no topo e "nome · papel" no rodapé. O rótulo da seção não é enfeite: é ele que diz que "Base de leads" é gestão e não atendimento. **Cada item tem um ícone próprio** (25/08/2026): "Equipe" e "Minha conta" usavam o mesmo desenho de duas pessoas, e "Funil" e "Base de leads" o mesmo de colunas — recolhida, onde só sobra o ícone, viravam pares indistinguíveis. Entraram `user` (uma pessoa) e `lista` (linhas de tabela). O teste do navegador compara o SVG de cada botão da barra e acusa qualquer repetição. **O "Mais" deixou de existir no PC** e continua no celular, onde a largura é escassa de verdade (`LIMITE_NAV`). A barra encolhe de 236px para 200px abaixo de 1200px, porque a tela de conversas (lista + conversa + ficha lado a lado) é quem paga a largura que ela tomar. **Recolher é preferência da CONTA** (25/08/2026, `users.barra_recolhida`, `POST /auth/me/barra`): a pessoa escolhe uma vez e vale em qualquer computador — foi "por conta" o que o Ali pediu, e é coisa de notebook (no celular a barra lateral nem existe). Recolhida tem 64px, só ícones com o nome no `title`, o rótulo da seção vira um risco e o contador vira uma bolinha no canto do ícone — o número não cabe, mas sumir com ele esconderia o aviso de lead esperando. **Rota própria, fora do `PATCH /me`**: aquele valida nome/e-mail/telefone e devolve TOKEN NOVO (o nome assina as mensagens no WhatsApp), e um clique de layout não pode passar por isso. **O clique também não mexe na sessão** — mexer derrubava o master de volta ao hub. E **`toSession` copia campo a campo**: preferência que não for listada lá chega do servidor e é jogada fora em silêncio, exatamente como já tinha acontecido com o `master`. **A densidade da referência NÃO foi copiada** — aquele sistema é de análise no computador, e este é usado em pé, no celular, com o cliente esperando.
- **Barra de navegação** (13/08/2026): a ordem é a mesma no PC e no celular; **o celular corta em 4 itens + "Mais"** (`LIMITE_NAV`), o PC mostra tudo na barra lateral. A barra do PC é uma coluna com a tela inteira de altura sobrando; a do celular é uma linha de 375px, onde sete ícones viram alvos que o dedo não acerta. Como a ordem é a mesma, o que está à vista no celular está à vista no PC. **Funil** ficou logo abaixo de **Painel**, e **Plantão** saiu do "Mais".
- **Escolher quais fotos do imóvel enviar** (13/08/2026, `POST /leads/:id/produto` aceita `fotos_ids`): sem a lista continua indo o anúncio inteiro (o padrão e o caso comum). Com ela vão só as escolhidas, sempre **na ordem do anúncio** — a primeira foto é a capa que o captador definiu e é ela que leva a legenda no WhatsApp. Quando não vão todas, o registro na conversa diz `(2 de 5 fotos)`: dois dias depois, ninguém saberia se o cliente viu o anúncio inteiro ou três fotos escolhidas.
- **Onde cada cartão do lead aparece** (13/08/2026): **Resumo da IA** e **Tarefas** aparecem no popup do funil E na ficha; a **Etapa lida pela IA** fica **só na ficha**. Duas telas oferecendo a mesma decisão é como o mesmo lead acaba movido duas vezes.
- **Filtros do Kanban** (26/08/2026, pedido do Ali): o quadro é a ferramenta de leitura do funil e respondia uma pergunta só — "como está a imobiliária inteira". A pergunta que a gestão faz é "como está o funil DA MARINA". Ganhou **busca sempre à vista**, com um **seletor de onde procurar** grudado nela — *Lead ou corretor* (padrão), *Só o lead*, *Só o corretor* (26/08/2026, pedido do Ali). O campo único acertava, mas não CONTAVA o que estava fazendo: "juliana" é o nome de uma corretora e de um lead, e devolvia os dois montes juntos sem dizer de onde veio cada um. O padrão continua sendo os dois, que é o que serve a quem já sabe o que digitou; o seletor existe para o nome ambíguo, que é justamente quando a busca única confunde. Telefone só casa em "lead" — número é do cliente. O seletor **não aparece para o corretor**: ele só vê os próprios leads, e "buscar por corretor" ali devolveria sempre a mesma coisa. Junto, uma **gaveta "Filtros"** com corretor (incluindo "na fila, sem dono"), temperatura e período. A peneira roda no navegador, sobre os leads já carregados. Um contador diz "X de Y" quando há filtro ligado: sem ele, o total das colunas deixa de ser o total da base e ninguém percebe que está lendo um funil filtrado.
- **Lead aberto de fora, no celular, abria a LISTA em vez da conversa** (26/08/2026, `usarAberturaDeFora`): Relatórios → etapa → nome do lead, funil, painel e Base de leads têm o mesmo gesto — toca no lead e cai na conversa dele. No computador funcionava (lista e conversa lado a lado); no celular só cabe um painel, e a tela de atendimento abria **sempre na lista** — a pessoa tocava no lead e chegava numa lista de sessenta conversas para procurar o mesmo lead na mão. Não era um caminho quebrado, eram todos: quem escolhe o lead está fora do componente de conversas, e ele nunca ficava sabendo. Por isso a regra virou um gancho usado pelos dois painéis (corretor e supervisão), e não um ajuste em cada botão — botão novo nasce funcionando. Vale para lead já escolhido na montagem e para o que chega um instante depois, quando a ficha ainda estava sendo buscada.
- **Funil (kanban) que responde sem clique** (13/08/2026): o card mostra temperatura escrita, **desde quando o lead está NESTA etapa**, quando foi a última conversa, a tarefa marcada (coral se venceu) e uma faixa de urgência no topo. A cor de urgência olha a **última interação**, não a idade do lead — lead de um mês que conversou hoje está saudável; lead de hoje sem resposta está em chamas. Verde não existe de propósito: quadro cheio de verde vira enfeite. Âmbar em 24h, vermelho em 72h.
- **Histórico de etapas** (13/08/2026, tabela `lead_etapas`, `services/etapas.js` → `moverEtapa`): TODA mudança de etapa passa por ali e grava de/para/motivo/quem. `motivo` é `mao`, `palavra`, `ia`, `venda` ou `reanalise`. **O histórico começa no dia em que entrou** — lead que nunca mais mudou não tem linha, e a tela mostra "—" em vez de usar a data de criação, que quase nunca é a mesma coisa. É o pré-requisito que faltava para medir "quantos avançaram para X no período" nos relatórios.
- **Popup do lead no funil** (13/08/2026): clicar no card abre um popup com as mesmas leituras da IA da ficha (resumo e etapa), as tarefas, e um botão do WhatsApp para ir à conversa. Antes o clique jogava direto na conversa — e para decidir o que fazer com um lead parado, ler 40 mensagens não ajuda.
- **Observações do lead** (25/08/2026, tabela `observacoes`, `GET/POST/DELETE /leads/:id/observacoes`): o quadro de recados do atendimento — "só atende depois das 18h", "quem decide é o marido", "já foi negado na Caixa em janeiro". Não é etapa, não é tarefa e não cabe na conversa, mas é o que quem atende precisa saber ANTES de falar. O caso que motivou é o **repasse**: o que a atendente descobria se perdia, e o corretor começava do zero ou relia quarenta mensagens.

  **Aparece em dois lugares, e o segundo é o que importa**: na ficha, onde se escreve e se apaga; e numa **faixa âmbar acima da conversa**, onde se lê. Durante o atendimento ninguém abre ficha — o cliente está esperando, e no celular a ficha é outra tela. Recado atrás de um botão é recado que não é lido. A faixa mostra a mais recente e abre o resto num toque.

  **É uma LISTA com autor e hora, não um campo de texto único.** Com um campo só, a atendente e o corretor escrevendo ao mesmo tempo se apagariam em silêncio — e observação perdida por acidente ninguém descobre que existiu.

  **Escreve quem pode abrir a conversa** (dono + supervisão): é de propósito que a atendente anote num lead que JÁ é do corretor, senão o recurso não serve para o caso que o criou. **Apagar é mais restrito**: o autor apaga o que escreveu, a supervisão apaga qualquer uma — o corretor não apaga o recado que a atendente deixou para ele. Vem junto no `GET /leads/:id`, para a faixa nascer com a conversa e não uma requisição depois. Teste: `npm run teste:observacoes`.
- **Tarefas do lead** (13/08/2026, tabela `tarefas`, `routes/tarefas.routes.js`): "ligar terça", "levar a pasta na Caixa". Pequeno de propósito — texto, data/hora e um risco para dizer que foi feito; sem repetição e sem lembrete por e-mail. A tarefa nasce no nome de **quem está com o lead**, não de quem escreveu: o gestor que combina algo na reunião está criando trabalho para o corretor. Data é obrigatória — sem ela a tarefa não é cobrável.
- **Sino na lista de conversas** (13/08/2026): a temperatura saiu do item da lista e no lugar entrou o sino, para a atendente chamar o corretor sem abrir o lead. Só para quem supervisiona, e só quando há corretor. Fica verde depois de chamado (com a hora no title) para ninguém cutucar duas vezes pelo mesmo motivo.
- **Nome do lead corrigível** (13/08/2026, `PATCH /leads/:id/nome`; lápis no topo da ficha): o nome que entra pelo WhatsApp é o que a PESSOA escolheu no aparelho dela — às vezes o número puro, às vezes o nome do marido. Quem atende corrige, e a supervisão também: quem descobre o nome verdadeiro é quem está conversando. O nome só é gravado quando o lead nasce (`uazapi.webhook.js`), nunca atualizado depois — então a correção não é desfeita pela próxima mensagem. Nome vazio é recusado.
- **Data/hora da última mensagem na lista de conversas** (13/08/2026, `fmtQuando` em `app.jsx`): régua do WhatsApp — hoje mostra a hora (`15:06`), ontem diz "Ontem", dentro da semana o dia da semana, e passando disso a data. O arranjo do item também virou o do WhatsApp: hora em cima à direita, contador de não lidas embaixo à direita. Antes não havia carimbo nenhum na lista.
- **A mensagem aparece como o cliente escreveu** (20/08/2026, `TextoDaMensagem` em `app.jsx`): o balão desenhava o texto num `div` sem `white-space`, então o navegador juntava tudo num parágrafo só. A mensagem de "dados para simulação" — sete linhas com marcadores — virava um bloco ilegível na tela do corretor, diferente do que a pessoa mandou. Agora vale `pre-wrap`, e `*negrito*` / `_itálico_` do WhatsApp são aplicados (só com conteúdo entre os sinais e na mesma linha, para "2*3" continuar sendo texto): mostrar os asteriscos crus é mostrar uma mensagem que ninguém escreveu.
- **O corretor tem os mesmos filtros da atendente** (20/08/2026, pedido do Ali): ele tinha cinco pastilhas e **nem busca por nome** — e é quem mais precisa achar "quem está na Pasta" no meio de sessenta conversas. Ganhou busca por nome/telefone (telefone comparado só por dígitos), a gaveta "Filtros" com etapa, temperatura, "só quem está aguardando" e período, mais o contador de conversas. Fica de fora o que não se aplica a ele: o escopo "Minha caixa / Toda a equipe" (é sempre a dele) e o filtro por atendente. A peneira é **no navegador**, sobre os leads que já vieram — a caixa dele já está carregada, e ir ao servidor a cada clique seria uma volta inteira para filtrar uma lista que está na mão.
- **Mensagens do WhatsApp entram na conversa** (06/08/2026): o que o corretor digita direto no celular ou no WhatsApp Web aparece no CRM como mensagem enviada, sem assinatura (o número é único e o WhatsApp não diz quem digitou — a tela mostra "Enviada pelo WhatsApp"). O eco do que o próprio CRM mandou é descartado pelo `messages.wa_id`. Mensagem enviada para um número que ainda NÃO é lead não cria lead: o número da Conecta também fala com colega e fornecedor.
- **Responder uma mensagem específica** (09/08/2026): a seta ao lado do balão cita a mensagem, como o Responder do WhatsApp. `messages.reply_to` guarda o id LOCAL da citada (o `wa_id` é buscado na hora de enviar), e o servidor devolve a citação já resolvida em `/leads/:id`. O envio manda `replyid` para a Uazapi; se ela recusar o campo, a mensagem sai com o trecho citado escrito em cima — nunca se perde a mensagem por causa da citação. Mensagens anteriores a 08/08/2026 não têm `wa_id`: a citação vale só dentro do CRM.
- **Editar mensagem enviada** (10/08/2026): regras do WhatsApp — até 15 minutos, só texto, só o que saiu daqui; o autor edita a própria, a gestão edita qualquer uma. **O texto no CRM só muda depois que a Uazapi confirma** (`editMessage` em `services/uazapi.js` tenta `/message/edit`, `/send/edit`, `/message/update`; 404 passa para o próximo, qualquer outro erro para). Se a edição não sai no WhatsApp, o banco não é tocado — CRM que mostra texto diferente do que o cliente recebeu deixa de servir de registro. `messages.body_original` guarda a primeira versão.
- **Áudio também tem prévia antes de enviar** (22/08/2026, `PreviaAudio` + `usarAudioPendente` em `app.jsx`): gravar NÃO envia. Ao parar, o áudio volta como prévia com player, e só sai no botão — dá para ouvir, **Gravar de novo** ou **Descartar**. Durante a gravação existe **cancelar**: antes o único jeito de sair era mandar, então quem apertava o microfone sem querer tinha que enviar áudio ao cliente. Não era permissão faltando na conta da atendente — **não existia para ninguém**, e o `onstop` mandava direto. Os dois campos de mensagem (`Atendimento`, do corretor, e `ComporADM`, da supervisão) são componentes diferentes e já divergiram antes; por isso a lógica vive num gancho só, usado pelos dois. Trocar de conversa descarta o pendente: áudio gravado para um cliente não pode sair na conversa de outro.
- **Colar imagem (Ctrl+V) mostra a prévia antes de enviar** (10/08/2026): colar NÃO envia — as imagens ficam numa faixa acima do campo, com miniatura, tamanho e um × para tirar a que veio errada. O que estiver digitado vai como legenda (a Uazapi só aceita legenda na primeira). Só o botão Enviar dispara; trocar de conversa descarta o que estava pendente. Imagem errada no WhatsApp do cliente não tem desfazer.
- **Resultado da ligação** (10/08/2026): clicar em Ligar abre o discador e grava a TENTATIVA na hora (`POST /leads/:id/ligacao` devolve o `ligacao_id`); ao voltar, um popup pergunta o que aconteceu — falou / não atendeu / caixa postal / número errado, mais uma observação opcional (`PATCH /leads/:id/ligacao/:ligId`). Só quem ligou responde, nem a gestão. As ligações entram na MESMA linha do tempo da conversa (juntadas na leitura em `/leads/:id`, não gravadas em `messages`). Sem a resposta, a tentativa continua no histórico — antes o relatório contava toques no botão.
- **Aviso de cliente sem resposta** (`services/alerta.js`): passado `orgs.alerta_resposta_min` (padrão 30, 0 desliga), o corretor recebe push. "Esperando" = a última mensagem da conversa é do cliente. `leads.alerta_em` impede repetição; nova mensagem do cliente volta a valer aviso. A gestão também cutuca na mão (`POST /leads/:id/cutucar`), e o pedido fica gravado no lead — sem isso, quem não tem push (todo iPhone fora da Tela de Início) não seria avisado de nada.
- **"Esqueci minha senha", pelo próprio cliente** (02/09/2026, pedido do Ali depois de ligar o Resend; `POST /auth/esqueci-senha`, `senhaEmail` em `services/mail.js`, link na tela de entrada): até aqui recuperar senha era **pedir ao gestor**, que gerava o link em Equipe e repassava no WhatsApp. Isso existia porque não havia provedor de e-mail; mantê-lo depois do Resend seria continuar dependendo de alguém estar acordado — e quem mais precisa disso é justamente quem tenta entrar às 22h de um domingo.

  **A resposta é SEMPRE a mesma**, exista ou não a conta, e essa é a trava principal: qualquer diferença — a frase, o código HTTP, o corpo — transforma a rota num **consultor de e-mails cadastrados**, e bastaria ir testando endereços para descobrir quais clientes usam o ConHub. Conta removida, recusada ou que nunca definiu senha recebe a mesma resposta e **nenhum e-mail**. O 400 de formato é a única exceção, e ele não conta nada: `"abc"` não é endereço, existindo conta ou não.

  **O link NÃO volta na resposta** — só no e-mail e no log. Aqui quem pede apenas digitou um endereço, que pode ser de qualquer um; devolvê-lo entregaria a chave da conta a quem sabe o e-mail alheio. Na rota do gestor ele volta, porque lá quem pede está logado e é o dono da casa.

  **O freio é duplo**: por IP (impede a varredura) e **por e-mail** — sem o segundo, um laço usa o ConHub para encher a caixa de entrada de alguém em nome da nossa marca, e quem paga com a reputação do domínio somos nós. Passado o teto a resposta continua sendo a mesma frase: "muitas tentativas para este e-mail" já contaria que ele existe.

  **Token de 24h, não de 7 dias** como o convite: um link que troca a senha de conta ATIVA é outra coisa, e quanto mais tempo ele fica valendo numa caixa de entrada, maior a janela de quem tiver acesso a ela. E o e-mail é **um modelo próprio**, não o de convite adaptado — aquele diz "seu cadastro foi criado", e mandar isso para quem usa o CRM há meses faz pensar em conta duplicada bem no momento em que a pessoa já está com dificuldade de entrar. Ele avisa que **dá para ignorar**: este e-mail pode chegar sem ter sido pedido (basta alguém digitar o endereço errado), e a frase é o que separa "alguém se enganou" de "invadiram minha conta". Teste: `npm run teste:esqueci-senha` (11 casos, com o servidor de pé).

- **A tela de criar senha tem saída** (02/09/2026, pedido do Ali; `public/definir-senha.html`): ela terminava dizendo "pronto, entre no ConHub" **e parava** — sem nenhum caminho para entrar. A pessoa acabava de criar a senha, estava no minuto exato em que ia usar o sistema pela primeira vez, e a única saída era digitar o endereço na mão ou procurar o e-mail de novo; o primeiro login de alguém é justamente onde se perde quem já tinha decidido entrar. Agora o sucesso termina num botão **"Ir para a tela de login"**, e o formulário e o erro têm o mesmo link mais discreto — no erro ele importa porque "link vencido" é o caso mais comum ali, e "fazer o cadastro de novo" é conselho errado para quem só esqueceu a senha. **A logo virou link para o site**: quem chega aqui veio de um e-mail e não tem menu nenhum na tela, e a logo era o único lugar em que ele instintivamente clicaria. Ela aponta para o **domínio** e o botão para `/app` — hoje caem na mesma porta, mas depois da separação (site em `conhubcrm.com.br`, sistema em `app.`) cada um continua indo para o lugar certo sem ninguém precisar lembrar de trocar.

- **Link de nova senha** (`POST /auth/users/:id/redefinir-senha`, **só ADM**; botão "Nova senha" na tela Equipe): gera um link de 24h para a pessoa criar outra senha, usando a MESMA página `/definir-senha`. `users.invite_tipo` diz se o token é `convite` ou `redefinicao` — a redefinição vale para conta JÁ ATIVA e não mexe no status nem no papel. Manda por e-mail se o Resend estiver ligado, e devolve o link de qualquer jeito para a gestão repassar no WhatsApp. Gerar outro derruba o anterior.
- **KPIs — o que cada número mede** (revisto em 10/08/2026): **venda** é contada pela `sale_date` dentro do período, não pela entrada do lead. Era o furo que fazia o relatório parecer parado: venda fechada hoje de um lead de junho não aparecia em "esta semana". **Recebidos** e **por_etapa** continuam sendo de quem ENTROU no período (coorte), e `conversao` é de coorte também. `agendamentos` é foto do momento ("onde estão hoje"), não "avançaram no período" — o banco não guarda a data de cada mudança de etapa; para medir avanço por período seria preciso um histórico de etapas, que ainda não existe.
- **Recomendação de direcionamento** (mudou em 10/08/2026): quando não há `AMOSTRA_MINIMA` (5) atendimentos resolvidos por temperatura, a sugestão passa a sair do **desempenho da última semana entre os 5 melhores** (`situacao: "por_desempenho_da_semana"`), em vez de responder "histórico insuficiente" — que na prática travava a sugestão por semanas.
- **Configurações** (11/08/2026, aba que substituiu "Conexão"): duas seções com donos diferentes. **Mensagens automáticas** (`/config/mensagens`, tabela `mensagens_rapidas`) — os botões prontos acima do campo de conversa; gestor **e atendente** editam, criam, ligam/desligam e ordenam, porque texto de abordagem muda toda semana e quem sabe é quem atende; o corretor só usa. Na primeira abertura a imobiliária é semeada com os quatro textos que a Conecta já usava. **Conexão** (`/config/conexao`, só supervisão) — lista de provedores (hoje só Uazapi, marcada como **API não oficial** com o aviso de risco de bloqueio na tela), estado da instância, **Desconectar** (só ADM, exige escrever DESCONECTAR), a URL do webhook para colar e um tutorial de contratação da Uazapi passo a passo dentro da ferramenta.
- **Resumo da conversa por IA** (11/08/2026, `services/ia.js` → `resumirConversa`, botão na ficha do lead): a IA lê a conversa e devolve em campos curtos — situação, o que o cliente quer, quanto pode pagar, o que ficou combinado, próximo passo, o que falta perguntar e um alerta de risco quando existe. Nasceu para o repasse: o corretor recebe um lead com 40 mensagens e precisa saber o essencial em dez segundos. Três regras: **é leitura, nunca escrita** (nada daqui vai para o cliente); **só no clique** (o texto da conversa sai do servidor rumo ao provedor de IA, e isso não pode acontecer sozinho em toda conversa aberta); e **resumo velho se anuncia** (`leads.resumo_msgs` guarda com quantas mensagens foi feito, e a tela diz quantas entraram depois). Fica guardado em `leads.resumo_json` para não pagar duas vezes pelo mesmo clique. Sem `ANTHROPIC_API_KEY` o cartão não aparece — como a leitura do print da Caixa.
- **Marca da imobiliária (white-label)** (26/08/2026, `services/marca.js`, `orgs.logo_url/logo_key/cor_barra`, `GET/PATCH /config/marca` + `POST/DELETE /config/marca/logo`; aba Identidade em Configurações): a plataforma era multi-imobiliária por dentro — org, código, WhatsApp e assinatura próprios — e uma só por fora. Quem assina o sistema não pode ver a marca de outra imobiliária na tela da própria equipe, e foi isso que mudou. O gestor sobe a **logo** e escolhe a **cor do menu**; a barra do computador e a do celular passam a ser dele. **O nome da imobiliária virou o título da barra**, com "ConHub" embaixo: numa plataforma que outras imobiliárias assinam, a equipe abrir o sistema e ler o nome do fornecedor em cima do próprio é o oposto do que a marca na barra existe para fazer.

  **Só a barra muda de cor.** Coral e âmbar continuam fixos no código: são o cronômetro que esquenta e a tarefa vencida. Se a imobiliária escolhesse vermelho como cor de marca, a tela inteira ficaria vermelha e o sinal de urgência deixaria de existir — a personalização apagaria justamente a informação que o CRM existe para dar.

  **Cor clara é RECUSADA, com a versão escura da mesma cor junto** (`escurecerAte`, contraste ≥ 4.5:1 com o branco). A barra escreve em branco: cor clara não deixa a barra feia, deixa o menu ilegível, e quem escolheu só descobre quando um corretor reclamar. A recusa seca faria o gestor desistir da cor da marca dele, então ela vem com um tom mais escuro **da cor dele**, num botão. Escurecer multiplica os três canais pelo mesmo fator — amarelo continua amarelo, só mais escuro; se não, seria um preto disfarçado.

  **A marca vem JUNTO com a imobiliária** (`orgDoUsuario`, `/auth/me`, `/orgs/:id/entrar`), nunca numa requisição depois: senão a barra nasce verde e troca de cor um instante depois, em todo login — e o piscar diria à equipe que a marca dela é enfeite aplicado com atraso. Por isso o `resumo()` de `orgs.routes.js` também a devolve: é o que o master recebe ao ENTRAR numa imobiliária.

  **Nada salva sozinho** (26/08/2026, pedido do Ali): escolher a logo e escolher a cor mexem num RASCUNHO, e a barra da equipe só muda no botão **Salvar alterações** — um botão para as duas escolhas, com **Descartar** ao lado e um aviso âmbar de alteração pendente. Vale inclusive para a LOGO: o arquivo fica no navegador e só sobe no Salvar, senão cada logo experimentada e descartada deixaria arquivo pago no armazenamento. A **cor vai antes da logo** na gravação: é a única coisa que o servidor pode recusar, e salvar a logo primeiro deixaria o gestor com a marca trocada depois de ler "não pode". A sugestão de cor escura também vira rascunho — aceitar a sugestão não publica.

  **Quem mexe é o GESTOR, e só no computador** (pedido do Ali): escolher logo é procurar um arquivo, e o arquivo da marca está no notebook; escolher cor é olhar o resultado numa tela grande. Mesma régua de recolher a barra. No celular a aba não existe, e uma linha diz onde ela está — recurso que some sem explicação vira chamado. **O ícone na tela de início continua o do ConHub**: o `manifest.json` é um arquivo só, gerado no build, e por imobiliária exigiria recortar ícone em vários tamanhos — ficou para depois, de propósito.

  **"Conecta" saiu de tudo que o cliente e uma imobiliária nova veriam**: o `· Conecta` ao lado de quem enviou, "Número da Conecta", "Conexão da Conecta", o split de comissão e — o mais caro — o prompt do robô, que dizia `Você atende o WhatsApp da Conecta Imóveis, uma imobiliária de Petrolina/Juazeiro`. Esse texto sai pelo WhatsApp, para o cliente: seria a IA se apresentando com o nome do concorrente na primeira mensagem do primeiro contato. Agora o nome vem da org (`instrucaoAtendimento(imobiliaria)`), e a cidade saiu — dizer a região errada a quem mora em outra é pior que não dizer região nenhuma. O teste 29 do robô quebra se algum nome voltar a ficar fixo. Teste: `npm run teste:marca`.
- **Convite que nunca virou conta sai da lista** (26/08/2026, `DELETE /auth/users/:id`; botão "Apagar cadastro" na tela Equipe): um cadastro de teste ficou **preso para sempre** na Equipe como "Não confirmou o e-mail", sem nenhum botão que o tirasse de lá. O buraco não aparecia olhando cada parte sozinha — a tela só oferece **"Remover"** para conta ATIVA e **"Apagar de vez"** para conta JÁ REMOVIDA, e o servidor exigia `status='removido'`. Quem nunca definiu senha não é nenhum dos dois: duas metades que não se encontravam, e no meio delas um cadastro imortal. Agora `pendente` e `recusado` são apagados direto, e isso é seguro justamente porque a pessoa nunca entrou — não atendeu ninguém, não há conversa nem relatório apontando para ela, então o histórico que a regra "remova primeiro" protege não existe. O aviso de confirmação muda junto: a frase genérica sobre "conversas antigas" faria o gestor hesitar diante de uma decisão sem consequência. Junto, a trava do **"único gestor ativo"** passou a valer só para quem ESTÁ ativo — tirar da equipe um gestor que nunca confirmou o e-mail não muda a cobertura da imobiliária, e a trava mordia justamente quando o único gestor ativo era o master (que `semMaster` não conta). Teste: `npm run teste:apagar-convite`.
- **Tela de login: foto de fundo e título de aba** (26/08/2026, pedido do Ali): a foto é fundo do **cartão inteiro**, não de uma coluna — a imagem é 16:9 e, enfiada só na coluna da esquerda (quase quadrada), a pessoa saía do quadro. Três camadas, de baixo para cima: o verde profundo (**é o que resta se o arquivo faltar** — a porta de entrada nunca depende de um arquivo carregar), a foto, e o degradê. O degradê é **vertical, não horizontal**: a metade direita fica atrás do formulário branco, que é opaco, então um degradê da esquerda para a direita gastaria toda a transparência na parte que ninguém vê e apagaria a foto justo onde ela aparece. O arquivo vai em `backend/public/login-fundo.jpg`. No celular, `gridTemplateRows:"auto 1fr"` — sem isso as duas linhas dividiam a altura em partes iguais e sobrava meia tela verde vazia acima do campo de e-mail.

  **A foto sobe PELA TELA, no hub** (26/08/2026, tabela `config_plataforma`, `GET/POST/DELETE /orgs/login-fundo`, rota pública `GET /login-fundo.jpg` no `server.js`): colocar o arquivo dentro do projeto exigiria mexer no GitHub a cada troca — e ele sumiria no deploy seguinte, porque o disco do container é descartável. A imagem vai para o mesmo armazenamento das fotos dos imóveis (R2/disco persistente) e o endereço fica no banco. A rota pública **redireciona** para o arquivo e responde 404 quando não há foto: o navegador só não pinta a camada e o verde de baixo continua ali. Ela fica **antes do `express.static`** — se alguém puser um `login-fundo.jpg` em `public/`, o static responderia primeiro e a escolha feita na tela deixaria de valer sem nenhum erro aparecer. A prévia no hub mostra a foto **com o degradê e o texto branco por cima**: miniatura crua engana, porque foto clara demais só se revela quando o texto entra em cima. A tabela é chave-valor e não coluna em `orgs` porque o que mora nela não é de imobiliária nenhuma.

  **O título da aba passou a dizer a tela** (`usarTitulo`): "Login | ConHub", "Relatórios · Imobiliária Modelo | ConHub". O CRM é uma página só, o endereço não muda ao trocar de tela, e com quatro abas abertas — que é como a gestão trabalha — todas ficavam iguais. A ordem é **tela primeiro** porque a aba corta o fim quando estreita: o que sobra ao lado do ícone precisa ser o que muda, não a marca que se repete.

  Junto, o texto da porta de entrada foi corrigido: ele prometia "funil que anda sozinho conforme a conversa evolui", e o avanço automático saiu no mesmo dia. Promessa falsa na tela de login é a primeira coisa que o cliente descobre não ser verdade.
- **O cliente ativa a própria cobrança, e digita UM campo** (27/08/2026, `POST /assinatura/asaas`): a rota exigia nome, e-mail, telefone e valor no formulário, e na prática era o Ali preenchendo os dados de cada cliente — cada conta nova virava uma digitação dele. Nome, e-mail e telefone o CRM já tem (são os do titular da conta), então sobrou o **CPF/CNPJ**, que é o único dado que o sistema não tem e que o Asaas exige.

  **E O VALOR SAIU DO FORMULÁRIO.** Vinha do corpo da requisição, e a rota é `soDono` — num cliente o dono é ele mesmo, então quem ativasse a própria assinatura escolheria quanto paga. Agora o servidor usa `orgs.valor_mensal` e ignora o que o cliente mandar; só o master pode definir preço. Conta sem valor definido recusa com a frase certa ("fale com o ConHub para combinar o plano") em vez de criar cobrança de R$ 0.

  **O furo de verdade estava um passo antes**: `PATCH /assinatura` também é `soDono`, e deixava o cliente gravar `valor_mensal = 1` e só então ativar. Fechar só a tela de ativar teria deixado a porta dos fundos aberta. Agora valor, vencimento e carência exigem master nas duas rotas, e os campos somem da tela do cliente — campo que não vale nada é pior que campo nenhum, porque promete uma edição que vai dar erro. Sobra o nome do plano, que é rótulo.

  **`GET /assinatura` devolve `valor_mensal` separado do `valor` da situação**: a situação só carrega valor quando existe cobrança em curso, e conta sem vencimento nenhum é exatamente a que precisa ATIVAR — sem o campo, o cliente com plano definido via "o valor ainda não foi definido". O preço do autônomo é gravado na criação da conta, pelo hub.
- **Gerenciar assinatura: os três planos do corretor autônomo** (27/08/2026, pedido do Ali; `services/planos.js`, `orgs.plano_id`, `pagamentos.meses`, `GET /assinatura/planos` + `POST /assinatura/plano`): mensal **R$ 297**, semestral **R$ 247/mês** (R$ 1.482 a cada 6) e anual **R$ 197/mês em 12x no cartão**. Ele escolhe, digita o CPF e vai direto para a tela de pagamento. **Só do autônomo** — o preço da imobiliária é negociado caso a caso e continua em `orgs.valor_mensal`; três preços de prateleira ao lado do plano dela seriam três ofertas que não existem, e a rota devolve 404 para ela.

  **A tabela mora no servidor, não numa tela de configuração.** O preço é o que o cliente paga: campo editável em algum lugar é preço que muda por engano. O cliente manda um `plano_id`; o valor sai da tabela — é a mesma trava do valor, valendo no caminho novo.

  **O anual é PARCELADO, não assinatura, e a tela diz isso.** Assinatura anual no Asaas cobra os R$ 2.364 de uma vez por ano — que é outro produto. O que foi vendido foi a parcela, então ele vira `POST /payments` com 12 parcelas, e o cartão do plano avisa que no fim do ano ele renova. Mensal e semestral são `/subscriptions` e renovam sozinhos. Trocar de plano **cancela a assinatura anterior** — sem isso o corretor descobriria as duas cobranças no extrato do mês seguinte; e a falha do cancelamento não derruba a troca, senão a limpeza do plano velho deixaria o cliente sem plano nenhum.

  **O vencimento passou a andar por MESES PAGOS, não por número de pagamentos** (`pagamentos.meses`, `mesesPagos`). Era `COUNT(*)`, e valia enquanto todo plano fosse mensal: uma cobrança semestral de R$ 1.482 empurrava a data trinta dias, e quem tinha acabado de pagar meio ano era bloqueado no mês seguinte. Agora a conta é `valor ÷ preço mensal do plano` — que acerta os três casos e também o anual pago à vista (2364 ÷ 197 = 12) sem nenhum caso especial. Linha antiga, sem o campo, vale 1, que é o que ela sempre valeu. Nunca zero: cortesia de R$ 1 creditando nada deixaria o cliente bloqueado logo depois de pagar.

  **Contratar durante o teste não encurta o teste**: a primeira cobrança vence no dia em que o teste acaba. Contratar no terceiro dia cobrando pelos onze que sobram seria cobrar por período já vendido como grátis.

  **O CPF é conferido por DÍGITO.** "111.444.777-35" tem 14 caracteres — do tamanho de um CNPJ —, então uma conferência feita no texto cru aceitaria qualquer coisa pontuada.

  **Cartão nenhum passa pelo ConHub.** O corretor é levado para a fatura hospedada pelo Asaas, e é lá que ele digita o cartão. Um formulário de cartão aqui colocaria o CRM dentro do escopo de PCI-DSS e faria o Railway trafegar dado de cartão, para ganhar uma tela que o Asaas já entrega auditada. Se o navegador bloquear a aba nova, o endereço fica num cartão na tela — plano contratado sem caminho para pagar seria o pior desfecho possível.

  **E dá para assinar de dentro da TELA DE BLOQUEIO.** Sem isso o autônomo caía num beco: o teste acabava, o CRM travava, e a única tela que sobrava dizia "fale com a gestão" — mas a gestão é ele, e escolher plano morava em Minha conta, que é justamente o que o bloqueio esconde. Funciona porque o roteador da assinatura é montado **antes do porteiro** em `server.js`. Teste: `npm run teste:planos`.
- **Dar baixa em pagamento é de quem RECEBE** (27/08/2026, `soCobranca`): `POST /assinatura/pagar`, o apagar, o corrigir e o `reorganizar` eram todos `soDono` — e o dono da conta, num cliente, é o próprio cliente. Ele clicava em "Registrar pagamento" e ganhava um mês, quantas vezes quisesse; bloqueado, o mesmo clique destravava a conta ("já paguei — liberar acesso"). Era o furo do preço outra vez, num botão diferente: a régua de "o que vira dinheiro é do ConHub" não tinha alcançado a baixa manual. Agora as quatro exigem master, e os botões somem da tela do cliente — botão que sempre dá erro é pior que botão nenhum. Ele continua **vendo** o histórico, que é o extrato dele, e o "já paguei" vira o "Verificar de novo" que já existia.
- **Corretor autônomo: a imobiliária de um corretor só** (27/08/2026, `orgs.tipo`/`orgs.trial_ate`, `POST /orgs/autonomos`, `POST /orgs/autonomos/:id/liberar`; seção própria no hub): por dentro é uma org como qualquer outra — e é por isso que saiu barato: WhatsApp próprio, kanban, funil, IA, expediente, importação de leads, marca e mensalidade já existiam. O que muda é o **tamanho da casa**.

  **Duas travas, e as duas na porta de entrada** (`/auth/register`): corretor não entra (o corretor é ele; uma segunda pessoa atendendo já é imobiliária, que é outro plano) e atendente vai até **um**. Sem elas o link de cadastro montaria uma equipe inteira numa assinatura vendida como individual. A recusa vem com a razão escrita — quem cadastra é alguém que o corretor convidou, e precisa entender por que foi barrado.

  **A catraca some do menu** (`NAV.filter`): ela distribui entre corretores disponíveis, e ali existe um só — a fila teria sempre uma pessoa. O repasse pela ficha continua, porque o atendente dele precisa dele.

  **O teste de 14 dias começa quando a conta é EFETIVADA**, no `set-password`, e não na criação (pedido do Ali). Criar na segunda e mandar o link na quinta não pode custar três dias a quem ainda nem tinha entrado. Só o `fundador` dispara: o atendente que chega depois não reinicia o teste da casa.

  **O teste reaproveita a máquina de vencimento** em vez de inventar outra — o fim do teste É o primeiro vencimento, e o `cobrando` que já barra imobiliária em atraso barra ele também (402). O que muda é a FRASE: `situacao()` devolve `status:"teste"` com os dias, e a faixa no painel dele conta "faltam 14, 13, 12…" em verde, âmbar na última semana e coral nos três últimos dias. Entrando o primeiro pagamento o teste deixa de valer sozinho e a conta vira assinatura comum. `POST /autonomos/:id/liberar` é o "pagou libera, não pagou trava" num botão, sem tocar no histórico de pagamentos.

  **O hub separa as duas listas**, e não é enfeite: de uma imobiliária o master quer saber equipe e movimento; de um autônomo, quase sempre em teste, o que importa é um número só — quantos dias faltam. Misturados, a segunda pergunta se perde.

  **O master ENTRA na conta do autônomo, igual numa imobiliária** (27/08/2026, pedido do Ali): o servidor nunca distinguiu tipo de conta em `POST /orgs/:id/entrar` — faltava o botão na lista, e conta sem botão é conta sem socorro. Quando o autônomo diz que a mensagem não sai ou que o lead não aparece, ninguém ajuda de fora; é preciso ver a tela dele.

  **E entra mesmo com a conta TRAVADA**, que é justamente quando ele precisa entrar. Eram duas portas fechadas: o `porteiro` (`services/assinatura.js`) devolvia 402 para qualquer um, e o frontend desenhava a tela de bloqueio antes de qualquer coisa. Se a trava valesse para o master, o suporte a quem não pagou teria que COMEÇAR liberando quem não pagou. Agora os dois abrem exceção para ele — o `porteiro` conferindo no BANCO e não no crachá (o token dura 30 dias, e um master despromovido hoje continuaria entrando por um mês) — e uma tarja coral no alto avisa que a conta está travada **para o cliente**, senão ele mexe numa conta parada achando que está no ar.

  **A assinatura passou a ser rebuscada ao TROCAR de casa** (`useEffect` com `org.id` nas dependências): entrar numa conta pelo hub não troca a sessão, é a mesma pessoa. Sem isso `entrarNaConta` zerava a assinatura e nada buscava a nova — o master ficava até uma hora sem saber se a conta em que acabou de entrar está em dia, vencendo ou travada. Valia para toda imobiliária, não só para o autônomo. Teste: `npm run teste:autonomo`.
- **Convite de SÓCIO da plataforma** (26/08/2026, `GET/POST /orgs/masters`, `DELETE /orgs/masters/:id`; seção "Sócios e administradores" no hub): antes só existiam duas portas — `/cadastro?c=CODIGO`, que põe alguém dentro de UMA imobiliária, e a variável `MASTER_EMAIL` no servidor, que promove uma conta já existente. Sócio novo exigia mexer na configuração da hospedagem, que é justamente o que o Ali não faz sozinho.

  **NÃO é um link com código, como o dos corretores, e a diferença é de categoria.** O link do corretor pode circular no grupo da equipe: quem entra cai numa imobiliária só, com papel limitado, e ainda passa por aprovação. O master vê TODAS as imobiliárias, os clientes de cada uma e o que cada uma paga. Por isso o convite é **nominal**: nasce preso a um e-mail, vale **48h** (contra 7 dias do corretor), serve **uma vez** e morre quando a senha é definida. A tela diz isso por escrito — sem a frase, o link parece o mesmo dos corretores e vai parar no lugar errado por analogia.

  **E-mail que já é de alguém de equipe é RECUSADO, não promovido.** Digitar o e-mail da corretora e transformá-la em administradora da plataforma inteira, em silêncio, é o pior desfecho possível desta tela.

  **O sócio convidado entra DIRETO ao definir a senha** (`socio` no `set-password`, ao lado do `fundador`). Sem isso ele cairia em `aguardando_aprovacao` dentro de uma imobiliária qualquer e ficaria **preso lá para sempre**: `semMaster` mantém o master fora da lista de equipe, que é onde o gestor aprova — um convite que não dá para aceitar nem recusar. A aprovação já aconteceu quando outro sócio o convidou.

  **Tirar o acesso DESATIVA a conta** (o histórico fica e dá para reativar pela tela Equipe). Antes só o crachá caía, e a pessoa virava gestora comum da imobiliária onde a conta nasceu — visível na Equipe, com acesso aos leads daquela casa. Quem deixa de ser sócio do ConHub não vira, por tabela, gestor de um cliente do ConHub, e **o último sócio ativo não pode ser removido** — sem essa trava a plataforma fica sem ninguém que possa criar imobiliária ou convidar sócio, e não há caminho de volta pela tela. Teste: `npm run teste:socio`.
- **Cópia de segurança diária do banco** (27/08/2026, pedido do Ali; `services/backup.js`, `GET/POST /orgs/backup`, seção "Cópia de segurança" no hub): o banco é **um arquivo só**, com os leads de todas as contas dentro, e até aqui **não havia cópia nenhuma** — nem no código nem no `DEPLOY.md`. Perder o volume do Railway era perder tudo de todo mundo de uma vez. É um risco de outra natureza do "está lento": lentidão avisa antes, disco perdido não avisa nada.

  **Vai para FORA do servidor, ou não vai.** Cópia gravada no disco da hospedagem fica no mesmo volume do banco que ela deveria proteger — some junto, e no meio do caminho todo mundo achou que estava protegido. Por isso `enviarAoR2` é a única função do `storage.js` que **não** cai para o disco quando o R2 recusa: aqui a queda seria pior que a falha. Sem R2 configurado o backup não acontece e o hub avisa em vermelho.

  **É conferida antes de subir**, e são duas perguntas, não uma: `integrity_check` (o arquivo abre inteiro?) **e uma contagem de imobiliárias** (tem dado dentro?). A segunda existe porque **arquivo vazio passa no `integrity_check`** — sem ela, um banco zerado subiria todo dia parecendo a cópia boa. Backup quebrado é pior que backup nenhum: com nenhum você se sabe desprotegido; com um quebrado você descobre no dia em que precisa dele.

  **`db.backup()`, e não copiar o arquivo.** O banco roda em WAL: o `.db` no disco NÃO contém as escritas recentes, que estão no `.db-wal` ao lado. Copiar só o `.db` entregaria uma base sem os últimos atendimentos, e ninguém perceberia até precisar dela.

  **Quem manda é o registro, não o relógio** (`config_plataforma.backup_estado`) — mesma regra do `orgs.ultimo_corte` e do `ultimo_aviso_plantao`: servidor fora do ar às 03:00 faz a cópia quando voltar, servidor que reinicia dez vezes continua fazendo uma. E **falha não marca o dia como feito**, senão a tentativa que deu errado tiraria o backup do dia inteiro.

  **O temporário é sempre apagado** (`finally`): ele tem o tamanho do banco, e um por dia enche o volume e derruba o CRM **por causa do** backup.

  **O erro chega em português** (`emPortugues`): o SDK da Amazon devolve "@aws-sdk XML parse error… inspect the hidden field {error}.$response", que é verdade e não serve para nada — quem lê a tela administra a plataforma, não escreveu o cliente HTTP. Os casos conhecidos do R2 são nomeados com o que fazer; o que não se sabe nomear passa cru, porque esconder some com a única pista. E **listagem que falhou não é "não há cópia"**: são coisas diferentes, e trocar uma pela outra manda o gestor consertar o problema errado.

  **Chave errada e falta de permissão também não são o mesmo problema** (28/08/2026, apareceu na primeira publicação): os dois chegam como **403**, e a primeira versão jogava os dois na frase "confira R2_ACCESS_KEY_ID e R2_SECRET_ACCESS_KEY". No R2 essa chave é a **mesma das fotos dos imóveis** — se as fotos sobem e só o backup reclama, a chave está certa e o que falta é permissão no token (`AccessDenied`, típico de token criado como *Object Read only* ou preso a outro bucket). Mandar trocar a chave nesse caso é mandar consertar o que não está quebrado, e arriscar derrubar o envio de fotos que estava funcionando. Agora `AccessDenied` tem frase própria, **dizendo qual operação foi negada** — sem LISTAR as cópias podem estar sendo feitas; sem GRAVAR não há cópia nenhuma —, e o 403 sem nome cita as duas causas em vez de escolher a errada.

  **E as duas chaves são conferidas pelo FORMATO, antes de a Cloudflare recusar** (28/08/2026): `conferirR2` já checava o Account ID, o bucket e a URL pública, mas não as credenciais — que é justamente onde o erro cai. As do S3 do R2 têm forma fixa (`R2_ACCESS_KEY_ID` 32 hex, `R2_SECRET_ACCESS_KEY` 64 hex), então dá para nomear os dois erros de colar em vez de esperar o "não reconheceu a chave", que é verdade e não diz onde procurar: **o token da API colado no lugar do Access Key ID** (a tela do Cloudflare mostra o token em destaque e o Access Key ID discreto ao lado) e **o segredo cortado ao copiar** (são 64 caracteres numa caixa que rola). A conferência diz o tamanho que chegou e o esperado, e **nunca imprime o valor** — `/integracoes` é público, e é quando o valor está errado que a tentação de mostrá-lo aparece.

  **A tela não joga fora o que já sabe**: quando a listagem falha, a data da última cópia vem do registro em `config_plataforma`, com a ressalva escrita ("o servidor registra ter feito, mas não consegue conferir no R2"). A listagem é melhor — diz o que está guardado de verdade, não o que achamos que mandamos —, mas na falta dela o registro é muito melhor que "não sei". E **"ainda não rodou" é âmbar, não vermelho**: no dia em que o recurso é publicado isso é o esperado, e pintar de pane manda o gestor procurar defeito onde não tem. Vermelho fica para R2 desligado e para tentativa que falhou. Teste: `npm run teste:backup`.
- **A cópia de segurança leva o BANCO, não as fotos** (28/08/2026, descoberto ao diagnosticar o R2): com `DB_PATH=/data/concrm.db`, o `storage.js` guarda os arquivos em `/data/uploads` quando o R2 não está funcionando — o **mesmo volume** do banco. Elas sobrevivem ao deploy (é disco persistente, e a dica do diagnóstico dizia o contrário, assustando com a coisa errada), mas ficam **dentro do ponto único de falha que a cópia existe para cobrir**. Perder o volume, hoje, é perder as fotos dos imóveis mesmo com o backup em dia. Enquanto o R2 estiver de pé isso não acontece — as fotos vão para lá e o volume só guarda o banco; mas o dia em que a chave do R2 para de valer, o CRM volta a acumular arquivo no volume **em silêncio**, porque cair para o disco é de propósito (foto que não sobe não pode travar o corretor). O sinal é `/integracoes/armazenamento/teste`.
- **A escala de plantão salvava — no ano errado** (29/08/2026, `services/plantao.js`; o Ali relatou como "não está salvando, e o mês anterior apagou"). Eram três defeitos, e os três respondiam "deu certo".

  **(1) `lerDia` tinha um `new Date(t)` de consolo** no fim, aceitando qualquer coisa que o JavaScript soubesse interpretar. `"01/09"` — dia/mês **sem ano**, que é o formato normal numa escala mensal, porque o mês está no título da planilha — virava **09/01/2001**. `"Sábado"` virava 01/01/2001; o serial cru do Excel virava o ano 46235. Nada disso dava erro: viravam **dias válidos em anos errados**, a importação respondia "30 dias lidos" e o mês aberto na tela continuava vazio. E como gravar **apaga dia+turno antes de inserir**, dois meses caindo no mesmo janeiro de 2001 faziam o segundo apagar o primeiro — que é o "mês anterior sumiu". Agora dd/mm sem ano usa **o mês aberto na tela** como referência (o relógio do servidor erraria toda virada de ano, e a escala do mês seguinte é montada no mês anterior), e o que não casa com formato nenhum é **recusado e contado**, nunca convertido.

  **(2) Nome de uma palavra só era ambíguo consigo mesmo**: o casamento gravava `chave(nome)` e logo em seguida perguntava se o primeiro nome já existia — existia, era ele próprio. Equipe cadastrada por primeiro nome ("Marina", "Vanessa"), que é como quase toda equipe é cadastrada, não casava com ninguém e **toda importação devolvia zero escalas**. Agora são duas passadas: nome inteiro primeiro e imune, primeiro nome depois; a ambiguidade só existe entre primeiros nomes de pessoas **diferentes**.

  **(3) `definirTurno` descartava calado** quem não fosse corretor ou atendente — inclusive o gestor — e respondia `ok` com `quantos: 0`. Do lado de quem usa, isso é exatamente "a escala não está salvando": some o que estava lá e nada aparece. Agora devolve `recusados` com o motivo.

  **Escolher o arquivo LÊ; só o botão Salvar grava** (31/08/2026, pedido do Ali; `previa: true` em `POST /plantoes/importar-arquivo`). É a mesma regra da imagem colada, do áudio gravado e da marca — e aqui pesa mais que em todos, porque a importação **apaga o dia+turno inteiro**: subir a planilha de outubro com setembro aberto apagava setembro em silêncio. A leitura devolve o mês em que vai cair, **os primeiros dias com os nomes de cada turno** (total sem conteúdo não dá para conferir, e conferir é a única razão de o botão existir) e **quantas escalas já existentes seriam substituídas** — o aviso que só existe antes, porque depois o que sumiu já sumiu. Quando o mês lido não é o da tela, o cartão fica coral e o botão escreve o mês que vai gravar de verdade.

- **A atendente confere quem veio ao plantão** (31/08/2026, pedido do Ali; tabela `plantao_presencas`, `PUT /plantoes/presenca`, botões Veio/Faltou na tela de Plantão). Ela é quem está na imobiliária durante o turno; o corretor não confere a própria presença — nota que a pessoa se dá não é conferência, é declaração.

  **Tabela SEPARADA de `plantoes`, com chave dia+turno+pessoa**, e não o id da linha da escala. Subir a planilha de novo apaga e recria as linhas de `plantoes` — é assim que refazer o mês funciona. Se a presença morasse lá, corrigir um nome na planilha e reenviar apagaria a conferência do mês inteiro, sem nada aparecer na tela.

  **Três estados, e o terceiro é o que sustenta o número**: veio, não veio e **ninguém conferiu** (ausência de linha). Contar quem ninguém conferiu como falta faria o relatório **inventar faltas** no mês em que a atendente esqueceu de marcar — é a mesma régua da visita confirmada e do lead sem temperatura. Clicar de novo no mesmo botão **desmarca**: errar o clique não pode ter como única saída registrar o contrário no relatório de alguém.

  **Só depois que o turno aconteceu**, e **só de quem está escalado** naquele dia e turno. "Veio" marcado no plantão de amanhã é um fato que ainda não existe, e no relatório ficaria indistinguível de uma presença de verdade; presença de quem não estava na escala faria "presenças" passar de "turnos escalados", e número impossível derruba a confiança na tela inteira.

  **No relatório individual do corretor**, o bloco conta **turnos**, não dias (dá para vir de manhã e faltar à tarde, e somar os dois esconderia a metade que faltou), e mostra sempre `veio / faltou / não conferido` com o "X de Y turnos conferidos" ao lado — sem isso, "1 falta" num mês em que se conferiram dois turnos parece um mês quase perfeito. Turno que **ainda não aconteceu** não entra como pendente: ele ainda não é nada, e sem esse corte o relatório do mês corrente mostraria uma pilha de pendências que é só o resto do mês. **A nota do score não mudou** — entrar no cálculo com o histórico vazio mexeria no ranking de todo mundo por um número que ainda não existe. Teste: `npm run teste:plantao`.

- **O relatório de plantões** (02/09/2026, pedido do Ali; `services/plantao.js` → `relatorio`, `GET /plantoes/relatorio`, aba "Relatório" na tela de Plantão): a conferência da atendente já entrava no relatório individual de cada corretor desde 31/08. O que faltava era a outra pergunta, que é da gestão — **como foi a escala do mês**: quem veio mais, quem faltou, e se a manhã é pior que a tarde.

  **O aproveitamento divide pelo que foi CONFERIDO**, e não pelos turnos passados. É a trava mais importante desta tela: dividir pelos passados transformaria o esquecimento da atendente na nota do corretor, e num mês de conferência fraca a equipe inteira apareceria reprovada por um trabalho que ela fez. Quando **nada** foi conferido o valor é **nulo, nunca 0%** — zero por cento lê-se como "não apareceu nenhuma vez", que é a acusação mais grave que a tela pode fazer, e estaria sendo feita justamente onde não se sabe de nada. O teste 31 monta uma pessoa com presença E turno não conferido de propósito: num caso em que as duas fórmulas dão o mesmo número, o teste não prova qual delas está sendo usada.

  **Manhã e tarde separados**, porque a pergunta é operacional: se a falta se concentra num turno, o problema é da escala (horário ruim, plantão vazio) e não das pessoas. Somados, a diferença some dentro da média e a conversa vira sobre gente.

  **A lista começa por quem MAIS faltou** — quem abre este relatório está procurando problema, e ordenar pelo melhor faria a gestão rolar a lista inteira para achar o que veio ver. E os três números andam sempre juntos (veio / faltou / não conferido), com uma tarja âmbar quando há turno não conferido: sem ela o gestor lê "100%" num mês em que quase nada foi conferido e vai para a reunião com um número que a equipe não reconhece.

  **A aba é só de supervisão**, e por isso não fica junto da escala, que é aberta à equipe inteira. A escala responde "quem está de plantão amanhã", que é operação; este responde "quem faltou no mês", que é o nome de cada um numa lista ordenada por falta — mesma régua do ranking do score. O corretor não fica sem: os números dele continuam no relatório individual dele. Teste: `npm run teste:plantao` (34 casos).

- **O corretor autônomo é CORRETOR, e manda na casa por ser o dono** (02/09/2026, pedido do Ali: *"ao criar a conta de corretor ele é identificado como Gestor e ele precisa ser identificado como corretor, em todos os processos"*; `auth.js` → `ehDonoAutonomo`, `roles`, `supervisiona`): ele nascia `adm`, e isso o deixava de fora de **tudo que o sistema procura por papel**. Não era rótulo errado, eram quatro furos: a **catraca** procura `sdr` e a casa nasce sem atendente, então o lead novo ficava na fila **sem dono, esperando um repasse de alguém que não existe**; o **rodízio** listava zero disponíveis; o **score** e o **relatório de produtividade** filtram `role='corretor'`, então ele pagava por um CRM cujo relatório principal **nunca teria o nome dele**.

  Agora o papel é `corretor` — o que a catraca, o rodízio e o score enxergam — e o poder de gestor vem de **ser o dono**, resolvido **num lugar só**: `roles()` libera `adm` para o `dono_user_id` de uma org `autonomo`, e `supervisiona` faz o mesmo. Espalhar isso pelas dezenas de `roles("adm")` deixaria a esquecida barrando o próprio dono da conta em silêncio — é a mesma razão de `semMaster` existir. **A conferência vai ao BANCO e não ao crachá**: o token dura 30 dias, e quem deixasse de ser dono continuaria entrando por um mês.

  **A catraca entrega o lead A ELE.** Numa imobiliária, lead sem atendente fica na fila sem dono — há várias contas possíveis e escolher uma seria chutar. Na casa de uma pessoa não há chute: só existe ele, e a casa **nasce** sem atendente, então esse era o caso normal e não a exceção.

  **E ele fica fora do corte diário de prontidão** (`services/expediente.js`). O corte existe para o corretor declarar todo dia que entra no rodízio; sem rodízio nem quem repasse, desligá-lo às 18h faria o dono remarcar prontidão toda manhã para receber o próprio lead — e esquecer não o tiraria de fila nenhuma, só o faria contar como indisponível nos números dele mesmo.

  **Na tela, `role` diz o que a pessoa É e `gestor` diz o que ela PODE.** O servidor manda `user.gestor` (nunca deduzido no navegador: quem decide permissão é quem a aplica), o `toSession` copia — mesma armadilha do `master` e do `barra_recolhida`, que sem estar listados chegavam e eram jogados fora em silêncio — e o `podeGerir(session)` substituiu as travas de acesso. O **menu segue o que ela pode**, somando os itens do corretor (Disponib., Produção) aos do gestor; a Catraca continua fora. O rótulo mostra **"Corretor(a)"**, que é o pedido.

  **E "Disponib." NÃO entra no menu dele** (02/09/2026, o Ali abriu a tela e disse que não fazia sentido — não fazia mesmo). A prontidão decide UMA coisa: quem entra na fila de repasse do rodízio. Na casa de uma pessoa não há rodízio, a catraca entrega o lead a ele esteja marcado ou não, e ele já ficou fora do corte das 18h — o botão não mudava nada. Pior: a tela AFIRMAVA três coisas que não existem ali (que "a SDR pode te transferir", que há uma catraca, e que a prontidão vence às 18:00). Interruptor que não liga nada é pior que interruptor nenhum, porque ensina uma regra falsa sobre o próprio sistema. Junto, o texto da tela foi corrigido para o corretor de imobiliária, onde ela continua valendo: dizia "a SDR confirma e transfere manualmente, um a um", de antes de a fila virar um rodízio numerado (25/08/2026) — e mandava o corretor cobrar a pessoa errada, porque a gestão também repassa.

  **O titular da conta virou não-removível** (`DELETE`/remover em `auth.routes.js`). A trava antiga contava GESTORES ativos, e numa casa dessas não existe nenhum `adm` — a contagem dava zero e a trava nem disparava. Removê-lo deixaria a conta sem dono, sem quem responda pela mensalidade e sem caminho de volta pela tela. Vale para os dois tipos de conta.

  **A migração roda no start** (`bootstrap.js`), é idempotente e mexe só no dono: o atendente que ele contratou continua atendente. Teste: `npm run teste:autonomo` (caso 3 e 3b — as asserções andam juntas de propósito, porque sozinha a primeira passaria numa versão que o transformasse num corretor sem acesso à própria conta).

- **A casa de um corretor só não tem turno: nem plantão, nem horário da IA** (02/09/2026, pedido do Ali: *"não faz sentido o corretor autônomo também ter uma opção de plantão, sendo que o plantão dele é na hora que ele quiser e puder… foco total em ativar a qualquer hora pra a IA não deixar o lead dele esfriar"*). São três recortes com a mesma raiz — recursos que existem porque numa imobiliária há **turno de outra pessoa** —, e um furo que apareceu no caminho.

  **PLANTÃO some do menu** (`NAV.filter`, junto com a catraca). A escala responde quem está na imobiliária em cada turno, e a conferência de presença existe porque é a ATENDENTE quem marca quem veio — nota que a pessoa se dá a si mesma não é conferência, é declaração. Numa casa de um corretor só ele seria o escalado, o conferente e o conferido ao mesmo tempo, e os três números do relatório (veio / faltou / não conferido) deixariam de significar coisa alguma. Some junto o lembrete no alto do sistema e a consulta de hora em hora que o alimentava.

  **A IA ATENDE A QUALQUER HORA por padrão** (`orgs.robo_sempre`, `configDoRobo`, `dentroDaJanela`). Na imobiliária o robô se cala às 09:00 justamente para **preservar o tempo de resposta DA ATENDENTE**, que é o número do relatório; um corretor sozinho não tem turno de ninguém para cobrir nem tempo de outra pessoa para preservar — o lead que chega às 22h esfria até ele ver o celular. **`robo_sempre` NULO é um estado válido** e é o de quase todas as linhas: significa "ninguém escolheu", e a resposta sai do TIPO da conta. É o que faz a mudança valer para as contas que já existem sem migração de dados nenhuma e sem mexer no que uma imobiliária configurou. Escolher grava 0/1 e **vale mais que o tipo**, nos dois sentidos.

  **Os controles ficam, como OPÇÃO** (foi o pedido). O seletor "A qualquer hora × Só fora do expediente" é uma escolha explícita, e não sete botões de dia significando uma delas por tabela: desmarcar os sete dias já produzia o mesmo efeito, mas aí o estado ficaria escondido num detalhe e **um clique errado em qualquer dia emudeceria a IA das 09:00 às 18:00** sem ninguém entender por quê. Com "a qualquer hora" ligado, os campos de horário e de dias **somem** em vez de ficarem cinzas — campo que não decide nada faz o próximo a abrir a tela ajustar o horário e ir embora achando que mudou alguma coisa.

  **O FURO: a IA ficaria ligada e MUDA em 100% das conversas dele.** A mudança de papel da manhã de 02/09 tornou o autônomo `corretor` (que é o que a catraca, o rodízio e o score precisam enxergar), e a catraca entrega TODO lead dele a ele — então `podeAtender` lia "já tem corretor" e se calava sempre, sem erro em lugar nenhum. É a mesma inversão da linha pessoal, pelo mesmo motivo: no número da CASA de uma imobiliária o robô cobre o vazio entre o lead chegar e alguém assumir, e na casa de uma pessoa esse vazio não existe. O que sustenta a inversão é o mesmo consentimento: **ele continua nascendo DESLIGADO** e quem liga é o dono, no próprio painel.

  **E a tela parou de afirmar o contrário de si mesma**: a aba se chama "Atendimento pela IA" (não "Fora do expediente", que anunciaria um horário que a conta não tem), "Atender lead de corretor" sai da lista do que ele nunca faz (ali seria falso e falso justamente na direção que apaga o recurso), e o cartão da ficha deixa de dizer "dentro do horário" e "nem durante o expediente". **Ensinar a IA continua igual** — é o "ele treinar ela como ele quiser". Testes: `npm run teste:robo` (casos 30 a 33 — o 31 confere no MESMO instante que a imobiliária continua calada, senão a inversão não estaria sendo provada).

- **O gasto de IA é do ConHub, e some da tela do cliente** (02/09/2026, pedido do Ali; `GET /config/ia` passou de `roles("adm","sdr")` para `soMaster`, e a aba "Uso da IA" só aparece para o master): a chave da IA é **uma só, do ConHub** (`ANTHROPIC_API_KEY` no servidor) — não existe chave por imobiliária, então o dólar daquela tela nunca foi despesa do cliente, é a nossa. Mostrá-la a ele fazia duas coisas ruins ao mesmo tempo: dava um número de custo que não é dele (e que ele leria como conta a pagar, ou como argumento de desconto) e abria a estrutura de custo da plataforma para quem assina a plataforma. Vale para imobiliária e autônomo. **O master continua vendo de DENTRO da conta do cliente** — é ele quem paga a Anthropic e quem precisa saber se o recurso pegou na equipe ou está parado; o par (cliente 403 / master 200 na mesma conta) é o caso 21b de `npm run teste:autonomo`. Para o cliente a régua continua sendo a de sempre: **o custo estimado aparece ANTES do botão**, nas rodadas em lote, que é onde ele decide gastar.

- **O painel de mensalidade é do MASTER, e só dele** (conferido em 02/09/2026): valor, vencimento, carência, "Salvar" e "Registrar pagamento" já estavam atrás de `master` na tela desde 27/08/2026, e o servidor recusa por `soCobranca`. O cliente — autônomo ou imobiliária — vê o cartão da mensalidade (é o extrato dele) e o "Gerenciar assinatura" para escolher plano, **sem nenhum campo que decida dinheiro**. Quando esses campos aparecem, quem está olhando é o master: ou na própria conta, ou dentro de um cliente pelo hub, e a barra lateral diz "ConHub · master".

- **O site dizia ser "Lovable App"** (02/09/2026, achado ao trocar o favicon; `src/routes/__root.tsx`): o cabeçalho do molde nunca tinha sido trocado — título "Lovable App", descrição "Lovable Generated Project", `author: Lovable` e `twitter:site: @Lovable`. Título e descrição a home sobrescreve, então **na tela ninguém via**; mas `author` e `twitter:site` não eram sobrescritos em lugar nenhum e `og:title` só na home, então **qualquer outra página compartilhada no WhatsApp aparecia com o nome de outra empresa na prévia**. É o tipo de erro que só se descobre depois de o link circular. Junto, o **favicon virou o mesmo do CRM** — o cronômetro com o ponto coral, SVG embutido no endereço em vez de arquivo: nítido em qualquer tamanho e sem depender de um `.ico` carregar (se ele falha, a aba fica com o ícone genérico e ninguém descobre). O `.ico` continua listado depois, como reserva.

- **A busca do corretor não filtrava nada — uma linha** (02/09/2026, relatado pelo Ali: *"eles estão pesquisando o nome de um lead e não aparece ninguém; a conta da atendente está funcionando"*). A peneira da lista dele terminava em `soNumeros(l.tel).includes(soNumeros(t))`. Procurando por "Carlos", `soNumeros("Carlos")` vira **texto vazio** — e `qualquerCoisa.includes("")` é **sempre verdadeiro** em JavaScript. Toda busca por nome casava com todo lead, e a lista ficava exatamente como estava. A busca por telefone funcionava; a busca por nome, que é a que se usa, nunca funcionou desde que nasceu em 20/08/2026.

  **O modo de falhar é o que fez o relato ser outro:** o campo aceitava o texto, o contador continuava dizendo o total de sempre e nada mudava na tela. Não parecia defeito, parecia que a busca não existia — daí "não aparece a opção de pesquisar". Na conta da atendente funcionava porque **a busca dela vai ao servidor** e nunca passou por essa linha.

  O **Kanban já fazia certo** desde o começo (`digitos.length>=4 && tel.includes(digitos)`), e foi por isso que o defeito nunca apareceu naquela tela. A correção adota a mesma regra e acrescenta `semAcento` dos dois lados — procurar "joao" tem que achar "João", senão a busca falha justamente nos nomes mais comuns da base. **Não há teste automático cobrindo isto**: a peneira mora no `app.jsx`, que não tem suíte; a conferência foi feita no navegador, com conta de corretor e leads de verdade.

- **O corretor atende pelo WhatsApp DELE, de dentro do CRM** (01/09/2026, pedido do Ali; tabela `canais`, `services/canais.js`, `GET/POST /canais`, aba "Meu WhatsApp" em Minha conta, subcategorias em Atender): o sistema inteiro assumia **um número por imobiliária**, e essa suposição estava espalhada — no envio, no webhook, na assinatura das mensagens, na catraca, no robô e no download de mídia. Agora existem **linhas**: a da casa (`tipo='imobiliaria'`) e as pessoais (`tipo='corretor'`, uma por pessoa).

  **O que NÃO muda é o pedido inteiro numa frase: ele continua falando pelo CRM.** A linha muda; o lugar onde a conversa acontece, não. E o ganho real é que a regra deixa de depender de disciplina: **ninguém consegue impedir um corretor de abrir o WhatsApp e digitar** — mas com a linha dele ligada, o que ele digitar cai no CRM sozinho, pelo mesmo caminho `fromMe` que já existia.

  **A migração é invisível, e se cura na hora.** Cada imobiliária ganha a linha da casa com a conexão que já usava, no start **e também quando uma mensagem chega** (`garantirCasa`). O segundo caminho não é zelo repetido: uma conta criada pelo hub entre dois reinícios ficaria sem canal e **pararia de receber lead**, com o servidor de pé e nenhum erro em lugar nenhum. `orgs.uazapi_*` continua sendo escrito **em par** com a linha da casa (mesma decisão de `leads.stage` ao lado de `stage_id`), e o par só se escreve em `services/canais.js`.

  **A assinatura `*Marina:*` some na linha pessoal.** Ela nasceu do número único; no WhatsApp da própria Marina é a pessoa se anunciando na própria casa. A decisão fica em `sendText`/`sendMedia`, e não em cada chamador: são cinco pontos de envio e o sexto que alguém escrever depois, e o esquecido **não daria erro** — só mandaria uma mensagem esquisita, que só o cliente lê.

  **Lead que chega na linha pessoal nasce do dono dela, sem catraca.** A catraca reparte o que chega no número da CASA, que é de todo mundo e de ninguém; quem escreveu para a Marina escolheu a Marina, e sortear esse lead seria o CRM desfazendo uma decisão do cliente.

  **Responde-se pela linha que o cliente usou por último**, e é a única regra sem buraco: ele escreve para o número que tem salvo, e responder por outro faz a resposta chegar como mensagem de um desconhecido, fora da conversa que ele estava tendo. Vale nos dois sentidos, e **o lead continua UM SÓ** — dois leads partiriam histórico, relatório e repasse, que é o que este CRM existe para não deixar acontecer. `leads.canal_id` aponta para o presente; `messages.canal_id` guarda o passado. **Nulo é a linha da casa nas duas colunas** — uma convenção só, senão a mesma linha seria dois valores conforme quem escreveu.

  **O robô se cala na linha pessoal a menos que o dono ligue.** Ali a trava "já tem corretor" se inverte (todo lead dali é dele), então o que sustenta a inversão é o consentimento: `canais.robo_ligado` nasce desligado e quem liga é o dono do número — decidir que existe uma IA falando pelo seu WhatsApp pessoal não é decisão de gestão.

  **O chute do webhook saiu.** `orgDoWhatsapp` tinha um consolo — "se só existe uma imobiliária conectada, é dela". Com várias linhas ele passaria a acertar por acaso e a errar em silêncio, pondo a conversa de um cliente na caixa de outra pessoa.

  **O atalho na conversa** ("Continuar no meu WhatsApp", `POST /leads/:id/canal`) **não manda mensagem nenhuma sozinha**: troca a linha e **sugere o texto no campo**. Um "oi, salva esse número" escrito pelo CRM, vindo de um número que o cliente não conhece, com palavras que o corretor não escolheu, seria a pior primeira impressão possível. Trocar **leva a aba junto**, senão a conversa some da lista aberta enquanto continua na tela ao lado. Só o dono do lead troca.

  **Desligar uma linha devolve as conversas dela para o número da casa.** Lead apontando para linha sem credencial é lead ao qual ninguém consegue responder — o envio falharia e a tela diria "falha ao enviar" sem dizer por quê. E desligar **não apaga**: apagada, o CRM não saberia mais por onde aquele atendimento aconteceu.

  **O TETO É DO PLANO, o custo é à parte** (decisão do Ali): `orgs.limite_canais` (11 = a da casa mais dez corretores, que é o plano vendido hoje), `canais_incluidos` (1 — a da casa já vinha com a mensalidade, e cobrá-la agora seria aumentar o preço de quem já é cliente) e `valor_canal`. Os três exigem **master** em `PATCH /assinatura`: aquela rota é `soDono`, e num cliente o dono é o próprio cliente — sem a trava ele gravaria limite 99 e valor 0, que é o furo do preço de 27/08/2026 num campo novo. O gestor **libera nominalmente** (`users.canal_liberado`) quem pode ligar a própria linha; "os cinco primeiros que pedirem" faria a autorização depender de quem clicou primeiro. Preço nulo aparece como "a combinar", não R$ 0,00 — mostrar zero prometeria de graça o que vai ser cobrado.

  **Dois defeitos antigos apareceram no caminho**: `services/midia.js` ainda usava `UAZAPI_HOST`/`UAZAPI_TOKEN` do ambiente (a migração multi-imobiliária esqueceu dele — numa segunda casa toda foto do cliente falhava calada, e a conversa guardava só "Foto"), e o pino de localização ia com **"Conecta Imóveis" escrito no código**, para o cliente de qualquer imobiliária.

  **Risco que não é técnico, e precisa estar na tela:** hoje um ban da Uazapi derruba o WhatsApp comercial; com linhas pessoais, derruba o **WhatsApp pessoal do corretor**. É outra consequência, e ela fica escrita antes de ele ler o QR Code. Testes: `npm run teste:canais` (23 casos) e `npm run teste:canais-entrada` (8, com o servidor de pé, porque as regras de entrada moram na rota).

- **Cadastrar um lead na mão** (01/09/2026, pedido do Ali; `POST /leads`, botão ao lado da busca nas duas telas de Atender): o lead só nascia de três jeitos — o cliente escrevia no WhatsApp, o formulário do Meta disparava, ou o gestor subia uma planilha. Faltava o mais comum do dia a dia: **alguém ligou, alguém indicou, alguém apareceu na porta**. Sem lugar no sistema, esse lead ficava num papel ou no bloco de notas do celular, que é onde o atendimento deixa de ser medido.

  **Todo mundo cadastra** — restringir à gestão empurraria de volta para o papel justamente quem atende. São **três campos para todos** (nome, número, etapa) e **mais dois para quem supervisiona** (corretor responsável e observações gerais). O corretor não vê o campo de responsável: a resposta é uma só — ele — e campo com uma opção é campo que só serve para errar.

  **O número passa pela mesma normalização do WhatsApp.** É o que faz o cadastro e a conversa serem a mesma pessoa: digitado "(87) 9 9111-2222" e gravado `5587991112222`, que é o formato do webhook. Guardado como veio, o cliente que respondesse criaria um **segundo lead** ao lado do primeiro, cada um com metade da história.

  **Número repetido não vira lead novo, e a recusa devolve QUAL lead já existe** — a tela oferece "abrir a conversa desse lead". Só dizer "não" faria o caminho fácil ser cadastrar com o número trocado, e aí a duplicata acontece do mesmo jeito, sem ninguém perceber. E `normalizePhone` é permissiva de propósito (no webhook, devolver os dígitos que vieram é melhor que perder um lead), então **a conferência do tamanho fica na rota**: um lead com telefone "123" entraria no funil e no relatório contando como atendimento.

  **Não escolher responsável deixa o lead com QUEM CADASTROU, não na fila.** A atendente que acabou de atender a ligação é a dona natural daquele atendimento, e esta casa tem regra própria: lead novo nasce com dono, senão fica parado esperando alguém reparar nele. Para deixá-lo sem dono é preciso dizer — escolhendo "ninguém ainda". O corretor cadastra **sempre para si**: mandar `assigned_to` de outra pessoa no corpo da requisição não pode virar um jeito de empurrar lead para o colega por fora da catraca.

  **A observação vira observação, não mensagem.** Escrita na conversa apareceria como se tivesse sido enviada ao cliente; na faixa âmbar acima da conversa é lida por quem for atender **antes de falar** — que é o caso que motivou o pedido (a atendente descobre algo na ligação e passa adiante). E o repasse **avisa se o corretor vai mesmo ser chamado**, com a mesma tarja âmbar da catraca.

  Junto, um defeito antigo: **a importação em massa não ligava os leads ao funil** (gravava só o nome da etapa, com `pipeline_id`/`stage_id` nulos — certo antes de o funil ser configurável, defeito depois). Trezentos leads importados ficavam **fora de todas as colunas do Kanban**, com o contador ao lado dizendo que existiam; só o reinício seguinte do servidor os ligava. Teste: `npm run teste:lead-manual` (18 casos, com o servidor de pé — as regras que importam são de permissão e de recusa, e permissão testada por dentro do serviço não prova nada sobre a rota).

- **O funil de entrada é o de QUEM RECEBE o lead** (01/09/2026, relatado pelo Ali: *"os leads da Vanessa precisam cair no funil de SDR; os antigos eu transferi, mas os novos ainda não"*; `users.pipeline_entrada`, `entradaDe` em `services/pipelines.js`, `GET/POST /pipelines/entrada`, seção em Configurações → Funis e etapas): o lead sempre nascia no funil **padrão da imobiliária**, fosse de quem fosse. `entradaPadrao` respondia "qual é o funil desta casa", e a pergunta que faltava era outra — os leads que caem na atendente pertencem ao pré-atendimento, os do corretor ao comercial. Sem isso, o funil novo só existia para quem fosse movido para lá **na mão**, e a cada lead que entrava o trabalho recomeçava.

  **São duas metades, e a segunda é a que quase ficou de fora**: (1) o lead **nasce** no funil de quem o recebe — vale nos três caminhos de entrada, WhatsApp, Meta e cadastro manual; e (2) ele **troca de funil junto com o dono** no repasse. Sem a segunda, o lead entrava no pré-atendimento com a atendente e FICAVA lá depois de repassado — o corretor abria o kanban dele, no comercial, e o lead que acabou de receber não estava em coluna nenhuma.

  **Vazio é o funil padrão, e vazio NÃO MOVE NADA.** É a trava que impede a mudança de vazar para quem não pediu: `pipeline_entrada` em branco significa "uso o padrão da casa", e mover por causa disso puxaria de volta ao padrão um lead que alguém pôs de propósito num funil especial. Quem nunca abrir esta configuração não vê diferença nenhuma.

  **A troca de dono passou a ter UM lugar só.** Eram **seis rotas** em `distribution.routes.js` fazendo o mesmo `UPDATE leads SET assigned_to` cada uma por sua conta — a armadilha que este projeto já documentou duas vezes: regra escrita na rota vale para uma e não para as outras cinco, e a esquecida não dá erro nenhum. Agora todas chamam `trocarResponsavel` (`services/movimento.js`), que grava o dono, o `assigned_at` que faz o lead subir na caixa de quem recebeu, a linha do histórico de transferências e a troca de funil. **A fila não tem funil**: devolver o lead para ela não mexe no lugar em que ele está.

  Detalhe de rota que teria falhado calado: `GET /pipelines/entrada` precisou ser registrado **antes** do `r.get("/:id")`, senão o Express procuraria um funil chamado "entrada" e devolveria 404 sem nada no log. Teste: `npm run teste:funil-entrada` (12 casos).

- **A porta de entrada de quem vem do site** (02/09/2026, `routes/publico.routes.js`, `POST /publico/comecar` e `GET /publico/planos`): a conta de corretor autônomo só nascia de um jeito — o Ali criando uma a uma no hub. Isso servia enquanto a venda era conversada, e deixa de servir no minuto em que existe um site com um botão "Testar 14 dias grátis": cada clique viraria uma mensagem para ele responder no dia seguinte.

  **É a ÚNICA rota de escrita da plataforma que a internet chama sem login e sem código.** O `/auth/register` é aberto mas exige o `ADM_CODE`; o `/orgs/autonomos` exige master. Por isso ela mora num arquivo só dela, onde as travas são a primeira coisa que se lê.

  **O freio é por IP** — cinco contas por hora, em memória. Sem ele, um laço de dez linhas cria dez mil imobiliárias numa madrugada e a limpeza é manual. Em memória de propósito: some no reinício, e o objetivo é impedir a enxurrada, não manter cadastro de infrator. O teto é generoso para um escritório inteiro testando passar.

  **E-mail com conta ATIVA não cria outra** — devolve "faça login" e o endereço de entrada. A segunda conta deixaria a pessoa com duas, cada uma com metade dos leads, descoberto semanas depois. Mas quem **começou e não terminou** (pendente) é reaproveitado: sem isso, fechar a aba antes de criar a senha prenderia o e-mail para sempre.

  **O teste começa ao DEFINIR A SENHA, não no cadastro** — regra de 27/08/2026, e aqui pesa mais: quem preenche às 23h e abre o e-mail na segunda não pode chegar com três dias a menos.

  **O link volta na resposta, não só no e-mail.** Cadastro que depende de um e-mail chegar falha em silêncio para uma parte das pessoas — e essa parte não reclama, desiste.

  **`GET /publico/planos` serve a vitrine a partir do servidor.** Preço copiado no site seria uma segunda verdade sobre dinheiro, divergindo no primeiro reajuste e sendo descoberta pelo cliente.

  **E ela tem PÁGINA PRÓPRIA** (`public/comecar.html`, servida em `/comecar`, `/teste-gratis` e `/sou-corretor`), no mesmo padrão do `criar-imobiliaria.html`. Não é capricho: assim o botão do site de vendas é um **link simples** — sem código, sem chave, sem CORS — e o texto do cadastro muda aqui, sem tocar no site e sem depender de crédito de ferramenta nenhuma. A tela de sucesso leva um **botão** para criar a senha, e não a instrução de esperar um e-mail: é ali que metade das pessoas desistiria, e quem desiste não reclama. E-mail que já tem conta não vira erro vermelho — vira o link de entrar.

  **Montada com caminho explícito e ANTES do porteiro da assinatura** em `server.js`: quem chega não tem conta nenhuma — é o que vem criar. Depois de qualquer middleware de cobrança, o botão do site responderia 402 e falharia calado. O teste 13 confere que `/leads`, `/reports`, `/config`, `/canais` e `/orgs` continuam fechados.

  **A descrição da cobrança no Asaas mudou junto**: era `ConHub Mensal — Nome`, e virou `ConHub — Plano Anual (12 meses · R$ 197,00/mês)`. É o que o cliente lê no checkout e, meses depois, na fatura do cartão, quando não lembrar o que contratou — o texto mais barato de escrever e o que mais evita contestação. Teste: `npm run teste:porta-do-site` (13 casos, com o servidor de pé).

- **A API oficial da Meta (WhatsApp Cloud API), como segunda opção de conexão** (03/09/2026, pedido do Ali; `services/whatsapp_oficial.js`, `routes/whatsapp-oficial.webhook.js`, `services/mensageria.js`, coluna `canais.provider`): até aqui só existia a Uazapi — API não oficial, com risco de bloqueio, documentado desde o início. A oficial resolve o risco e troca por outras limitações, que também ficam escritas na tela: sem QR Code, mas exige verificar a empresa no Gerenciador de Negócios da Meta; sem risco de bloqueio, mas sem editar mensagem enviada e sem texto livre fora da janela de 24h desde a última mensagem do cliente (só modelo aprovado).

  **O modelo de credencial é o OPOSTO do da Uazapi, e foi isso que decidiu o desenho inteiro.** Na Uazapi cada linha é dona da própria credencial (host+token, uma instância por número). Na Meta, o TOKEN, o APP SECRET, o WABA ID e o VERIFY TOKEN são do APLICATIVO que o gestor criou — os MESMOS em toda linha da imobiliária, a da casa e a de cada corretor. O que muda de linha para linha é só o `phone_number_id`. Por isso uma linha pessoal na API oficial nunca vê o token do app: ela recebe só o `phone_number_id`, e o resto é herdado da linha da casa dentro de `salvarConexaoOficial` (`services/canais.js`) — do mesmo jeito que o corretor nunca via o token da Uazapi da casa.

  **`services/uazapi.js` virou o despachante único**, e continua sendo o único ponto de entrada para envio que o resto do sistema conhece. Toda função resolve o CANAL primeiro e, se ele for `provider === 'meta'`, desvia para `services/whatsapp_oficial.js` — sem tocar em `credenciais()`, que continua sendo só do mundo Uazapi, do jeito que já estava testado. Nenhum dos 8 pontos de chamada existentes precisou mudar.

  **O corpo de uma mensagem recebida virou uma função só**, `processarMensagemRecebida` (`services/mensageria.js`). Criar lead pela catraca certa, guardar mídia, trocar de linha, religar o funil, avisar o corretor, chamar o robô — nenhuma dessas regras muda por provedor, e até aqui elas viviam inteiras dentro de `routes/uazapi.webhook.js`. Copiar o corpo para o webhook novo teria criado a mesma armadilha que este projeto já documentou (seis rotas fazendo o mesmo UPDATE antes de existir `trocarResponsavel`, 01/09/2026): regra escrita duas vezes é regra que diverge. Cada webhook cuida só do que É diferente — o formato do payload, e como a identidade de quem mandou é conferida.

  **A conferência do webhook é ao contrário da de Lead Ads, por um motivo real: cada imobiliária tem o PRÓPRIO app_secret.** Não existe um segredo do servidor para testar a assinatura contra — primeiro é preciso descobrir DE QUEM é a mensagem, pelo `phone_number_id` que a Meta manda em toda mensagem, para só então saber qual app_secret conferir. É "identifica, depois autentica", ao contrário do webhook de Lead Ads, que autentica com um segredo só do servidor.

  **`fromMe` nunca é `true` na API oficial** — ela só entrega pelo webhook mensagens que o CLIENTE mandou; uma mensagem enviada pela própria API não volta como evento. É uma consequência que muda um comportamento real: **o número não pode mais ser usado no WhatsApp comum ou no Business App em paralelo**, diferente da Uazapi, onde o corretor sempre pôde digitar direto no celular como plano B. Isso precisa estar escrito na tela ANTES de o gestor migrar, não descoberto depois de já ter migrado.

  **`garantirCasa()` quase apagou o token da Meta, e é o tipo de bug que este projeto já documentou repetidas vezes: sem erro nenhum aparecer.** A função sincronizava `canais.host/token` a partir de `orgs.uazapi_host/uazapi_token` sempre que os dois divergiam — regra certa enquanto só existia Uazapi. Numa linha oficial, `orgs.uazapi_token` fica para sempre vazio (a Meta não escreve ali), então TODA leitura da casa (a tela de conexão, `limites()`, o diagnóstico) apagava o token de verdade um instante depois de conectar. Corrigido restringindo a sincronia a `provider !== 'meta'`. Achado testando manualmente antes do commit, com uma conta real conectada — nenhum teste de unidade cobria, porque não existia canal oficial nos fixtures até este mesmo dia.

  **O verify_token nasce ANTES de qualquer outro dado da Meta existir, e nunca troca sozinho** (`verificadorDaCasa`, `services/canais.js`): a ordem de quem configura é colar a URL e um verify_token na TELA DA META antes (ou junto) de preencher o resto — se o token só existisse depois de salvar as quatro credenciais, o gestor não teria o que colar até terminar, e a verificação do lado da Meta aconteceria às cegas. E trocar as credenciais depois não troca o verify_token: a Meta só confere esse token na hora da VERIFICAÇÃO (o GET com `hub.challenge`), não a cada mensagem — regenerá-lo a cada salvamento derrubaria uma verificação que a Meta já tinha aceitado, sem o gestor ter mexido em nada do lado de lá.

  **A tela de Conexão mostra os dois provedores lado a lado, cada um com o próprio tutorial e o próprio formulário** — não um botão só que precisaria adivinhar qual provedor abrir. `PROVEDORES` (`services/uazapi.js`) ganhou o item `meta`, com `aviso` no lugar de `risco` (a Meta não tem risco de bloqueio, tem limitação de recurso). Na tela do corretor (`MeuWhatsapp`), o formulário muda sozinho conforme `canais.casa_provider`: ele nunca escolhe entre Uazapi e Meta, porque a linha pessoal sempre segue o provedor que a casa já usa.

  **Não criptografado, mesma decisão já tomada para os tokens da Uazapi na auditoria de 02/09/2026**: o token e o app_secret da Meta ficam em claro na tabela `canais`, pela mesma razão — a busca do webhook precisa de um índice determinístico, e a exposição principal (a cópia de segurança) já saiu criptografada. Fica para a mesma mudança isolada que os tokens da Uazapi aguardam.

  Teste: `npm run teste:whatsapp-oficial` (17 casos, com o servidor de pé — inclusive o isolamento entre imobiliárias: a assinatura de uma não pode validar mensagem para o Phone Number ID de outra, mesmo com o payload certo).

- **A conta de demonstração** (03/09/2026, pedido do Ali para reunião comercial e print; `services/demo.js`, `orgs.demo`, `POST /orgs/demo/atualizar`): uma imobiliária de mentira chamada **"Imobiliária"**, visível na lista do hub do master, com login público próprio (não passa pelo hub — é a experiência exata de um cliente de verdade). Por dentro é uma org como qualquer outra, de propósito: o que aparece na reunião precisa ser o que o cliente vai usar, não uma versão maquiada.

  **Login fixo, sempre o mesmo**: `gestor@imobiliariademo.com.br` / `Demo@2026` (senha igual para a atendente fictícia e os três corretores fictícios, trocando só o e-mail — `atendente@`, `marina@`, `rafael@`, `bruno@`). Isso é INTOCÁVEL a cada renovação: só os LEADS são apagados e recriados, a org/equipe/pipeline ficam de pé, senão o e-mail que o Ali decorou pararia de funcionar de um dia para o outro.

  **Os 22 leads cobrem as 11 etapas do funil**, com temperatura variada, tarefa vencida (mostra o coral), tarefa futura, observação, e duas vendas com valor — é o que faz o Kanban, o "Atender" e o relatório parecerem uma operação de verdade num print, em vez de três exemplos soltos numa tela vazia. Um deles (Ricardo Silva) é a **conversa inteira atendida pela IA fora do expediente**, do acolhimento à despedida — o cartão mais vendável da demonstração, e por isso ganha destaque. As mensagens do robô são **escritas à mão aqui**, não geradas pela Anthropic de verdade: chamar a IA de produção para popular uma conta de demonstração custaria dinheiro toda vez que a base for renovada, e o texto de exemplo já mostra a régua (acolhe, uma pergunta por vez, nunca fala valor de parcela).

  **Todo horário é relativo a `Date.now()` na hora de gerar, nunca uma data fixa.** É a única forma de a conta nunca "envelhecer" — sem isso, o lead "aguardando há 20 minutos" que fica bem numa reunião vira, três semanas depois, um lead esquecido há 21 dias, o oposto do que se quer mostrar. Por isso a base inteira de leads é **apagada e recriada** a cada renovação, em vez de deslocar timestamps de tabelas que se referenciam (mensagens, tarefas, `lead_etapas`, o gatilho de `last_interaction_at`) — deslocar seria replicar a mesma lógica de criação em outro lugar, com chance real de um campo ficar para trás e a conta parecer "meio nova, meio velha".

  **Renova sozinha de madrugada** (`reseedDemoSePassouDaHora`, mesmo padrão do backup diário — registro em `config_plataforma` manda, não o relógio; ver `services/backup.js`). A hora de madrugada (`DEMO_RESEED_HORA`, padrão 4h) não é só higiene: é a trava que impede os leads de sumirem da tela **no meio de uma reunião comercial ao vivo** às 15h. **Antes de uma reunião, o Ali não depende do relógio**: `POST /orgs/demo/atualizar` (só master) faz a mesma renovação na hora.

- **A ficha do corretor era outra ficha** (03/09/2026, relatado pelo Ali: o corretor não tinha o campo de observação no "Novo lead", não conseguia editar renda/situação e o botão de simulação sumia). Eram dois defeitos, e o segundo é mais grave que o relato deixava parecer.

  **(1) `NovoLead`**: "Observações gerais" estava dentro do mesmo `{supervisor&&...}` que "Corretor responsável" — o corretor via só três campos, sem o lugar de anotar o que descobriu na ligação, que é justamente o caso que motivou o recurso em 01/09/2026. Corretor responsável continua só da supervisão (ele só cadastraria para si mesmo); observação passou a valer para todos.

  **(2) A tela de atendimento tinha DUAS fichas diferentes.** `FichaLead` (usada por quem supervisiona, em `Conversas`) sempre teve os campos de qualificação **editáveis** (`CampoQual`, clique para editar) e o botão **Registrar simulação**. A ficha embutida em `Atendimento` — a tela que TODO corretor usa no dia a dia — mostrava renda/entrada/situação/cpf/prazo como **texto fixo**, sem `onClick` nenhum, e **não tinha o botão de simulação em lugar nenhum**. Não era intermitente por lead: era assim para TODO corretor, em TODO lead, desde que os dois recursos existem — o "às vezes aparece" do relato era a diferença entre a tela dele (nunca) e a tela de quem testou como supervisor ou SDR (sempre). `podeVerLead`/`podeVer` já barravam edição em lead de outra pessoa com 403 e mensagem própria — a causa nunca foi permissão, era o componente errado. Corrigido para usar os mesmos `CampoQual` e o mesmo fluxo de `Simulacao` da ficha da supervisão.

- **Redução de redundância** (08/09/2026, pedido do Ali: *"vamos colocar em prática aí pra gente reduzir o que está redundante"*, antes de qualquer recurso novo): quatro achados, do mais cosmético ao mais sério.

  A mesma checagem de permissão (`podeGerir(session)||session.role==="sdr"`) estava escrita à mão **cinco vezes** em `app.jsx` — virou `podeSupervisionar(session)`, um lugar só. A lista de campos de qualificação (renda/entrada/situação/cpf/prazo) estava duplicada **quatro vezes** — a própria correção da ficha do corretor, dois itens acima, tinha acabado de copiar o mesmo array em vez de compartilhar; virou `CAMPOS_QUAL` no frontend, e a rota `PATCH /leads/:id/qualificacao` passou a reusar `CAMPOS_SIMULACAO` (`services/ia.js`) em vez de repetir a lista. O rótulo do papel (Gestor(a)/Atendente/Corretor(a)) também estava reescrito à mão na barra lateral — passou a usar `roleParaTexto`, que já existia.

  **O achado sério: `POST /auth/users/:id/remover` fazia um `UPDATE leads SET assigned_to` em massa** — a MESMA armadilha que este arquivo já documentou seis vezes em `distribution.routes.js` antes de existir `trocarResponsavel` (01/09/2026), só que numa sétima rota que nunca foi migrada. Sem passar por `trocarResponsavel`, o corretor que herdava os leads de um colega que saiu da equipe não ganhava `assigned_at` (o selo "novo com você" não aparecia), o lead não seguia o funil de quem recebeu, e a transferência não ficava no histórico (`lead_transfers`) — como se o repasse nunca tivesse acontecido. Corrigido: percorre os leads em aberto um a um chamando `trocarResponsavel`, como todo outro repasse do sistema.

- **O roadmap comercial contra o que já existe — e uma correção** (08/09/2026, pedido do Ali para comparar o roadmap discutido com o possível sócio contra o sistema atual): a auditoria inicial errou em pontos importantes por ter sido montada em cima deste arquivo em vez do código — e este arquivo estava incompleto. O commit de 28/08/2026 ("Core de gestão") entregou MUITO mais do que a seção correspondente aqui documentava: **construtor de campos personalizados** (`custom_fields`, 9 tipos, `CamposConfig` em Configurações → Funis e etapas, com toggle de onde cada campo aparece), **campos obrigatórios por etapa já com UI** (`EtapaLinha`, bloqueia o `moverLead` com 422 e a frase do que falta) e **quatro templates de operação prontos** (`services/templates.js` — SDR, Comercial, Recaptação, Locação — já selecionáveis ao criar um funil novo, tanto no Kanban quanto em Configurações). Nada disso precisou ser construído; precisou ser **descoberto**.

  **O que realmente faltava, e por isso quebrava na prática**: a definição do campo personalizado existia, o "obrigatório para entrar nesta etapa" existia e bloqueava — mas **não havia rota nem tela para preencher o valor**. Um gestor podia marcar "Orçamento máximo" como obrigatório para "Proposta" e o corretor batia numa mensagem de erro ("preencha: Orçamento máximo") sem nenhum lugar no CRM onde pudesse escrever esse valor. A trava existia; a porta não. Fechado com `PATCH /leads/:id/campos` (tipagem por definição — número recusa texto, seleção recusa valor fora da lista, vazio apaga a chave) e `CampoPersonalizado`/`CamposPersonalizadosDoLead` no frontend (clique-para-editar para texto/número/data, pílula para sim-não, lista para seleção única e múltipla) — nas duas fichas, e os campos marcados "no card" agora aparecem no Kanban (`CardFunil`), que também nunca tinha ganho essa metade. Teste: `npm run teste:campos-personalizados` (fim a fim: configura o campo obrigatório, confirma que bloqueia, preenche pela rota nova, confirma que destrava).

- **A rosca do funil, no painel e no relatório de cada pessoa** (08/09/2026, pedido do Ali: *"aquela visão de gráfico de pizza… quando a gente clica, ela carrega de forma fluida, e aparece o gráfico certinho com os relatórios diferenciados por tempo de atendimento"*; `RoscaDoFunil`/`DetalheDaFatia`/`fatiasDoFunil` em `app.jsx`).

  **É distribuição, não conversão — e a diferença não é preciosismo.** Pizza só é honesta quando as fatias somam o todo, e cada lead em aberto está em exatamente UMA etapa. Conversão não soma: quem chegou em Venda também passou por Visita, e desenhar isso em pizza produziria um todo maior que o todo — o primo do "seq 300%" que já tinha custado a confiança na tela. A conversão continua onde ela significa alguma coisa: nas duas taxas do bloco de degraus, logo abaixo da rosca.

  **Clicar na fatia é o recurso, não o enfeite.** A fatia cresce, as outras desbotam, o miolo troca o total pelo nome da etapa, e abre o detalhe: quantos leads, **há quanto tempo estão parados ali** (mediana, não média — um lead esquecido há dois anos faria a etapa inteira parecer travada), quantos passaram do prazo, e as taxas de conversão quando aquela etapa é degrau comercial. É o "diferenciado por tempo de atendimento" do pedido.

  **A COR NÃO IDENTIFICA A FATIA — o rótulo identifica.** As cores são as que a empresa escolheu para as etapas, e têm que ser: a mesma etapa não pode ter uma cor no Kanban e outra aqui. Só que elas nunca foram desenhadas para virar paleta de gráfico, e o validador de paleta reprova: no funil padrão, "Aprovação" (azul) e "Pasta" (roxo) ficam a **ΔE 0.4 sob daltonismo verde** — a mesma cor, para quem tem deuteranopia —, e **Agendamento e Visita nascem com o MESMO hexadecimal**. Por isso toda fatia tem nome, número e porcentagem escritos ao lado, o miolo escreve a etapa aberta, e o vão entre fatias é de 3px e não 2 (com 2 as duas laranjas liam como uma fatia só). Quem não separa as cores lê tudo na legenda.

  **Teto de sete fatias**, com o resto virando "Outras N etapas" — o detalhe dessa fatia lista quais são. Acima de sete, fatias vizinhas viram um borrão mesmo com visão perfeita, e o funil da Conecta tem onze etapas.

  **O CORRETOR ENTROU NA ROTA DO PAINEL, e essa foi a parte perigosa.** A rosca do relatório individual vem da MESMA rota do painel do gestor (`GET /painel/funil/:pipelineId`), filtrada por responsável — e não de uma conta paralela montada na tela, senão a fatia "Pasta" do relatório e a fatia "Pasta" do painel passariam a significar coisas diferentes. Só que aquela rota era fechada à supervisão inteira. Abri **só ela**, e a trava não é esconder: quem não supervisiona tem o `responsavel` **sobrescrito pelo próprio id**, e o que ele mandar na query é descartado — sem isso, qualquer corretor leria o funil do colega trocando um parâmetro no endereço. O resto do painel (visão geral, equipe, campanhas, opções) continua fechado. Teste: `npm run teste:funil-do-corretor` (8 casos, com o servidor de pé; o caso 4 pede o funil do colega de propósito e exige receber o próprio).

  **A animação respeita `prefers-reduced-motion`** e a informação não depende dela: quem pediu menos movimento no sistema vê a rosca pronta, sem o desenho.

- **Tags do lead** (08/09/2026, primeiro item do roadmap comercial a ser construído; tabelas `tags` e `lead_tags`, `services/tags.js`, `GET/POST/PATCH/DELETE /tags`, `POST/DELETE /leads/:id/tags/:tagId`): a marcação livre que faltava — "investidor", "indicação da Marina", "só financia", "não perturbe". Etapa é uma só e anda em ordem; campo tem um valor por lead; observação é texto que ninguém consegue filtrar. A tag é o que sobra, e é **feita para ser filtrada**.

  **É também a peça que o motor de automação vai pedir primeiro**, e por isso ela veio antes: boa parte das ações que o roadmap descreve para ele ("ao entrar nesta etapa, marque X", "quem tem a tag Y não recebe") não existe sem isto. Construir o motor antes das tags seria entregar metade dos botões desligados.

  **DUAS TABELAS, e não um JSON no lead** — ao contrário dos campos personalizados, que guardam o valor em `leads.custom_fields`. A diferença é a pergunta que cada um responde: do campo se pergunta "quanto é o orçamento DESTE lead", sempre a partir do lead; da tag se pergunta o contrário, "QUAIS leads são investidores" — e é dessa pergunta que sai o filtro do funil e, depois, o disparo segmentado. Varrer o JSON de todos os leads para responder isso fica lento justamente quando a base cresce e a resposta começa a importar. A definição separada também é o que faz **renomear a tag valer nos leads que já a têm, sem varredura** — o oposto de `leads.stage`, que guarda o nome da etapa e obriga a atualizar todos os leads quando alguém renomeia.

  **Quem CRIA não é quem MARCA, e a divisão é diferente da dos campos.** Criar e apagar a definição é da supervisão: tag é o vocabulário da casa, e vocabulário que qualquer um inventa vira três jeitos de escrever a mesma coisa. **Marcar num lead é de quem pode abrir aquela conversa** — o dono e a supervisão, a mesma régua das observações: quem está atendendo é quem descobre que o cliente é investidor, e ter que pedir para a gestão marcar seria o mesmo que não ter a tag.

  **Nome repetido é recusado sem olhar maiúscula nem acento** (`chave()` normaliza): senão "Investidor", "investidor" e "Indicação"/"indicacao" viram tags diferentes, e o filtro passa a devolver metade da resposta.

  **A PALETA É FECHADA — sete cores, e o coral do sistema ficou de fora.** Cor escolhida a esmo num seletor livre produz amarelo ilegível e dois azuis que ninguém distingue, e a tag existe para ser lida de relance. As sete passaram no validador de paleta categórica (pior par adjacente em ΔE 9.1 sob protanopia, acima do piso de 8). O coral (`#E1553A`) é o sinal de urgência do CRM — cronômetro estourado, tarefa vencida —, e uma tag daquela cor faria "não perturbe" parecer alerta; mesma razão pela qual a cor da marca não pinta a tela inteira. **No card e na ficha o nome vai escrito junto da cor**, sempre: duas tags podem ter a mesma cor, e cor sozinha não é leitura para todo mundo.

  **Apagar é DE VEZ e leva as marcações junto** — diferente do campo personalizado, que se desativa guardando o valor. A diferença é o que se perde: o campo guarda algo que alguém digitou, a tag **é** a marcação, e uma tag "desativada" colada em quarenta leads seria uma marca invisível na tela e visível no banco. Por isso o servidor **recusa a primeira tentativa** dizendo em quantos leads ela está, e a tela pergunta com o número na frase — apagar em silêncio tiraria a marca de todos.

  **As tags chegam JUNTO na lista e na ficha**, numa consulta só para a lista inteira (`tagsDeLeads`): buscá-las por lead seriam sessenta requisições a cada dez segundos em todo aparelho da equipe. E `adaptLead` precisou listá-las campo a campo — o que não é citado ali chega do servidor e é jogado fora em silêncio, como já aconteceu com o `master` e a barra recolhida.

  **Um defeito achado pelo próprio teste**: as rotas devolviam a recusa em `erro` (português), e o navegador lê `error`. Toda mensagem escrita com cuidado ("já existe uma tag chamada Investidor") chegava na tela como a frase genérica "erro ao falar com o servidor". Os testes agora conferem a MENSAGEM e não só o código. Teste: `npm run teste:tags` (19 casos, com o servidor de pé, incluindo o isolamento entre imobiliárias: tag de uma casa não cola em lead de outra nem sabendo o id).

- **A tela de entrada: KPIs, metas e funil de atividade** (08/09/2026, pedido do Ali a partir do painel da Confidere Imóveis — "seria a mesma tela pra todas as contas... o corretor tem a mesma visão mas apenas com os seus indicadores, gestor ver a visão da operação toda assim como a da atendente"; `services/painel-geral.js`, `GET/POST /painel/geral`, `GET/POST /painel/metas`, tabela `metas`, `leads.sale_commission_pct`; aba **Painel** — primeiro item do menu para todo papel, inclusive corretor desde hoje): sete indicadores com comparação ao período anterior (VGV, Ticket Médio, VGC, Vendas, Leads, Lead em Venda %, Visitas ao imóvel), seis metas do mês com barra de progresso, um funil de atividade (Leads → Ligações → Contato → Visita Agendada → Visita Realizada → Proposta → Vendas) com as duas colunas de porcentagem do painel de referência, e uma barra empilhada dos últimos 14 dias.

  **UMA TELA, TRÊS ESCOPOS, E A CONTA É SÓ NO SERVIDOR.** `GET /painel/geral` é a mesma exceção do `/painel/funil/:pipelineId` (08/09/2026): quem não supervisiona sai de lá com `responsavel` sobrescrito pelo próprio id, e o que a query mandar é descartado. O corretor não tem um componente "dele" e outro "do gestor" — é a mesma tela, o mesmo fetch, e a diferença nasce inteira no filtro que o servidor aplica. Foi o mesmo raciocínio da rosca do funil: duas telas para a mesma pergunta é onde elas começam a divergir.

  **FUNIL DE ATIVIDADE ≠ FUNIL DE ETAPAS, e são leituras deliberadamente diferentes.** O `funil()` de `painel.js` mede por onde os leads passaram, em CONJUNTOS, sem contar duas vezes — é a rosca. Este mede VOLUME DE AÇÃO no período: quantas ligações, quantos contatos, quantas visitas aconteceram. Por isso "Ligações" pode ser MAIOR que "Leads" — um corretor liga várias vezes para o mesmo lead, e liga para leads de antes do período também. Tratar os dois números como a mesma pergunta seria repetir o erro que este projeto já corrigiu (a taxa sequencial de 300%, 28/08/2026): aqui as duas colunas de porcentagem (sequencial e sobre o total de leads) são sobre CONTAGENS de atividade, não sobre conjuntos — e são exatamente o que o painel de referência mostra.

  **LIGAÇÕES E ETAPAS SÃO CONTADAS POR QUEM PRATICOU, NÃO POR QUEM É DONO DO LEAD HOJE** — mesma régua que `atividades()` já usa em `painel.js` (`ligacoes WHERE user_id=?`). "Os meus indicadores" do corretor são as ações que ele fez; um lead repassado depois de uma ligação continua contando a ligação para quem ligou. A exceção é **Leads**, que não tem "quem praticou" — tem dono, e usa a mesma `peneira()` (agora exportada de `painel.js`) que o resto do painel.

  **VENDAS/VGV/VGC USAM A MESMA CONTA DO RESTO DO SISTEMA** — dono do lead + `sale_date`, a conta que `painel()` já usa para o card de vendas, e não `lead_etapas`. Duas contas diferentes para a palavra "vendas" na mesma tela era exatamente o tipo de redundância que a auditoria de 08/09/2026 mandou reduzir; o passo "Vendas" do funil de atividade e o card VGV/Vendas mostram sempre o mesmo número.

  **VGC — "percentual por venda"** (decisão do Ali): cada venda carrega a própria comissão (`leads.sale_commission_pct`, preenchida no card de venda da ficha, campo opcional). O VGC soma só as vendas COM comissão preenchida, e a tela escreve a cobertura ao lado ("1 de 2 vendas com comissão") — a régua de sempre: número que ninguém mediu não vira zero, vira "de fora da conta".

  **METAS SÃO MENSAIS, POR DEFINIÇÃO, MESMO QUE O FILTRO DE CIMA SEJA "ESTA SEMANA".** Meta é cadência de mês; se o progresso das metas seguisse o período dos KPIs, ele pularia para trás toda vez que alguém trocasse o filtro sem a meta em si ter mudado. `metas` guarda `user_id=''` (sentinela, não NULL — SQLite trata cada NULL como diferente de si mesmo dentro de um UNIQUE, e a trava "uma meta por alvo por mês" furaria exatamente para a meta da casa) para a meta DA OPERAÇÃO, e um id real para a meta PESSOAL de um corretor. **Meta não definida é `null`, nunca 0%** — mesma régua da visita confirmada e do lead sem temperatura.

  **CONFIGURAR É SÓ DO GESTOR** (`podeGerir`, botão "Configurar metas" no card, modal com a operação e cada corretor lado a lado, um "Salvar" por linha). `GET/POST /painel/metas` são `soGestao`; o corretor lê a PRÓPRIA meta dentro do `/painel/geral` normal, sem rota própria — ele não configura, só acompanha.

  **"Painel" entrou no menu do corretor puro** (antes só existia para quem supervisiona, ou para o corretor autônomo via `podeGerir`): primeiro item, como no gestor — é a foto do "como eu estou indo" antes de entrar na caixa de atendimento. O conteúdo antigo do Painel (recomendações da IA, fila da catraca, comparativo por corretor, ranking) **continua só da supervisão** — mostrá-lo ao corretor exibiria "Nenhum corretor cadastrado" ou um ranking de uma pessoa só, e a recomendação da IA (`GET /reports/recomendacoes`, `soGestao`) tem o erro engolido em silêncio (`.catch(()=>setD({itens:[]}))`), que para quem não tem permissão soa como "nada para decidir" em vez de "você não vê isto" — expor esse card a um público novo teria espalhado essa mentira branca para mais gente.

  **A PALETA REAPROVEITA `CORES_TAG`** (`services/tags.js`) em vez de validar uma paleta nova para mais um gráfico de sete séries — mesma decisão de reduzir redundância que orientou a auditoria de 08/09/2026.

  Teste: `npm run teste:painel-geral` (15 casos, com o servidor de pé — inclusive a trava de escopo, a matemática da variação sem inventar "+100%" sem base, e a permissão de metas).

- **Vídeo "carrega e não vai"** (09/09/2026, relatado pelo Ali: "os corretores não estão conseguindo enviar os vídeos"; `services/storage.js` → `limiteBytes`, `server.js`): o limite de vídeo dizia **60 MB** quando o armazenamento é o Cloudflare R2 — um número pensado só para o CUSTO DE GUARDAR o arquivo, sem olhar o CAMINHO até lá. O anexo sobe em base64 dentro do JSON (`POST /leads/:id/anexo`), que cai em `jsonGrande` — teto de **30 MB** (`server.js`) — e base64 infla o arquivo em ~33%. 60 MB vira ~80 MB codificado: quase o triplo do teto. A requisição nunca chegava a rodar `limiteBytes()` — o Express recusava o corpo antes de qualquer rota ver o arquivo, com uma resposta em TEXTO PURO (não `{error:...}`), que o `api()` do navegador não sabia interpretar. Resultado: o corretor via o vídeo "carregando" e nada saía, sem mensagem de erro nenhuma — a mesma família de defeito documentada várias vezes neste arquivo, onde a falha não avisa que aconteceu.

  **O limite passou a ser único — 20 MB, sem depender de R2 ou disco** (era só no disco antes). O gargalo real nunca foi o armazenamento, foi sempre o transporte: 20 MB em base64 cabe em ~26,7 MB, com folga dentro do teto de 30 MB. R2 guardaria 60 MB sem esforço, mas o arquivo não chega vivo até lá — aumentar o teto do corpo para acomodar 60 MB reabriria exatamente o risco que a auditoria de segurança de 02/09/2026 fechou (corpo grande é o jeito mais barato de derrubar o servidor), só que numa rota autenticada em vez de aberta.

  **E se MESMO ASSIM alguém mandar um corpo grande demais**, a resposta agora é um 413 em JSON legível (`app.use((err,req,res,next)=>...)` em `server.js`, tratando `err.type==="entity.too.large"`) — rede de segurança para quando o `express.json` recusa o corpo antes de qualquer rota rodar, e não mais um texto que quebra o front em silêncio. Teste: `npm run teste:anexo-video`.

  **O limite subiu de 20 para 30 MB, e o defeito de verdade era outro** (14/09/2026, pedido do Ali: *"aumenta um pouco mais o limite e verifica se tem algum bug no envio dos vídeos"*, depois de continuar ouvindo a mesma reclamação mesmo com este conserto já no ar). `limiteBytes()` (`storage.js`) e `jsonGrande` (`server.js`, agora **45 MB**) subiram JUNTOS, na mesma proporção de antes — os dois números têm que andar sempre juntos, senão volta o defeito original (limite da rota que nunca dispara porque o corpo já foi recusado primeiro).

  **O bug de verdade era um comentário mentindo sobre o próprio código.** `Anexar` (`app.jsx`) tinha um comentário dizendo que os limites "existem para avisar antes de subir MB à toa no 4G do corretor" — só que `escolheuVideo` não conferia tamanho NENHUM, e `escolheuFotos` só conferia a QUANTIDADE. Um vídeo grande demais era lido inteiro, convertido para base64 e mandado pela rede **antes** de o servidor dizer que ia recusar. No 4G da rua isso pode levar minutos, e se a conexão cair no meio do caminho (comum em sinal fraco), o erro que aparece é genérico de rede — não "vídeo grande demais". Para quem está olhando a tela, os dois casos são indistinguíveis do "carrega e não vai" original: o conserto de 09/09 era real, mas só protegia depois de o corretor já ter gasto o tempo e o dado do próprio celular.

  **Agora `escolheuVideo` e `escolheuFotos` conferem o tamanho ANTES de ler o arquivo ou tocar na rede** — mesma régua que já existia para a foto de perfil e o fundo do login (`if(f.size>X*1024*1024) return setErro(...)`, só que nunca tinha chegado até aqui. `LIMITE_MB_VIDEO`/`LIMITE_MB_FOTO` espelham `limiteBytes()`: se um mudar sem o outro, o aviso do navegador erra a régua, mas quem decide de verdade continua sendo o servidor — o aviso é só para não gastar tempo e dado à toa. Provado com Playwright: um vídeo de 35 MB (acima do novo limite) mostra o aviso na hora, **sem nenhuma requisição de rede disparar**; um de 10 MB passa direto e sobe normalmente.

  Teste: `npm run teste:anexo-video` (4 casos, servidor de pé, números atualizados para o novo limite: 25 MB passa; 31 MB é recusado pela ROTA citando "30 MB"; 40 MB estoura o corpo inteiro de 45 MB e ainda assim volta JSON legível; sem login nem chega a olhar o arquivo). A checagem do navegador não tem teste automático (é leitura de `File.size`, não resposta de rota) — conferida com Playwright simulando o clique real de um corretor.

  **150 MB, e vídeo saiu do base64 de vez** (14/09/2026, mesmo dia, pedido do Ali: *"aumenta o limite do vídeo para 150mb"*). Pedi para confirmar antes de mexer, porque 150 MB pelo caminho de base64 tinha dois problemas reais, não só um número grande: (1) em base64 vira ~200 MB de TEXTO, e o servidor precisa juntar essa string inteira na memória e rodar `JSON.parse` nela — com o processo TRAVADO nesse tempo, porque Node é de uma thread só; um corretor subindo um vídeo grande atrasaria o webhook de lead novo da imobiliária inteira, que desiste de chamar se demorar. (2) o WhatsApp tem limite PRÓPRIO para mensagem do tipo "vídeo", que não é nosso e não tínhamos como confirmar de antemão. O Ali escolheu resolver o (1) de verdade e aceitar o risco do (2) (que vira erro visível da Uazapi, não silêncio).

  **Vídeo GRANDE saiu do `/anexo` e ganhou rota própria: `POST /leads/:id/anexo/video`** (`messages.routes.js`, `acoes.anexarVideo` em `app.jsx`). O corpo é o arquivo CRU — sem base64, sem JSON — lido por `express.raw()` como middleware de ROTA (não `app.use()` sem caminho, a armadilha de 13/08/2026 já documentada aqui: middleware sem caminho vale para tudo que vem depois dele). `mime`/`nome`/`legenda` vão na query, porque o corpo aqui é só o arquivo.

  **O teto global de JSON (`jsonGrande`/`jsonNormal`, `server.js`) nem chega a ver este upload.** Aquele middleware só reage a `Content-Type: application/json`; o navegador manda o vídeo com `Content-Type: video/...`, então o `express.json()` passa direto sem tocar no corpo — quem decide o limite aqui é só o `express.raw()` da própria rota. Foi por isso que **não precisou subir o `jsonGrande`** desta vez: as duas rotas de anexo (a antiga, em base64/JSON, e a nova, crua) nunca competem pelo mesmo corpo de requisição, então `limiteBytes()` (30 MB, para foto/áudio/documento e para vídeo caso algum caminho antigo ainda chame a rota velha) e `LIMITE_VIDEO_MB` (150, só da rota nova) são números independentes de propósito.

  **O navegador manda o `File` direto, sem passar por FileReader/base64** (`acoes.anexarVideo`). A versão antiga lia o vídeo inteiro para memória e o transformava em texto ANTES de começar a subir — para 150 MB isso é pesado até para o celular do corretor. Mandando o arquivo puro como corpo do `fetch`/XHR, o navegador transmite os bytes originais.

  **Usa `XMLHttpRequest`, não `fetch`, só por causa do progresso.** É o único jeito de saber, no navegador, quantos % de um upload já saíram (`xhr.upload.onprogress`) — sem isso, um vídeo de 150 MB numa rede fraca pode levar minutos sem NENHUM sinal na tela, que é exatamente a cara do "travou" relatado duas vezes antes. Agora o botão de anexar mostra "Enviando vídeo… 42%" enquanto sobe.

  **150 MB é o que foi pedido, não uma garantia de entrega.** Se a Uazapi recusar o vídeo por tamanho do lado do WhatsApp, a resposta chega como erro de verdade (o `sendMedia` já repassa a mensagem do provedor) — nunca como silêncio. Isso é uma limitação de fora do nosso sistema, e não tem como testar sem uma conta real conectada.

  Teste: `npm run teste:anexo-video-binario` (6 casos, servidor de pé: vídeo normal sobe e fica registrado na conversa; 151 MB é recusado pela rota citando "150 MB"; corretor sem acesso ao lead recebe 403; sem login nem chega a olhar o arquivo; mime que não é vídeo é recusado antes de processar; e a prova de que o teto global de JSON não participa — 60 MB, acima do `jsonGrande` de 45 MB, chega até a rota, que então recusa por não ser vídeo de verdade — 422 do `ffprobe`, não o 413 genérico que o `server.js` devolveria se o corpo tivesse sido barrado antes de chegar aqui). Confirmado com Playwright, ponta a ponta pela tela de verdade: um vídeo de 160 MB é barrado pelo navegador sem nenhuma requisição de rede, e um de 40 MB sobe pela rota nova, com o Content-Type certo e o badge de progresso aparecendo na tela.

  **HEVC (o padrão do iPhone) vira H.264 antes de ir pro WhatsApp** (14/09/2026, mesmo dia, relatado pelo Ali: *"o vídeo que estão tentando enviar é em formato HEVC e não tá carregando, preciso que suporte mais formatos"*; `services/video.js` → `garantirH264`). Desde o iOS 11 o iPhone grava vídeo em HEVC por padrão, e o WhatsApp — oficial ou não — só aceita mensagem de vídeo em H.264/AAC. **Não era falha nossa**: o CRM sempre aceitou o arquivo (o filtro é "começa com `video/`", nunca olhou o codec por dentro) e o servidor sempre subiu certinho; quem recusava, em silêncio, era o WhatsApp do outro lado — a mesma família de defeito que este arquivo já documentou várias vezes, só que desta vez a causa raiz é externa.

  **Pedi para confirmar antes de instalar algo novo no servidor**, porque não é uma mudança de número: rodar conversão de vídeo exige uma ferramenta externa de verdade (`ffmpeg`), não uma bibliotequinha — e o Ali topou. Os pacotes `ffmpeg-static`/`ffprobe-static` baixam o binário certo pra a plataforma durante o `npm install`, sem precisar configurar nada a mais no Railway (nenhum buildpack, nenhum Dockerfile) — é só mais duas linhas em `dependencies`.

  **SÓ CONVERTE QUANDO PRECISA.** Rodar `ffmpeg` custa CPU e tempo de verdade, e a maioria dos vídeos que chegam já está em H.264 (Android, ou iPhone com "Mais compatível" ligado) — recodificar todo vídeo penalizaria o caso comum para cobrir o raro. `ffprobe` lê o CODEC DE VERDADE do arquivo antes de decidir qualquer coisa (`stream=codec_name`, não a extensão nem o `mime` que o navegador mandou — os dois podem mentir); só quem não é `h264` passa pelo `ffmpeg`. Um vídeo já em H.264 sai da função com o MESMO buffer, sem tocar em nada — é o caminho rápido, e tem que continuar rápido.

  **A conversão acontece DEPOIS da checagem de tamanho, ANTES de salvar e mandar pro WhatsApp** — na mesma rota `POST /leads/:id/anexo/video` de 150 MB. Ordem importa: checar tamanho primeiro evita gastar CPU convertendo um arquivo que já ia ser recusado; converter antes de salvar evita guardar (e depois ter que trocar) o arquivo errado. **O nome muda de extensão junto com o conteúdo** (`.MOV` vira `.mp4`) — arquivo com extensão mentindo sobre o que tem dentro confunde quem baixar e tentar abrir no computador.

  **Arquivo que não é vídeo de verdade — bytes corrompidos, ou qualquer coisa que não seja mídia — é recusado com 422 e uma frase que diz isso**, não trava nem derruba o servidor: 5 minutos de teto no processo do `ffmpeg`/`ffprobe` (`TIMEOUT_MS` em `services/video.js`) é a rede de segurança contra um arquivo problemático prender a requisição para sempre — melhor um erro claro do que uma espera sem fim.

  **"Processando vídeo…" aparece na tela quando o upload termina mas a resposta ainda não chegou** (`Anexar`, `app.jsx`): com a conversão, existe agora um tempo de espera DEPOIS dos 100% do upload em que o servidor está trabalhando (recodificando), sem nenhuma forma de mostrar porcentagem (é processamento, não transferência). "100%" parado por mais tempo sem explicação voltaria a parecer travado — o texto muda sozinho nesse ponto, em vez de ficar preso em "100%".

  **`/integracoes` ganhou o bloco `conversao_video`** — confere que os binários existem e respondem, sem converter nada, na mesma régua de todo outro recurso externo deste painel (e-mail, IA, armazenamento): "a ferramenta está instalada?" antes de "por que não funcionou?".

  Teste: `npm run teste:video-hevc` (4 casos, servidor de pé, usando o PRÓPRIO `ffmpeg-static` para gerar vídeos de teste H.264 e HEVC de verdade — não é possível provar conversão de codec com bytes falsos). O teste 1 não confere só a resposta da rota: ele lê o ARQUIVO DE VOLTA do disco depois do envio e roda `ffprobe` nele, confirmando que o que ficou salvo é H.264 de verdade, byte a byte gerado pelo `ffmpeg` — não apenas que a rota disse `"convertido":true`. O teste 2 prova o oposto: vídeo que já era H.264 sai salvo EXATAMENTE igual ao que entrou (nenhum byte muda), provando que o caminho rápido não faz trabalho à toa. Confirmado também com Playwright, subindo um `.mov` HEVC de verdade pela tela: aparecem "Enviando vídeo…" e depois "Processando vídeo…", e o arquivo que fica salvo no disco — conferido com `ffprobe` de fora do CRM — é H.264, com o nome já em `.mp4`.

- **Notificação some sozinha, de novo — mas desta vez o conserto já existia e só rodava no lugar errado** (14/09/2026, relatado pelo Ali: "as notificações estão desativando sozinhas da conta do pessoal"; `curarInscricaoPush` em `app.jsx`). Reli o conserto de 17/08/2026 inteiro: `atualizarConHub()` continua usando `registration.update()` (não desregistra o service worker), o `sw.js` continua reinscrevendo sozinho em `pushsubscriptionchange` e chamando `/push/trocar`, e nenhum dos dois regrediu. O que sumia continuava sumindo pelo motivo de sempre — o navegador revoga notificação de site que ninguém visita, o sistema troca a chave sozinho — e o CRM já sabia SE CONSERTAR sozinho para isso, com uma rede de segurança que reinscreve em silêncio quando a permissão continua concedida.

  **O problema é ONDE essa rede de segurança vivia: só dentro do efeito de `Notificacoes`, e essa tela é Minha conta.** O corretor não mora em Minha conta, mora em Atender — abre o CRM, atende, fecha. Uma inscrição podia morrer e ficar morta por semanas, até o dia em que ele abrisse Minha conta por outro motivo qualquer (trocar a senha, ver o WhatsApp pessoal) — às vezes nunca. Do lado de quem usa, isso é indistinguível de "a notificação desliga sozinha", porque na prática ela ficava desligada até um acaso.

  **A correção não foi reescrever a lógica — foi tirá-la do lugar escondido.** `curarInscricaoPush(acoes)` virou uma função só, chamada em dois lugares: dentro de `Notificacoes` (que ainda precisa do resultado para desenhar "ativo neste aparelho") e uma vez no login, no componente raiz (`ConCRM`), que roda para TODO papel em TODA sessão aberta — não gasta em duplicar a lógica em dois lugares que divergem com o tempo, é a mesma função chamada duas vezes. **Não pede permissão nova**: só reaproveita a que a pessoa já concedeu (`Notification.permission==="granted"`) — pedir de novo sem clique da pessoa seria o site abrindo um popup de permissão sozinho, o oposto do que "conserta em silêncio" quer dizer.

  **O que continua fora do alcance do código, e precisa estar dito**: se o navegador REVOGOU a permissão (não só perdeu a inscrição — Chrome revoga notificação de site pouco usado, e o iPhone limpa dado de site fora da Tela de Início), `Notification.permission` deixa de ser `"granted"`, e aí não tem conserto silencioso possível — a pessoa precisa clicar em "Ativar" de novo. Não há teste automático cobrindo isto: é comportamento do navegador, não do servidor, e `npm run teste:push` (que já existe) continua provando a metade que É nossa — a troca de inscrição sem perder o corretor de vista.

- **Baixar foto e documento da conversa, como no WhatsApp — e por que a primeira versão não baixava** (14/09/2026, pedido do Ali; segunda parte relatada no mesmo dia: *"a função de baixar abre o arquivo... em uma nova aba... preciso que baixe direto ao clicar"*; `BotaoBaixar`/`Midia` em `app.jsx`, rota `GET /leads/:id/anexo/:messageId/baixar` em `messages.routes.js`, `chaveDaUrl` em `services/storage.js`).

  **A primeira versão buscava a URL da mídia direto** (`fetch`+`blob`+`URL.createObjectURL`), e funcionou no teste feito com armazenamento em disco — mesma origem do CRM. Em produção, com o Cloudflare R2 (metade das contas), a URL do arquivo é OUTRO domínio, e o R2 não manda cabeçalho de CORS por padrão. Um `fetch()` do navegador para outra origem sem `Access-Control-Allow-Origin` é **bloqueado antes do JavaScript conseguir ler a resposta** — o `catch` engolia o erro e caía no `window.open(url)`, e é exatamente esse o "abre numa aba" que o Ali viu, para foto, áudio **e** documento ao mesmo tempo, porque os três passam pelo mesmo `BotaoBaixar`. Provado com Playwright: a mesma chamada que falha com "Failed to fetch" contra uma origem diferente sem CORS funciona normalmente contra a rota nova, na mesma origem da página.

  **O conserto é o navegador nunca falar direto com o R2.** `BotaoBaixar` agora busca de `/leads/:id/anexo/:messageId/baixar` — sempre a MESMA origem do CRM, então CORS não entra em jogo — e é o SERVIDOR quem busca o arquivo de verdade, com `bytesDoArquivo` (a mesma função que já lia do disco ou do R2 para o envio ao WhatsApp, quando a URL pública não estava alcançável de fora). `chaveDaUrl(url)` faz o caminho inverso de `salvar()`: reconhece tanto `.../arquivos/<chave>` (disco) quanto `<R2_PUBLIC_URL>/<chave>` (R2) e devolve a chave; sem reconhecer nenhum dos dois formatos (arquivo bem antigo, outro provedor), a rota busca a própria URL a partir do servidor — pior que o caminho normal, melhor que recusar.

  A rota confere `podeVerLead` antes de tudo (mesma régua de sempre: quem não pode abrir a conversa não baixa o anexo dela) e responde com `Content-Disposition: attachment` e `X-Content-Type-Options: nosniff` — é o servidor mandando "baixe", não "abra", igual à `/arquivos` estática.

  **Foto e vídeo têm um botão redondo no canto**, do jeito que o WhatsApp mostra — sem tirar o clique-para-abrir que já existia (ver e baixar continuam duas ações diferentes). Documento e áudio têm o mesmo ícone ao lado do que já tinha. Teste: `npm run teste:baixar-anexo` (7 casos, com o servidor de pé — arquivo baixado bate byte a byte com o original, cabeçalhos corretos, 403 para quem não pode ver o lead, 401 sem login, 404 para mensagem sem mídia ou inexistente). E conferido num navegador de verdade com Playwright, simulando o cenário que os testes de servidor não alcançam: um arquivo servido por OUTRA origem sem CORS — provando que o `fetch` antigo falha ali e o novo, pela rota própria, funciona.

- **"O cliente responde uma mensagem específica e no CRM ela aparece solta"** (17/09/2026, relatado pela SDR via Ali). O caminho inteiro — extrair a citação do webhook, resolver o id do WhatsApp para a mensagem local, devolver a citação já resolvida em `GET /leads/:id`, desenhar o cartão de citação no balão — **nunca tinha teste automático**, e a auditoria linha a linha (`uazapi.webhook.js` → `mensageria.js` → `leads.routes.js` → `app.jsx`) não achou nada errado no CAMINHO: `npm run teste:citacao-recebida` prova o caminho feliz inteiro, de ponta a ponta, e ele funciona.

  **O que sobrou são as duas causas que este projeto já documentou para o mesmo tipo de campo, e que falham CALADAS — a Uazapi responde 200 e ignora o que não reconhece, ela não erra.** (1) A mensagem que o cliente respondeu foi ENVIADA por este CRM, e a resposta da Uazapi para aquele envio pode não trazer nenhum dos nomes de id conhecidos (`idDaMensagem`, já documentado como "nenhum dos caminhos é garantido" desde a citação de saída, 09/08/2026) — sem id, a mensagem nunca grava `wa_id`, e nunca vai poder ser o ALVO de uma citação depois, para sempre, mesmo que o resto funcione perfeitamente. (2) O payload da Uazapi PARA ESTA CONTA usa, para "id da mensagem respondida", um nome de campo fora da lista de apelidos conhecidos (`quotedMessageId`, `quoted.messageid`, `quoted.id`, `contextInfo.stanzaId`, `context.id`, `replyid`) — igual já aconteceu com o campo de citação de SAÍDA, que precisou de cinco apelidos mandados juntos porque não dava pra saber qual a conta aceitava sem tentar.

  **Sem acesso ao payload real de produção, adivinhar mais um nome de campo seria o mesmo erro de novo** — e por isso a correção desta vez não é mais um apelido no meio do caminho, é a MESMA ferramenta que já resolveu o problema da citação de saída: um diagnóstico que guarda a FORMA do que aconteceu (nomes de campo, nunca conteúdo) para a PRÓXIMA vez que isto se repetir aparecer com a resposta, em vez de continuar invisível.

  Três avisos novos, nenhum deles muda o comportamento — a mensagem sempre entra, citada ou não, exatamente como antes:

  `/integracoes` → **`envio_sem_id`**: a última vez que um ENVIO (de texto normal, não só citação) voltou da Uazapi sem nenhum id reconhecido, com o caminho chamado e os nomes de campo que vieram na resposta. Se isto tiver alguma coisa, a explicação está aqui: mensagens enviadas depois desse momento nunca puderam ser citadas pelo cliente.

  `/integracoes/webhooks` → **"a mensagem cita outra, mas nenhuma mensagem desta conversa tem esse id do WhatsApp guardado"**: o campo de citação FOI reconhecido, mas o id que ele trouxe não bate com o `wa_id` de nenhuma mensagem local — aponta direto para a causa (1) acima.

  `/integracoes/webhooks` → **"a mensagem parece responder outra, mas nenhum campo conhecido trouxe o id — pistas: ..."** (`pistasDeResposta` em `uazapi.webhook.js`, varre o payload inteiro procurando nomes de chave com `quot`/`reply`/`context`/`stanza`/`cited`/`refer`, em qualquer profundidade, **nunca o valor**): aponta para a causa (2) — e desta vez com o NOME REAL do campo desta conta, pronto para virar mais um apelido na lista sem precisar adivinhar de novo. Teste: `npm run teste:citacao-recebida` (prova o caminho feliz e os dois avisos, com o servidor de pé).

- **Uma seção do Kanban só para aluguéis, e o "Etapa do funil" que não via etapa nova** (17/09/2026, pedido do Ali). Duas coisas, e a segunda apareceu por causa da primeira.

  **A seção de locação já existia pronta — faltava só um jeito de mandar UM lead para lá.** O template "Locação" (Lead novo → Qualificado → Visita → Documentação → Análise → Contrato → Locado) está em `services/templates.js` desde 28/08/2026; criar o funil é **Configurações → Funis e etapas → Novo funil → modelo "Locação"**, sem código nenhum. O que faltava era mover um lead ESPECÍFICO para lá no momento em que alguém descobre no meio da conversa que é aluguel — a ferramenta que já existia (`services/lote.js` → `moverParaFunil`) move todos os leads de uma PESSOA de uma vez, não serve para um só. Novo componente `MoverParaOutroFunil` na ficha do lead ("Funil: Comercial — mover para outro"), reusando a mesma rota `PATCH /leads/:id/stage` de sempre (já aceitava `stage_id` de qualquer funil da empresa, não só do lead) — **o responsável não muda**, e entra sempre na primeira etapa do destino, mesma regra da ferramenta em lote.

  **Isso expôs um bug de verdade, relatado no mesmo dia**: *"quando eu atualizo uma nova etapa no funil ela não aparece na conversa"*. Os DOIS seletores "Etapa do funil" que aparecem durante o atendimento — o embutido em `Atendimento` (o que o corretor usa no dia a dia) e o da `FichaLead` (usado pela supervisão em `Conversas`) — mostravam a lista fixa `STAGES`: as 11 etapas do funil "Comercial" ORIGINAL, escritas no código antes de 28/08/2026, sempre as mesmas não importa em que pipeline o lead estivesse de verdade. O Kanban já lia as etapas certas desde a fundação dos múltiplos funis (`pipeAtual.stages`); só esses dois seletores, que nasceram antes e nunca foram migrados, ficaram presos na lista velha. Etapa nova criada num funil, ou um funil inteiro diferente (Locação, SDR, Recaptação), nunca aparecia neles — o gestor criava a etapa em Configurações, via ela funcionar no Kanban, e o corretor continuava sem conseguir escolhê-la na conversa.

  **`usarEtapasDoLead(lead,acoes,session)` — um hook só, usado pelos dois seletores E pelo `MoverParaOutroFunil`** (é a mesma pergunta: quais etapas este lead pode ter). Busca o pipeline do lead pelos mesmos dados que o Kanban já usa (`acoes.pipelines()`) e cai para `STAGES` só quando o pipeline ainda não carregou — nunca um quadro vazio no meio do carregamento.

  **Achado no caminho, achado por sorte antes de qualquer usuário ver**: o componente novo tinha sido nomeado `MoverFunil` — mas já existe um `function MoverFunil` (a ferramenta em lote da ADM, dentro de `ArrumarBase`, de 28/08/2026). Dois componentes com o mesmo nome no mesmo arquivo (sem imports, sem módulos) não dá erro de build nenhum — o segundo declarado simplesmente sobrescreve o primeiro, e todo lugar que chamava `<MoverFunil lead=.../>` passou a renderizar o componente ERRADO (o da ADM, esperando outras props) sem nenhuma mensagem. Renomeado para `MoverParaOutroFunil`. É a mesma família de defeito que este arquivo já documentou várias vezes — falha que não avisa que aconteceu —, só que desta vez em tempo de BUILD, não de produção: pegou na conferência antes do commit, com Playwright abrindo a tela de verdade.

  Confirmado com Playwright: criada uma etapa nova via API num pipeline já em uso, o seletor da conversa mostra ela na hora, sem precisar de mais nada além de abrir o lead — e o `MoverParaOutroFunil` aparece corretamente abaixo, sem os dois se atropelarem.

- **"Algo quebrou nesta tela" ao clicar em "Registrar simulação" — React error #310** (18/09/2026, relatado pelo Ali com um print da tela de erro, um dia depois do conserto acima). O conserto da etapa do funil (item anterior) tinha um bug próprio, achado só em produção: `usarEtapasDoLead` chama hooks do React por dentro (`useState`/`useEffect`, dentro de `usarPipelines`), e as duas chamadas estavam no lugar ERRADO.

  **A regra dos hooks: toda chamada tem que acontecer em TODA renderização do componente, na mesma ordem — nunca atrás de um `if`/`return` condicional, nunca dentro de um bloco que às vezes não é desenhado.** As duas chamadas que eu tinha escrito quebravam essa regra de dois jeitos diferentes, e os dois batiam bem com "algo quebrou" sem padrão óbvio:

  Em `FichaLead`, a chamada vinha DEPOIS do `if(simulando) return <Simulacao .../>`. Com a ficha normal (`simulando=false`) o React contava um hook a mais do que contava com a simulação aberta (`simulando=true`, que sai por aquele `return` antes de chegar na chamada) — e alternar entre as duas é exatamente o clique em **"Registrar simulação"**.

  Em `Atendimento` (a tela do corretor), a chamada morava dentro de `{showFicha&&!simulando&&<div>...}` — e `showFicha` é falso sempre que não há lead selecionado. Selecionar um lead, ou entrar/sair da simulação, muda quantos hooks o React viu naquela renderização, pelo mesmo motivo.

  **O conserto não foi mudar O QUE o hook faz — foi mudar ONDE ele é chamado.** As duas chamadas subiram para o topo de cada componente, ANTES de qualquer `if`/`return`, e passaram a rodar em TODA renderização — só o que é FEITO com o resultado (`etapasFunil`) continua condicionado à tela certa. `usarEtapasDoLead` ganhou uma guarda para `lead` nulo (`Atendimento` pode chamá-la sem lead nenhum selecionado ainda), devolvendo a lista `STAGES` de sempre nesse caso.

  **Por que isso não apareceu no teste do dia anterior**: o Playwright de ontem abriu um lead e leu o `<select>` — nunca clicou em "Registrar simulação" nem trocou de lead depois de aberto, os dois gestos que expõem a mudança na contagem de hooks. Confirmado agora com os dois gestos de propósito: abrir um lead, clicar em "Registrar simulação" (o gatilho exato do relato), fechar e trocar de lead — sem erro nenhum no console e sem a tela de "algo quebrou".

- **"Os relatórios de leads recebidos e leads vendidos não estão sendo atualizados automaticamente"** (18-19/09/2026, relatado pelo Ali: "quando ele recebe 4 leads no dia, aparece que ele recebeu 2 ou que ele recebeu 7"). Dois defeitos reais, os dois em código, nenhum de "cache" ou "precisa recarregar" — a tela sempre busca do banco na hora.

  **1. Venda gravava com a data de ONTEM.** `PATCH /leads/:id/venda` fazia `new Date(data).getTime()` com `data` no formato "AAAA-MM-DD" — o que o campo de data do formulário sempre manda. Uma string de data SEM HORA é interpretada pelo JavaScript como meia-noite em UTC, e Petrolina/Juazeiro é UTC-3: a venda registrada hoje gravava três horas ANTES da meia-noite local, ou seja, em ontem. Os relatórios filtram `sale_date` pelos limites do dia em hora LOCAL (`inicioDoDia`/`fimDoDia`, `resolverPeriodo` — que já faziam isso certo), então a venda ficava sistematicamente fora do "hoje" que devia mostrá-la. Corrigido ancorando em meia-noite local (`` `${data}T00:00:00` ``), o mesmo padrão que o resto do sistema já usa.

  **2. "Recebidos" contava pela data em que o LEAD NASCEU, não pela data em que ele ficou com ESTA PESSOA.** Nesta casa todo lead nasce com a atendente e é repassado depois — é a regra do negócio, não a exceção. A tela de Relatórios (`routes/reports.routes.js`) e o Score (`services/score.js`) filtravam por `created_at` (quando o lead entrou no CRM) e depois por `assigned_to` (de quem é AGORA), misturando duas perguntas diferentes: "quando o lead nasceu" e "quem é o dono hoje". Consequência prática: um lead repassado HOJE, criado há três dias, não contava como recebido hoje — o corretor recebia 4 de verdade e o relatório mostrava 2.

  **O conserto usa `leads.assigned_at`** — a mesma coluna que já carimba "toda atribuição" e sustenta o selo "novo com você" (13/08/2026) — em vez de `created_at`, para responder a pergunta certa: "quando este lead ficou com ele". `services/painel.js` → `atividades()` já fazia essa conta certa desde 28/08/2026 (`recebidosNoPeriodo`); o que faltava era levar o mesmo critério para as DUAS telas que Ali realmente olha no dia a dia — Relatórios e Score — que ainda usavam `created_at`. As duas foram corrigidas juntas, e de propósito: `npm run teste:score` já existe desde 13/08/2026 só para flagrar quando as duas contas divergem, e deixar uma corrigida e a outra não teria reproduzido o mesmo "os números não batem" um passo adiante, entre as duas telas em vez de dentro de uma só.

  **O que NÃO mudou, de propósito**: `por_etapa`, `agendamentos`, `conversão` e os grupos por temperatura continuam de COORTE (por `created_at`) nas duas telas — essas perguntas são legitimamente "dos leads que entraram no período, onde eles estão hoje / quantos converteram", diferentes de "quantos ele recebeu". Misturar as duas de novo reabriria o problema que a documentação de KPIs já separa.

  **Limitação conhecida deste conserto — resolvida no dia seguinte** (ver a entrada de 19/09/2026 logo abaixo): "recebidos" ainda era lido pelo dono ATUAL dentro da janela de `assigned_at`, e um lead repassado adiante, em outro dia, sumia do "recebidos" daquele dia para quem o teve primeiro.

  Teste: `npm run teste:relatorio-recebidos` (4 casos, servidor de pé — repasse de lead antigo contando no dia certo, não contando no dia errado, venda de hoje gravando no dia local certo e aparecendo no relatório de hoje).

- **O relatório fiel ao que aconteceu, e uma penalidade nova por leads perdidos** (19/09/2026, pedido do Ali no dia seguinte ao conserto acima: *"se eu transferir um lead de um corretor para o outro, ele não pode sair do relatório do antigo, porque ele teve um atendimento feito por aquele antigo... tem que ser mantido os relatórios de maneira fiel a tudo que foi feito. Inclusive, acrescentar a quantidade de leads que foram perdidos pelo corretor anterior... isso gera uma penalidade... Se atualiza no Kanban uma venda, ela não entra no relatório. Se é transferido cinco leads para o corretor, identifica que só foi transferido um."*): quatro problemas na mesma família — o relatório respondendo pelo ESTADO ATUAL do lead em vez de pelo que de fato aconteceu.

  **`eventosDeAtribuicao(orgId)` (`services/movimento.js`) — o registro, não a foto.** `lead_transfers` já grava toda troca de dono desde 28/08/2026 (`trocarResponsavel`, o único caminho por onde o dono muda depois que o lead já tem um), mas só a partir da SEGUNDA vez que o lead tem dono — a primeira atribuição é um `INSERT` na criação do lead (a catraca já entrega com dono), não uma troca, e não tem "de onde" para registrar. A função reconstrói os dois: o dono de NASCENÇA é o `from_user_id` da mais antiga linha de `lead_transfers` do lead, se ele já trocou de mão (ou `COALESCE(assigned_at, created_at)` quando nunca trocou — mesma régua de sempre, e o que mantém compatível qualquer atribuição direta que não passe por `trocarResponsavel`). Devolve duas listas — `recebidos` (quando cada lead entrou na mão de cada pessoa) e `perdidos` (quando saiu, com o `motivo`) — usadas por `reports.routes.js` E `services/score.js`, calculada UMA VEZ por chamada, não por corretor.

  **"Recebidos" de um período FECHADO não muda mais depois.** Antes, um lead recebido dia 4 e repassado dia 6 saía do "recebidos" de dia 4 assim que alguém abrisse o relatório DEPOIS do repasse — o número de um dia já fechado mudava sozinho, para trás. Agora ele lê o EVENTO (`eventos.recebidos`, filtrado por `noPeriodo`), não `leads.assigned_to` (o dono de agora). Vale para o corretor (`linhas`) e para a atendente (bloco `atendimento` — era a mesma doença, só que pela data em que ELA fez o primeiro contato).

  **"Leads perdidos para outro corretor" — a penalidade nova.** `leads_perdidos_para_outro`, visível direto na tabela de Relatórios e como componente da nota (`services/score.js`, peso 10, em PORCENTAGEM dos recebidos — não em contagem crua, mesma régua da "Perda": quem recebe mais não pode ser penalizado só por receber mais). Só conta repasse com `motivo === "mao"` — decisão de gente tirando o lead da mão dele. `automatica` (a etapa redistribuiu sozinha) e `saida_equipe` (o colega saiu da equipe) ficam de fora: nenhuma das duas é desempenho da pessoa, e contá-las inventaria uma culpa. 30% é o teto escolhido para este primeiro corte — sem histórico da Conecta com este número, é um chute deliberadamente conservador, ajustável num lugar só (`COMPONENTES` em `score.js`) se a régua se mostrar errada na prática.

  **Kanban → "Venda" sem valor passou a ser RECUSADO**, do mesmo jeito que qualquer campo obrigatório faltando (`camposQueFaltam`, `services/etapas.js`). Arrastar o card chama `moverLead`, não a rota `PATCH /leads/:id/venda` — a ÚNICA que grava `sale_value`/`sale_date`, o par que todo relatório de dinheiro lê. Sem a trava, o card ia para a coluna certa e a venda simplesmente não existia em nenhuma conta — o Kanban dizia uma coisa e o relatório dizia outra. **Não é `required_fields` configurável** — é a etapa de destino ter `status_type === "ganho"`, o que já nasce assim em todo template e no funil migrado, sem configuração nova em conta nenhuma. **MAS NEM TODO "GANHO" É DINHEIRO**: a etapa "Lead qualificado" do funil do SDR e "Reativado" da Recaptação também são `status_type: "ganho"` — o "ganho" delas é entregar ao comercial ou o lead voltar a responder, não uma venda. Exigir valor ali bloquearia a automação de distribuição do SDR por um dado que aquela etapa nunca teve (pegou no teste 35 de `pipelines.mjs` na primeira rodada). Por isso a trava também confere o TIPO do pipeline (`pipelines.type`) e só exige o valor fora de `sdr`/`recapture`. A mesma trava foi ligada num segundo caminho que também podia gravar "Venda" sem valor, em silêncio: o botão "Confirmar" da recomendação de etapa por palavra-chave (`POST /leads/:id/sugestao-etapa`) chamava `moverEtapa` direto, sem passar pelo `moverLead` que confere isso — "contrato" é um dos gatilhos e aponta para "Venda".

  **"Transferi cinco, o sistema mostra um" — o bloco da atendente tinha a mesma doença.** `repassados` filtrava `dela.assigned_to !== u.id` no ESTADO ATUAL: um lead devolvido à fila (sem dono agora) ou repassado de novo por engano desaparecia da conta, mesmo o repasse original tendo acontecido dentro do período. Agora usa o mesmo `eventos.perdidos` do bloco do corretor. `primeiro_contato`/`primeira_resposta_mediana_min` tinham a variante COORTE do mesmo bug (filtravam pela data de CRIAÇÃO do lead, não pela data do CONTATO) e foram corrigidos junto, mais o filtro de imobiliária que faltava na consulta (lia mensagem de toda conta antes de cruzar com os leads desta — o resultado final não vazava, mas lia à toa).

  **O que continua de COORTE, de propósito**: `sem_contato` e `na_fila` respondem "dos que chegaram agora, quantos...", a mesma pergunta de `por_etapa`/`agendamentos` — não são a mesma pergunta que "recebidos"/"repassados", e não deveriam responder como se fossem.

  Teste: `npm run teste:historico-fiel` (o relatório de um dia fechado não muda depois de um repasse seguinte; a penalidade ignora repasse automático; o Kanban recusa "Venda" sem valor e aceita registrando a venda de verdade; a atendente aparece com os 5 repasses mesmo com um deles voltando para a fila).

- **Baixar anexo não funcionava no celular** (21/09/2026, relatado pelo Ali: "não tá funcionando corretamente no celular deles"). O conserto de 14/09 (`BotaoBaixar`) tirou o "abre numa aba" no COMPUTADOR — buscava o arquivo com `fetch`+`Authorization`, transformava em blob e simulava um clique num link `<a download>`. Isso nunca funcionou de verdade no Safari do iPhone: ele **ignora o atributo `download` em URLs de blob** para a maioria dos tipos de arquivo — o link só abre/mostra o arquivo, não salva. É a mesma cara do defeito antigo, reaparecendo no aparelho que o corretor mais usa em campo.

  **A única forma confiável em celular é uma navegação DE VERDADE** para uma URL com `Content-Disposition: attachment` — aí o sistema operacional usa o mecanismo NATIVO de salvar arquivo, e nenhum navegador trata isso como "sair da página" (baixa e continua na tela — vale em desktop também, então não precisa de dois caminhos por aparelho). O problema é que navegação não leva o cabeçalho `Authorization`, e colocar o crachá de 30 dias numa URL deixaria um crachá de um mês inteiro no histórico do navegador do celular — a exposição que a auditoria de 02/09/2026 evitou em todo o resto do sistema.

  **Token à parte, de 2 minutos, bom só para UM anexo** (`emitirTokenAnexo`/`verificarTokenAnexo`, `backend/src/auth.js`; `conferirCrachaDeSessao` também saiu de lá — é o núcleo de `authRequired` sem escrever resposta, extraído para não duplicar a lógica). O app pede um pelo caminho normal (`GET /leads/:id/anexo/:messageId/token-baixar`, dentro de `/leads`, com o crachá de sempre) e só então navega para `GET /anexo-baixar/:leadId/:messageId?t=...`.

  **Por que essa segunda rota é OUTRO arquivo, fora de `/leads`.** A primeira tentativa foi deixar tudo dentro de `messages.routes.js`, com a rota de download registrada ANTES de `r.use(authRequired)` daquele arquivo — não funcionou: `server.js` protege o prefixo `/leads` INTEIRO com `cobrando` (que já chama `authRequired`) antes de qualquer rota dos routers montados ali rodar, então uma rota sem cabeçalho nunca tinha chance, não importa onde ficasse dentro daquele arquivo. Uma rota sem cabeçalho só pode morar fora de `/leads` — daí `routes/anexo-download.routes.js`, montado em `/anexo-baixar`, com a própria conferência (o token, nunca o crachá de sessão).

  Abre em **aba nova**, não na mesma — mesma régua já usada no checkout do Asaas (`window.open` com fallback para `location.href` se o navegador bloquear): se o token falhar ou o arquivo tiver sumido, o erro aparece numa aba descartável, sem tirar o corretor do atendimento. Teste: `npm run teste:baixar-anexo` (12 casos, servidor de pé — inclui o token só servindo o anexo para o qual foi emitido, token inventado recusado, e emitir token exige poder ver o lead).

- **Captação de imóveis só tinha venda** (21/09/2026, relatado pelo Ali: "acrescente a opção de casa para alugar dentro da sessão de captação, lá só tem casa pra vender"; coluna `produtos.finalidade`, `services`/`routes/produtos.routes.js`): o catálogo (`CREATE TABLE produtos`) nunca teve uma dimensão de venda-ou-aluguel — só `tipo` (casa/terreno). `finalidade` entra como `venda|aluguel`, **default `'venda'`** — toda linha que já existe é venda de verdade, então a migração não precisa tocar em nada além de declarar a coluna.

  **"Vendido" não existe para aluguel.** A situação terminal ganhou uma irmã, `alugado` — mesma cor, mesmo peso (fecha o negócio do mesmo jeito), rótulo diferente. `podeEditar` passou a testar contra um conjunto `FECHADO = {vendido, alugado}` em vez de comparar só com `"vendido"` — sem isso, um imóvel de aluguel marcado como alugado continuaria editável pelo corretor que captou, porque a trava antiga só reconhecia a palavra errada.

  **Um seletor a mais, no mesmo desenho do "Casa ou terreno"** — nasce em "Venda" porque é o que o catálogo sempre foi. O filtro do catálogo (`?finalidade=`) segue a mesma régua dos outros filtros (cidade, modalidade, status): sem escolher nada, os dois catálogos aparecem juntos, do jeito que sempre apareceram — quem nunca usar o filtro não vê diferença nenhuma. **Os rótulos de dinheiro mudam com a finalidade** ("Valor do imóvel" → "Valor do aluguel (mensal)", "Comissão da venda" → "Comissão do aluguel", com "/mês" no valor e na comissão) — chamar aluguel de "venda" na tela seria a mesma mentira de campo com nome errado que este projeto já corrigiu antes (comprar-ou-alugar do robô, 16/08/2026). No card do catálogo, só ALUGUEL ganha selo — venda é o padrão desde sempre, e marcar o padrão gastaria a linha mais visível do card para dizer o óbvio (mesma régua de "sem temperatura = sem pastilha").

  Teste: `npm run teste:produtos-aluguel` (7 casos, servidor de pé — cadastro dos dois tipos, filtro separando os catálogos, finalidade inválida recusada, "alugado" bloqueando edição de quem só captou, isolamento entre imobiliárias). Não existia suíte nenhuma para `produtos` antes disto — o módulo inteiro (CRUD, aprovação, mídia) ficou sem teste automático até hoje; cobrir o resto fica para outra vez, fora do escopo deste pedido.

- **"Esse erro continua aparecendo na hora de baixar arquivos"** (21/09/2026, relatado pelo Ali com um print do iPhone no mesmo dia do conserto de mobile acima — a tela mostrava `{"error":"Não consegui buscar o arquivo para baixar."}` crua, numa aba nova, exatamente a cara do `window.open()` que o conserto de mobile passou a usar). O conserto de mobile estava certo — abrir numa navegação de verdade, com token — mas a LEITURA por trás dele (`bytesDoArquivo`, a mesma usada pela rota de desktop desde 14/09) tinha um caminho sem rede de segurança nenhuma: se `chaveDaUrl` reconhecia a chave mas o `GetObject` no R2 (ou o `readFile` no disco) falhasse por QUALQUER motivo — token do R2 sem permissão de LEITURA (só escrita, que é o único caminho já exercitado de verdade até hoje: upload sempre funcionou, e ler de volta só acontecia no reenvio raro pro WhatsApp quando a URL pública falhava), `R2_PUBLIC_URL` trocado depois de o arquivo já ter sido salvo com o endereço antigo, ou o arquivo genuinamente sumido — a rota desistia na hora, com a mensagem genérica. Não dava para confirmar QUAL dessas era a causa em produção (sem acesso a ela), então o conserto não aposta numa só: cobre todas.

  **`bytesParaBaixar` (`services/storage.js`) substitui a leitura direta nas DUAS rotas de download** (a de desktop, `messages.routes.js`, e a de mobile por token, `anexo-download.routes.js` — mais um caso de "regra escrita duas vezes diverge", então ficou numa função só). Ela tenta a CHAVE primeiro (mesmo caminho de sempre, mais rápido e não depende do domínio público estar no ar) e, SE ISSO FALHAR POR QUALQUER MOTIVO, cai para a URL PÚBLICA do arquivo: o bucket do R2 é público (é para isso que `R2_PUBLIC_URL` existe), então mesmo sem permissão de leitura pela API o arquivo costuma abrir por HTTP comum — a mesma lógica de "serviço externo degrada, não derruba" que já vale para e-mail, push e IA. A falha registrada vai para `ultima_falha_download` em `/integracoes`, ao lado de `ultima_falha_download`/`falhaR2` que já existiam para upload — a próxima vez que isto acontecer, o motivo de verdade (não o genérico) fica ali, em vez de só sumir num 502. E o `catch` de cada rota ganhou `console.error` — ele NÃO logava nada antes, então mesmo essa falha "rara" não deixava rastro nenhum no Railway.

  **`mediaUrl` relativa (sem domínio) também vira erro de parse cru dentro de `fetch()` no servidor** — achado escrevendo o teste do fallback: uma URL como `/arquivos/conversas/xyz.pdf`, sem `APP_URL` na frente (linha muito antiga, salva antes de `APP_URL` estar configurado), faz `fetch()` no Node lançar "Failed to parse URL" em vez de um erro HTTP normal. Em produção isso não deveria acontecer nunca — sem `APP_URL` o envio de mídia pro WhatsApp já não funcionaria, então o valor sempre foi absoluto — mas a rede de segurança resolve contra o `APP_URL` do próprio servidor antes de tentar, de graça, para o caso raro de uma linha antiga ter ficado para trás.

  Teste: `npm run teste:baixar-anexo` (13 casos agora — o caso 13 força a leitura pela chave a falhar de propósito e confere que o arquivo chega mesmo assim, pelo fallback).

- **"Ainda abre uma tela em branco, não baixar direto no aparelho"** (21/09/2026, mesmo dia, mesmo relato — depois do conserto do 502 acima, o erro sumiu mas o sintoma original voltou). O conserto anterior tinha resolvido a metade certa do problema (a leitura do arquivo) e mantido o defeito de verdade: `window.open(alvo,"_blank","noopener")`.

  **O corretor usa o CRM instalado na Tela de Início do iPhone — um PWA em modo standalone, sem barra de abas.** `window.open` pedindo uma aba nova, dentro de um app standalone, faz o WebKit abrir uma janela SEM a moldura do Safari — sem lugar para o download aparecer, sem barra de endereço, nada. O que sobra na tela é uma janela em branco, com o arquivo baixado ou não sem nenhum jeito de saber — é exatamente a "tela em branco" do relato, e explica por que o conserto de 14/09 (que usava a mesma técnica) nunca tinha funcionado de verdade no aparelho instalado, só no Safari comum.

  **A troca é não abrir NENHUMA aba ou janela.** `BotaoBaixar` (`app.jsx`) cria um `<iframe>` escondido (`display:none`) apontando para a mesma URL com token — o navegador intercepta a resposta como download (por causa do `Content-Disposition: attachment`) antes de tentar desenhar qualquer coisa dentro do iframe, e isso funciona em computador, no Safari comum e dentro do app instalado, porque não depende de existir uma aba para o download acontecer. A tela do corretor nunca sai do atendimento — nem por um instante, nem numa janela em branco ao lado.

  **Continua pedindo o token de 2 minutos antes** (`GET /leads/:id/anexo/:messageId/token-baixar`) pelo mesmo motivo do conserto de 14/09: uma navegação — mesmo dentro de um iframe — não carrega o cabeçalho `Authorization`, e colocar o crachá de 30 dias na URL deixaria um crachá de um mês no histórico do navegador do celular.

  Confirmado com Playwright, navegador de verdade: login como corretor, abrir um lead com anexo, clicar em "Baixar" — **zero abas/janelas novas abertas** (`context.on("page")` nunca dispara), o iframe aparece com a URL certa, a requisição para `/anexo-baixar/...` volta 200 com `Content-Disposition: attachment`, e a página principal continua em `/app` o tempo todo, sem navegar para lugar nenhum.

- **"Testei no celular e ainda não baixou direto"** (21/09/2026, mesmo dia, QUARTA volta no mesmo relato — o iframe escondido tirou a tela em branco, mas o arquivo continuava não aparecendo salvo no aparelho). A raiz que faltava entender não era o VEÍCULO (aba, janela, iframe) — era o DESTINO: um app instalado na Tela de Início do iPhone roda num WKWebView em modo standalone, e **esse modo não tem gerenciador de download nenhum**. No Safari comum, um arquivo com `Content-Disposition: attachment` vira um aviso "Download concluído" com atalho para o Files; no app instalado essa barra, essa UI e esse atalho **não existem** — o download pode até acontecer nos bastidores do sistema, mas não tem para onde aparecer, e do lado de quem usa isso é indistinguível de nada ter acontecido. `window.open` e o `<iframe>` escondido dependiam exatamente do mesmo mecanismo que o modo standalone não oferece — por isso os dois "consertos" anteriores, tecnicamente corretos sobre a PARTE de navegação, não resolviam a parte que importava.

  **A saída certa para "salvar um arquivo" dentro de um PWA instalado é outra: a folha de compartilhamento nativa do sistema — a Web Share API, com arquivo** (`navigator.share({files:[...]})`). É a MESMA folha que abre ao compartilhar uma foto do WhatsApp, com "Salvar em Arquivos", "Salvar imagem" (para foto) e o resto dos apps instalados — e ela **sempre existe**, com ou sem moldura de navegador, porque não é parte da UI do navegador: é uma folha do sistema operacional, chamada pelo JavaScript da página.

  **O arquivo é buscado por `fetch`, não por navegação.** `BotaoBaixar` volta a buscar a rota de sempre (`GET /leads/:id/anexo/:messageId/baixar`, com o cabeçalho `Authorization` — a mesma origem do CRM, então CORS não atrapalha). É a mesma técnica que a versão de 14/09 tinha abandonado — mas aquela vez o problema era buscar a URL PÚBLICA do arquivo, que no R2 é outro domínio sem CORS liberado; buscando da NOSSA rota, que é sempre a mesma origem da página, esse problema nunca existiu. `fetch`+`blob` sempre foi seguro aqui — o que faltava era o destino certo para o blob no celular.

  **Três degraus, do melhor ao pior**: (1) `navigator.share` com arquivo, quando existe — funciona dentro do PWA instalado, é o caso que estava faltando; (2) blob local + `<a download>`, quando não existe `navigator.share` de arquivo (computador, Android mais antigo) — o clássico "baixar do navegador", que sempre funcionou fora do modo standalone; (3) a navegação por token de antes (`/anexo-baixar/...`), só se as duas de cima falharem — pior que as outras duas, melhor que um botão que não faz nada. **Cancelar a folha de compartilhamento não é falha**: `AbortError` sai sem cair no degrau de baixo, porque é a pessoa desistindo, não o download quebrando.

  Verificado com Playwright em dois cenários reais, não só lido no código: com `navigator.share`/`canShare` instrumentados (emulando um iPhone que tem a API, algo que o Chromium headless não tem de verdade) o clique chama `navigator.share` com o arquivo exato — mesmo nome, mesmo tipo, mesmos bytes — sem abrir nenhuma aba; e SEM essa instrumentação (o caso real do Chromium de mesa, que não implementa compartilhamento de arquivo) o app cai sozinho no degrau de baixo e o Chromium captura um download de verdade, byte a byte igual ao original. Os dois cenários passam sem abrir popup e sem a página sair de `/app`.

- **A construtora não sai mais no texto que vai pro cliente** (21/09/2026, pedido do Ali: *"na hora de enviar o imóvel para o cliente no CRM não envie automaticamente a construtora ou o construtor que foi cadastrado, que seja apenas uma informação interna da imobiliária"*; `textoDoProduto`, `messages.routes.js`). `produtos.construtor` sempre foi dado de CAPTAÇÃO — quem a imobiliária negociou o empreendimento —, útil para o corretor e o gestor identificarem o imóvel por dentro, mas `textoDoProduto` (a função que monta o texto do `POST /leads/:id/produto`, a rota que manda o produto pro WhatsApp) incluía o nome no texto do jeito que incluía metragem ou bairro: escrito para o cliente ler, dando a ele um caminho para pular a imobiliária e negociar direto com a construtora — o oposto do que a captação existe para proteger.

  **Uma linha removida, o campo continua existindo.** O comentário da função já dizia "comissão e captador NUNCA entram aqui" — o construtor tinha ficado de fora dessa regra por engano. O campo `construtor` continua no cadastro, na busca (`?q=`), no formulário de captação e na ficha interna do produto — só parou de entrar no texto que sai pelo WhatsApp.

  **`textoDoProduto` é a ÚNICA função que monta esse texto, para toda imobiliária, sem cópia por conta e sem configuração por org** — é por isso que corrigir aqui valeu IMEDIATAMENTE para toda conta existente e futura, sem tocar em nenhum lead, mensagem ou produto já cadastrado. É o mesmo raciocínio de sempre neste projeto ("regra escrita duas vezes diverge"), só que ao contrário: aqui a regra JÁ estava escrita um lugar só, e foi exatamente isso que fez o conserto valer para toda a base de uma vez, sem precisar tocar em dado nenhum — a identidade de cada lead e de cada imobiliária fica intocada, porque a mudança é só no CÓDIGO que gera o texto, nunca nos registros.

  Teste: `npm run teste:produto-sem-construtor` (confere que o nome da construtora não aparece no texto enviado nem como substring solta da palavra "construtor", que o resto da apresentação — título, local, cômodos, metragem, valor — continua saindo normalmente, e que o campo continua voltando pela API interna do produto, para a tela de captação não perder a informação).

- **"Os corretores estão automaticamente na etapa da SDR"** (22/09/2026, relatado pelo Ali com um print do celular mostrando, na ficha de um corretor, o seletor "Etapa do funil" cheio de etapas do SDR — Lead novo, Primeiro contato, Tentativa 2, Tentativa 3, Em qualificação — em vez das etapas do Comercial; `services/movimento.js` → `mudarParaOFunilDe`). A regra "o funil segue quem está com o lead" (01/09/2026) tinha uma condição boa demais: **só movia o funil quando quem RECEBIA o lead tinha um `pipeline_entrada` pessoal configurado.** Isso foi pensado para proteger o caso de alguém já ter posto um lead de propósito num funil especial — mas `pipeline_entrada` pessoal é uma configuração que praticamente **só a atendente usa** (é ela quem escolhe "meus leads nascem no SDR"); nenhum corretor visita essa tela para se configurar. Resultado: `entrada.proprio` dava `false` para quase todo corretor, e a trava pensada para um caso raro (proteger Locação) acabava bloqueando o caso comum (repasse manual do dia a dia, o que a SDR faz toda hora) — o lead saía da mão da atendente, ganhava um novo dono, e **ficava preso no funil de SDR para sempre**, porque ninguém jamais tinha "escolhido" tirá-lo de lá.

  **A automação da etapa "Lead qualificado" (que já chama `mover_para_pipeline` antes de trocar o dono) escapava dessa doença por acidente** — só o repasse manual pela ficha e pelo rodízio, que é o caminho que a SDR usa todos os dias, ficava preso.

  **O conserto distingue os dois casos que a trava original tratava como um só.** SDR não é um lugar onde um lead deveria DESCANSAR — é a sala de espera antes de um corretor; Locação (ou qualquer outro funil que o gestor tenha escolhido na mão via `MoverParaOutroFunil`, 17/09/2026) é uma escolha deliberada, e essa continua protegida. A regra ficou: sem funil próprio escolhido pelo destinatário, o lead só é EMPURRADO para fora do funil atual quando esse funil é do **tipo `sdr`** (`pipelines.type`) — nesse caso ele vai para o padrão da casa, que é exatamente o que `entradaDe` já devolve quando ninguém configurou nada. Qualquer outro tipo de funil (comercial, locação, recaptação, personalizado) continua intocado.

  **A checagem lê o `pipeline_id` FRESCO do banco, não o do objeto em memória.** Na automação de "Lead qualificado", o passo que move o pipeline já escreveu no banco antes deste código rodar, mas o objeto `lead` que chega até `trocarResponsavel` foi lido no início da chamada, antes de qualquer coisa acontecer — confiar nele faria a nova checagem "encontrar" um SDR que já não existe mais e mover o lead uma segunda vez, por cima da escolha explícita que o passo anterior tinha acabado de fazer.

  Teste: `npm run teste:funil-entrada` (13 casos agora — o caso 7 mudou para provar o comportamento certo: repasse do SDR para quem não tem funil próprio move para o padrão da casa; o caso 8 é novo e prova que a proteção original continua de pé para um funil especial de verdade, como Locação — repassar um lead posto lá de propósito para alguém sem funil próprio NÃO move nada).

- **"Marquei a mensagem no CRM e não apareceu no WhatsApp"** (22/09/2026, relatado pelo Ali). A citação de saída (09/08/2026) já manda o campo `replyid` — conferido contra a documentação pública da Uazapi e contra o código de outros projetos que integram a mesma API: é o nome certo, e o CRM já manda quatro apelidos junto por segurança. O bug não está no nome do campo.

  **Essa citação falha CALADA, por desenho da própria Uazapi**: ela responde 200 e ignora um `replyid` que não reconhece, sem erro nenhum — é a mesma família de defeito documentada várias vezes neste arquivo (o `app.use` sem caminho, a busca do corretor, o robô mudo). Sem acesso ao payload real desta conta em produção, trocar o nome do campo de novo seria repetir o mesmo erro de adivinhar sem dado — já documentado como armadilha na entrada de 17/09/2026 sobre citação recebida.

  **O diagnóstico já existia — só que só em `/integracoes`, JSON cru atrás de um Bearer token que o Ali não tem como colar numa aba do navegador.** `citacaoDiagnostico()`/`envioSemIdDiagnostico()` (`services/uazapi.js`) guardam a ÚLTIMA tentativa de citar (o que foi mandado, o que voltou) e o último envio que voltou sem nenhum id do WhatsApp reconhecido — e essa segunda causa é tão provável quanto a primeira: se o envio da mensagem original nunca recebeu `wa_id`, `reply_to` nunca vira `replyTo` na hora de enviar, e a citação nem chega a ser tentada (a mensagem sai normal, sem citar nada) — mesmo a mensagem aparecendo citada dentro do CRM, porque ali a citação é local e não depende do WhatsApp.

  **`GET /config/conexao` ganhou o campo `citacao`** (`diagnosticoCitacao()`), a mesma informação, traduzida para português simples, na tela que a gestão já abre todo dia (Configurações → Conexão) — sem exigir curl nem token colado na mão. `ConexaoConfig` (`app.jsx`) mostra um cartão logo abaixo do estado da conexão: verde/neutro sem tentativa ainda, âmbar quando a Uazapi aceitou (200) mas isso não é garantia nenhuma de que a citação apareceu, vermelho quando ela recusou de vez. Não muda o comportamento do envio — é só o que faltava para a PRÓXIMA tentativa virar evidência (o que a Uazapi respondeu de verdade) em vez de só "funciona/não funciona" sem detalhe nenhum para seguir a partir daí.

  **Investigado ao vivo com o Ali, direto na documentação interativa da uazapiGO**: o campo (`replyid`) e o formato do id batem exatamente com o que o CRM já manda, e um teste cru — sem o CRM no meio, direto no painel "Try It" da Uazapi — mostrou a Uazapi construindo o `contextInfo`/`quotedMessage` corretamente na resposta, mas a citação não apareceu no WhatsApp mesmo assim. Ou seja: não é bug do CRM. A pista que sobrou é que o teste foi feito mandando mensagem PARA O PRÓPRIO NÚMERO DA INSTÂNCIA (self-chat) — a resposta trouxe um campo `participant` com sufixo `@lid`, que normalmente só aparece em mensagem de GRUPO, sugerindo que "mandar para si mesmo" tem sincronização diferente no WhatsApp e pode estar interferindo na citação. Isso nunca aconteceria numa conversa real com cliente. Próximo passo, ainda em aberto: repetir o mesmo teste (dois envios crus, `replyid` apontando para o primeiro) mandando para um número DIFERENTE do da instância — se a citação aparecer lá, o problema é exclusivo de self-chat e o CRM está confirmado correto; se não aparecer nem assim, o caminho é abrir chamado com o suporte da Uazapi citando este teste exato. Não consigo reproduzir esse teste eu mesmo: o ambiente onde rodo tem o egress bloqueado para o domínio da Uazapi.

- **O master migra o TIPO de uma conta — imobiliária ⇄ autônomo** (22/09/2026, pedido do Ali: *"preciso que a conta master... consiga migrar alguém que criou a conta como imobiliária... transformar a conta dele em conta de corretor autônomo e vice versa, um cliente se cadastrou como imobiliária mas ele é corretor autônomo"*; `POST /orgs/:id/tipo`, `orgs.routes.js`, só master; cartão "Tipo da conta" na tela Equipe, visível só para quem é master). `tipo` não é um rótulo solto — ele decide o PAPEL de quem manda na casa (`ehDonoAutonomo`), o que aparece no menu (catraca e plantão somem no autônomo), as travas da porta de cadastro e o padrão do robô fora do expediente. Virar só a coluna sem acertar o resto deixaria a conta num estado que não é nem um nem outro — por isso a conversão é uma função só, que mexe nas duas pontas juntas, e não um PATCH solto no campo.

  **Imobiliária → autônomo exige escolher QUEM é o corretor titular** (`dono_user_id`, obrigatório — o master decide, não tem como adivinhar) **e trava se a equipe for grande demais para caber no plano**: a MESMA régua da porta de cadastro (`auth.routes.js`) — nenhum outro gestor, nenhum outro corretor, no máximo um atendente. Sem essa conferência a conta converteria com gente sobrando, e "o" corretor autônomo ficaria ambíguo — a recusa (409) lista exatamente o que sobra ("1 outro(s) gestor(es), 2 atendentes...") para o master saber o que resolver antes de tentar de novo. O titular escolhido vira `role='corretor'` e ganha `available=1` (mesma regra de sempre: numa casa de uma pessoa, esperar prontidão pra receber o próprio lead é fila de um que começa vazia) — o acesso de gestor não se perde, vem de ser o dono (`ehDonoAutonomo`), do mesmo jeito que já vale para quem nasce autônomo pela porta normal.

  **Autônomo → imobiliária devolve o dono ao papel de gestor** (`adm`) — sem isso ele ficaria preso no único papel que o autônomo usa, numa casa que agora tem catraca e espera um `adm` de verdade em dezenas de rotas.

  **Mensalidade não é tocada** (`valor_mensal`/`plano_id`/`trial_ate` ficam como estavam): é correção de categoria, não venda nova — o que já foi combinado com o cliente continua valendo, e o master ajusta preço à parte se precisar, pelo painel de mensalidade de sempre.

  **O crachá do titular é derrubado na hora** (`encerrarSessoes`) — nos dois sentidos da conversão. Ele estava numa tela pensada para o papel ANTIGO (menu, botões, rotas visíveis); deixar o crachá de 30 dias valendo faria a tela dele ficar num estado misto até o próximo login sozinho. Não é força bruta: como o `role` já é lido fresco do banco a cada chamada (auditoria de 02/09/2026), a troca de papel já valeria na próxima requisição de qualquer jeito — derrubar a sessão só torna isso imediato e explícito, em vez de deixar a pessoa logada numa tela que mistura menu antigo com permissão nova até ela mesma recarregar.

  **O cartão só aparece para quem é master** (`session.master`, o mesmo sinalizador que já esconde "Uso da IA"), dentro da tela Equipe — é lá que o master já está olhando a lista de gente para escolher quem é "o" titular, e não faz sentido nenhuma imobiliária mexer no próprio tipo sozinha. Depois de converter, a tela inteira **recarrega** de propósito: `org.tipo` e `role` vivem espalhados pelo app (menu, catraca, plantão, painel), e um reload garante que tudo lê o estado novo de uma vez — mais simples e mais seguro do que caçar cada pedacinho de estado que precisaria mudar junto.

  Teste: `npm run teste:converter-tipo` (11 casos, com o servidor de pé — a recusa por equipe grande, `dono_user_id` obrigatório e não vazando entre contas, só master, o papel indo e voltando nos dois sentidos, e o crachá do titular caindo nas duas conversões).

- **"As mensagens estão indo duplicadas para o lead do cliente"** (22/09/2026, relatado pelo Ali com print — dois balões de ÁUDIO idênticos, mesma hora, os dois "Enviada pelo WhatsApp"). Confirmado com ele antes de mexer em qualquer coisa: **o cliente recebeu uma vez só** — a duplicação era só na TELA do CRM. Isso muda tudo: se o cliente tivesse recebido duas vezes de verdade, o problema seria outro lugar (ou nenhum, se foi alguém reenviando); como não recebeu, o defeito é nosso, e dá pra corrigir sem arriscar esconder um duplo-envio real.

  **É uma CORRIDA (race condition), não repetição de ninguém — e o "Enviada pelo WhatsApp" foi a pista.** Esse rótulo só aparece quando a mensagem NÃO passou pelo CRM (`from_user_id` nulo — digitada direto no celular, ou eco do próprio WhatsApp via webhook), nunca quando sai pelo botão de gravar áudio do CRM. Ou seja: os dois balões vieram do MESMO caminho — o webhook da Uazapi — e a pergunta virou "por que o mesmo evento gerou duas linhas".

  **A checagem de eco em `processarMensagemRecebida` (mensageria.js) faz um SELECT por `wa_id` ANTES de inserir — e ali mora a fresta.** Mensagem de TEXTO não tem `await` nenhum entre o SELECT e o INSERT (roda tudo síncrono, no mesmo giro do event loop); mensagem de MÍDIA tem: o download do arquivo na Uazapi é uma chamada de rede de verdade, com tempo de verdade. Se a Uazapi reentrega o MESMO evento em sucessão rápida — coisa que provedores baseados em Baileys fazem ao resincronizar entre aparelhos —, o SEGUNDO webhook roda o PRÓPRIO SELECT enquanto o primeiro ainda está no meio do download, não encontra nada (o primeiro ainda não inseriu) e TAMBÉM insere. Dois SELECTs "ao mesmo tempo", os dois vazios — é exatamente por isso que só mensagem de áudio/foto duplicava e texto nunca duplicou, e por isso a checagem em JavaScript nunca ia fechar essa fresta sozinha: o problema não é a LÓGICA da checagem, é que ela olha o banco num instante e o outro processo escreve depois desse instante — clássico "time-of-check to time-of-use", e só o BANCO fecha isso de verdade, porque a escrita em si é atômica.

  **A trava definitiva é um índice ÚNICO em `wa_id`** (`db.js`), e `processarMensagemRecebida` passa a tratar a recusa do banco como **"perdi a corrida, é eco"** — não como erro (`try/catch` em volta do `INSERT`, reconhecendo `SQLITE_CONSTRAINT_UNIQUE`). Provado com o teste rodando as DUAS chamadas do webhook em paralelo de verdade (`Promise.all`, sem esperar uma terminar), contra um servidor de mídia de mentira com atraso de propósito: as duas tentativas baixam o arquivo (o log mostra os dois downloads acontecendo), e mesmo assim só UMA mensagem sobrevive no banco.

  **PARCIAL, em dois sentidos — e o segundo quase virou uma regressão pega só ao rodar a suíte inteira.** `WHERE wa_id IS NOT NULL` é o óbvio (mensagem sem `wa_id` é normal — antiga, ou envio que nunca recebeu id). O segundo, `AND direction = 'out'`, existe porque **mensagem RECEBIDA do cliente é outra regra, e já era testada de propósito** (`teste:whatsapp-oficial`, caso 8, de 03/09/2026): a Meta pode reentregar o mesmo `wamid` para uma mensagem que o cliente mandou de novo por conta própria, e as DUAS contam como atendimento de verdade — travar por `wa_id` ali apagaria conversa real, não eco. A primeira versão deste conserto tentou destravar por `wa_id` sozinho, sem olhar a direção, e quebrou esse teste na hora — a suíte pegou antes de qualquer deploy, que é exatamente para isso que ela existe. Único = só do lado em que "mesmo id duas vezes" SEMPRE significa eco (o que SAI), nunca conteúdo novo (o que ENTRA).

  **A LIMPEZA vem ANTES do índice, e só das linhas ENVIADAS.** Este defeito já tinha duplicado mensagem de verdade em produção (o caso do Vilmar) — criar o índice único sem limpar primeiro faria o `CREATE UNIQUE INDEX` falhar na hora, e o SERVIDOR NÃO SUBIRIA no próximo deploy. A migração conta grupos de `wa_id` repetido (só entre `direction='out'`, pela mesma razão do índice), mantém a linha mais ANTIGA de cada grupo — é a que o cliente viu primeiro, e a que uma citação já feita, se houver, aponta — e apaga as repetidas, com o total no log (nunca uma limpeza muda). Testado num banco "sujo" de propósito, num PROCESSO SEPARADO (é migração que roda no `import` de `db.js`, não dá pra simular "banco de antes" reimportando o módulo no mesmo processo): confirma que o servidor sobe sem travar e que sobra exatamente uma linha, a mais velha.

  Teste: `npm run teste:mensagem-duplicada` (migração seguro em processo separado + a corrida de verdade pelo webhook, com download concorrente).

- **O teste de 14 dias passou a exigir cartão de crédito** (22/09/2026, pedido do Ali, depois de ver o painel manual de mensalidade na própria conta: *"Essa seção não faz sentido estar aqui visto que tudo está sendo feito pelo asaas eu preciso que a comunicação agora seja que sim temos um teste de 14 dias mas precisa SIM cadastrar o cartão de crédito"*; `orgs.exige_cartao`/`cartao_confirmado_em`, `services/assinatura.js` → status `aguardando_cartao`, `services/asaas.js` → `cartaoRegistrado`). Até aqui o teste começava liberado — sem cartão, sem cobrança nenhuma — e a assinatura só existia se a pessoa escolhesse contratar depois. Ali confirmou por perguntas diretas: a exigência vale para as **duas contas de prateleira** (autônomo e imobiliária, as duas portas públicas do site), o painel manual de mensalidade deveria **sumir** para quem já está no Asaas, e o cartão é coletado **redirecionando para o checkout do Asaas logo após o cadastro** — nunca formulário próprio. Perguntei se dava para coletar o cartão aqui e cadastrar no Asaas via webhook depois: não dá sem certificação PCI-DSS nível SAQ-D (o Asaas não oferece tokenização do lado do cliente), e o Ali topou seguir com o checkout hospedado, que é o que já existia para pagamento.

  **`exige_cartao` marca a PORTA, não o tipo de conta** — autônomo e imobiliária de prateleira ganham a marca; conta que o Ali cria na mão pelo hub (`POST /orgs`, `POST /orgs/autonomos`) não, porque ali a venda já foi conversada e não tem por que ganhar um pedágio no meio de um convite que o próprio Ali está repassando. A marca precisou entrar em **duas portas**, não uma: `/publico/comecar` (02/09/2026, cobre os dois tipos) e `/auth/criar-imobiliaria` — a porta MAIS ANTIGA de imobiliária, que continua linkada (`comecar.html` manda "tem imobiliária com equipe?" para lá) e tinha ficado de fora até um teste bater nela. A trava vale pelo DESTINO (a coluna na org), não por onde a pessoa entrou — regra escrita numa porta só e esquecida na outra seria a mesma armadilha que este arquivo já documentou várias vezes.

  **`criarAssinatura`/`criarParcelado` passaram a forçar `billingType: "CREDIT_CARD"`** (`services/asaas.js`) — era `"UNDEFINED"`, que deixava a Asaas oferecer Pix, boleto ou cartão na fatura. Fixar o tipo também muda a cara da fatura hospedada: só aparece o formulário de cartão, porque é o único que serve para o que esta cobrança precisa fazer depois (cobrar sozinha, sem ninguém voltar à tela).

  **O teste não começa no cadastro, nem no `set-password` — começa quando o ASAAS CONFIRMA o cartão.** Antes disto, `set-password` já era quem iniciava o relógio (27/08/2026, para quem preenche o formulário às 23h não perder dias de graça); essa razão continua de pé, só que agora existe um passo a mais antes de haver "teste" para começar. `trial_ate` fica `null` até `cartao_confirmado_em` ser gravado — nunca antes disso, em conta nenhuma, por nenhuma porta.

  **Como saber se o cartão foi anexado, sem o Asaas cobrar nada ainda**, é a pergunta que não tinha uma resposta documentada com certeza (`cartaoRegistrado`, `services/asaas.js`): a assinatura nasce com vencimento no fim do teste, e a cobrança fica pendente até a data chegar — o que muda quando a pessoa termina de preencher o cartão na fatura é a cobrança pendente passar a trazer o objeto `creditCard`. Por não ser algo que dava para confirmar contra a documentação (o ambiente onde este código roda não alcança `docs.asaas.com`), a checagem **nunca é o único caminho**: o webhook tenta a cada evento da assinatura, **até os que o sistema sempre ignorou** (`interpretarEvento` passou a devolver o `assinatura` mesmo na `acao:"ignorar"`, senão a busca da org cairia sempre no consolo de "imobiliária única" e erraria a conta certa com mais de um cliente) — é a forma de não apostar num nome de evento específico para "cartão anexado". `GET /assinatura` também tenta, ao vivo, toda vez que a tela abre enquanto a conta ainda não confirmou. E o master sempre tem o "liberar sem cartão (master)" como saída manual — que funciona pelo mesmo caminho de sempre: registrar um pagamento faz `pagos` deixar de ser zero, e a checagem de cartão só bloqueia enquanto `!pagos`.

  **`aguardando_cartao` é a MESMA tela de bloqueio, com motivo diferente** (`Bloqueado`, `app.jsx`) — não uma tela nova. Ficaria estranho ter duas telas para "o sistema não deixa eu trabalhar". O seletor de planos dentro dela (`GerenciarAssinatura`) **deixou de ser só do autônomo**: a imobiliária de prateleira (Essencial/Plus, 02/09/2026) tem o mesmo direito de assinar de dentro do bloqueio, e o backend já liberava as duas — só a tela nunca tinha ligado os pontos. Quem tem preço negociado fora da tabela não perde nada: o seletor nem aparece.

  **O painel manual de mensalidade foi REMOVIDO DE VEZ, de TODA conta — e a primeira versão errou nisso.** A primeira leitura só escondia os controles manuais (Plano/Valor/Vencimento/Carência, "Salvar", "Registrar pagamento", corrigir/apagar/"Reorganizar") em contas que JÁ tinham cliente no Asaas (`asaas_ligado`, via `orgs.asaas_customer_id`), preservando-os para quem tem preço negociado fora da tabela — a leitura mais segura, mas não a que o Ali pediu. Ele testou de novo, mandou o mesmo print ("ainda tá aqui") de uma conta sem Asaas nenhum, e confirmado por pergunta direta: **remover de vez, para todas as contas**, mesmo as negociadas (`Rede` e afins) — que ficam **sem nenhum jeito de registrar pagamento por este painel**; se precisar de novo, é outra ferramenta, mais simples, a pedido.

  `PainelAssinatura` perdeu o card "Mensalidade do sistema" inteiro (título, selo de status, resumo de vencimento/plano/valor, histórico de pagamentos) e todo o estado que o sustentava (`f`, `pagos`, `lancar`, `editando`, `rascunho`, `confirmar`, `roda`). **O que sobrou não é "painel manual" — é o caminho de LIGAR a cobrança automática de verdade**, que é o próprio "tudo está sendo feito pelo Asaas": `GerenciarAssinatura` (prateleira do autônomo) para quem tem plano de prateleira, e o formulário de CPF/CNPJ para a imobiliária negociada ativar a própria assinatura no Asaas — o `&& false` morto que nunca escondia esse formulário depois de ativado (bug encontrado na primeira versão do conserto) foi corrigido junto, usando o mesmo `asaas_ligado`.

  **`asaas_customer_id`, não `asaas_subscription_id`** continua sendo o sinal certo para "já ativou": o plano ANUAL é parcelado (`/payments`), não assinatura, e nunca grava uma `asaas_subscription_id` — usar aquele campo deixaria quem escolheu o anual vendo o formulário de ativação de novo, exatamente o cliente que menos precisa dele.

  **As três portas públicas ganharam o aviso** (`comecar.html`, `criar-imobiliaria.html`): "sem cartão" virou "cartão necessário para começar", com a régua explícita de que nada é cobrado no cadastro — só depois dos 14 dias, automaticamente, a menos que a pessoa cancele antes. A tela de sucesso e os passos numerados pararam de dizer "o teste começa ao entrar" (não começa mais) e passaram a dizer "escolha um plano e cadastre o cartão — é isso que liga o teste".

  Teste: `npm run teste:cartao-obrigatorio` (15 casos, com o servidor de pé e um Asaas de mentira num servidor HTTP local, apontado por `ASAAS_API_URL` — inclusive a confirmação pelo WEBHOOK e pela checagem AO VIVO como dois caminhos independentes, o `billingType` sempre `CREDIT_CARD`, a rede de segurança do pagamento já registrado, e o plano anual provando que `asaas_ligado` não depende de `asaas_subscription_id`).

- **Reauditoria completa (23/09/2026, pedido do Ali: "faz uma reanálise completa do sistema... o sistema tá demorando a atualizar, as mensagens não estão chegando... vídeo não tá carregando na sessão de imóveis... reavalia a segurança e a criptografia do sistema").** Quatro investigações em paralelo — vídeo de imóvel, mensagens do WhatsApp, performance/dependências, segurança/criptografia. Duas viraram código nesta entrada; as outras duas (mensagens, performance) são diagnóstico sem bug de código confirmado, relatadas ao Ali para decidir o próximo passo.

  **Vídeo de imóvel nunca tinha recebido os três consertos que o vídeo de CONVERSA já tinha** (09/09, 14/09×2/2026): `POST /produtos/:id/midias` tratava vídeo do mesmo jeito que foto — base64 dentro do JSON, preso a `limiteBytes()` (30 MB, pensado para foto/áudio), sem conversão de HEVC. Vídeo de celular passa de 30 MB com frequência: o corretor escolhia o vídeo, o navegador lia e codificava o arquivo inteiro em base64 (minutos em 4G), e só no fim vinha o 413 — indistinguível de "carrega e não vai". Um vídeo HEVC (padrão do iPhone) nem chegava a ser recusado por tamanho: simplesmente não tinha conversão nenhuma, e o WhatsApp do cliente recusaria em silêncio na hora de enviar o imóvel.

  **`POST /produtos/:id/midias/video` é o MESMO desenho já provado na conversa** — não um desenho novo: `express.raw()` como middleware de ROTA (nunca `app.use()` sem caminho, a armadilha de 13/08/2026), corpo cru sem base64, limite de 150 MB, `garantirH264` convertendo antes de salvar. `podeReceberMidia`/`gravarMidia` (produtos.routes.js) saíram como funções compartilhadas entre a rota antiga (foto) e a nova (vídeo) — regra de permissão e de teto por tipo escrita num lugar só, a régua de sempre deste projeto.

  **O navegador ganhou o mesmo pré-checar de tamanho e a mesma barra de progresso** (`acoes.subirVideoProduto`, XHR por causa de `xhr.upload.onprogress`; `FormularioProduto.enviarArquivo` em `app.jsx`) — foto continua no caminho antigo (base64), só vídeo foi migrado. Sem o pré-checar, o corretor gastaria minutos de 4G num vídeo que a rota ia recusar de qualquer jeito.

  Teste: `npm run teste:produto-video` (8 casos, servidor de pé, com `ffmpeg-static` gerando vídeos H.264 e HEVC de verdade — o teste 2 lê o ARQUIVO DE VOLTA do disco com `ffprobe` para confirmar que o que ficou salvo é H.264 de verdade, não só que a rota respondeu 200).

  **Mensagens do WhatsApp não chegando: nenhum bug de código encontrado.** Reauditoria linha a linha do caminho inteiro (webhook da Uazapi → `mensageria.js` → gravação → aviso ao corretor) não achou regressão nem lacuna — toda a suíte relevante (`teste:webhook`, `teste:mensagem-duplicada`, `teste:canais-entrada`, `teste:citacao-recebida`) passa. A causa mais provável, sem acesso à produção para confirmar, é de configuração/conta: instância da Uazapi desconectada, token vencido, webhook apontando para uma URL antiga, ou um deploy que não pegou — os mesmos primeiros passos que `/integracoes` já existe para responder ("Parou de chegar lead" no topo deste arquivo). Fica para o Ali conferir `/integracoes` (`ultima_entrada`, `whatsapp.ok`) e `/integracoes/webhooks` quando o problema estiver acontecendo ao vivo — sem isso, não dá para diferenciar "o código tem um bug" de "a conexão da Uazapi caiu".

  **Performance: achado real, sem regressão.** A tela "Painel" (porta de entrada de todo papel desde 08/09/2026) faz ~100 consultas separadas ao banco para montar o gráfico de 14 dias (`services/painel-geral.js` → `serieDiaria`, 7 perguntas × 14 dias), e a tabela `ligacoes` não tem índice em `lead_id` — as duas coisas juntas custam mais conforme a base cresce, mesmo sem serem "bug". Fica registrado aqui para virar tarefa própria (reescrever `serieDiaria` para poucas consultas agregadas + índice em `ligacoes(lead_id)`), fora do escopo desta rodada porque não é o que o Ali relatou como quebrado — é o que pode estar deixando "tudo mais devagar" à medida que a base cresce.

  **Segurança: duas regressões reais, achadas e corrigidas na mesma rodada — as duas na CAMADA de segurança em si, não no resto do sistema.**

  (1) `GET /integracoes` (`ehMaster`, `diag.routes.js`) reimplementava `jwt.verify` com a PRÓPRIA cópia do `process.env.JWT_SECRET || "dev-secret"` — a mesma linha que a auditoria de 02/09/2026 já tinha descrito como "a pior linha do sistema" e removido de `auth.js`, só que nunca removida DAQUI. Como desde 03/09/2026 a ausência de `JWT_SECRET` deixou de derrubar o servidor (ele gera e guarda uma chave própria no banco — ver a nota acima, na seção de segurança), a chave real dos crachás não era mais "dev-secret" havia semanas; só esta função continuava tentando abrir com ela. Sem `JWT_SECRET` configurada, isso dava a qualquer pessoa que soubesse o id de um usuário master um jeito de forjar um crachá e ler os números comerciais que a auditoria de 02/09 tirou do público (nome/quantidade de imobiliárias, contagem de usuários/leads/mensagens) — confirmado ao vivo, não só lido no código. Corrigido usando a MESMA `conferirCrachaDeSessao` que toda rota autenticada já usa — regra escrita duas vezes diverge, e esta é mais uma vez o mesmo defeito.

  (2) Os diagnósticos de citação e edição de mensagem (`citacaoDiagnostico`/`edicaoDiagnostico`, 17-22/09/2026) guardam `resposta`, o CORPO BRUTO que a Uazapi devolveu para um envio real — e uma API de WhatsApp não-oficial costuma ecoar o telefone do destinatário nesse corpo. O comentário ao lado desse campo promete "nada do conteúdo da conversa", mas nunca foi aplicada a régua que o `envio_sem_id` (o diagnóstico irmão, também de 17/09) já segue certo — guardar só NOMES de campo, nunca valor. E os dois saíam por `/integracoes`, que é pública sem login de propósito. Corrigido escondendo `resposta` de quem não é master (`semRespostaCrua`, `diag.routes.js`) — o registro completo continua existindo para o master investigar de verdade, só parou de sair para a internet.

  As duas regressões compartilham a mesma raiz: lógica de segurança reimplementada FORA de `auth.js`/`services/uazapi.js` em vez de reusar o que já existia — o padrão "regra escrita duas vezes diverge" encontrado, desta vez, dentro da própria camada que existe para proteger contra ele. `npm run teste:seguranca` (23 casos) continua passando; nenhum teste cobria os dois achados específicos antes desta rodada — ficam sem suíte nova dedicada porque os dois são mudanças de UMA linha de lógica cada, verificadas ao vivo (curl com crachá forjado, antes/depois) em vez de justificar mais um arquivo de teste.

- **"Só consigo ajustar os números na setinha ao lado e eu quero digitar os números"** (23/09/2026, relatado pelo Ali sobre a tela Configurar metas do Painel; `ConfigurarMetasModal`/`LinhaMeta`, `app.jsx`). O campo era `<input type="number">`, então em teoria dava para digitar — mas confirmado com Playwright, o defeito real era outro: digitar "450" só gravava "4"; digitar "ab12xy3" não gravava nada.

  **A causa era um componente declarado DENTRO do corpo de outro componente.** `Linha` (o card de cada linha de meta) era um `const Linha=(...)=>...` escrito no MEIO da função `ConfigurarMetasModal`, não um componente à parte. Cada tecla digitada chama `setForm`, que faz `ConfigurarMetasModal` renderizar de novo — e recriar `Linha` como uma função NOVA a cada renderização. O React não reconhece isso como "o mesmo componente com um valor diferente": vê uma identidade de componente diferente, desmonta a árvore antiga (o `<input>` incluído) e monta outra no lugar, perdendo o foco do campo a cada tecla. A primeira tecla já disparava o ciclo; a segunda não tinha mais onde cair. Um clique na setinha nunca expunha isso — é uma ação só, sem foco sustentado —, e foi exatamente por isso que "só a setinha funciona" era a cara exata do defeito.

  **O conserto é tirar `LinhaMeta` do corpo da função pai** — agora é um componente de verdade, declarado uma vez só, fora de `ConfigurarMetasModal`, recebendo `valores`/`status`/`onCampo`/`onSalvar` por props em vez de fechar sobre o estado do componente pai. Com identidade estável entre renderizações, o React atualiza o `<input>` existente em vez de trocar de árvore, e o foco sobrevive a toda tecla. É a mesma regra que este arquivo já documentou para o `MoverFunil` duplicado (17/09/2026) — componente aninhado ou reaproveitado sem cuidado quebra em silêncio, sem erro de build nenhum.

  **Junto, o campo saiu de `type="number"` para texto com teclado numérico** (`inputMode="numeric"`) e uma peneira que descarta tudo que não é dígito, a mesma régua do `soNumeros` do telefone do lead — sem isso, mesmo com o foco corrigido, o campo de 58-64px continuaria reservando espaço para as setinhas do navegador (pouco sobrando para o texto) e mudando de valor sozinho se a roda do mouse passasse por cima focado. O campo de VGV, que lida com valores de seis dígitos, ganhou largura própria (100-112px) — os demais (ligações, contatos, visitas, propostas) continuam mais estreitos, do tamanho que já bastava.

  Confirmado com Playwright, digitando de verdade (não só lendo o valor da prop): "450" grava "450"; "ab12xy3" grava "123" (letras descartadas, dígitos preservados); o campo de VGV aceita "400000" inteiro; e o botão Salvar de cada linha continua funcionando depois da digitação. Não há teste automático de servidor cobrindo isto — é comportamento só do navegador (identidade de componente React, foco de campo), e a suíte de backend (`npm run teste:painel-geral`) já prova que a rota `POST /painel/metas` em si nunca teve bug nenhum — o defeito inteiro morava na tela.

- **"O Alberto parou de receber mensagens dos leads pelo CRM"** (24/09/2026, relatado pelo Ali: Alberto é um CLIENTE do ConHub — corretor autônomo que tinha se cadastrado por engano como imobiliária, e o Ali converteu o tipo da conta dele pelo hub; as mensagens pararam logo depois). Não era o webhook, não era a Uazapi, não era catraca nem permissão de banco — o bug morava na TELA, num `&&` que faltava desde 02/09/2026.

  **`role==="corretor"` e supervisionar a própria conta são, para TODO corretor autônomo, a MESMA coisa ao mesmo tempo** — e esse é o único caso do sistema em que isso acontece. Um corretor comum nunca supervisiona; um gestor (`adm`) ou atendente (`sdr`) nunca tem `role==="corretor"`. Mas o dono de uma conta autônoma é as duas coisas juntas: `role` é `corretor` (é o que a catraca, o rodízio e o score precisam enxergar, regra de 02/09/2026) e `supervisiona()` também é verdadeiro para ele (`ehDonoAutonomo`, `auth.js`).

  **A tela "Atender" (`app.jsx`, componente `ConCRM`) decide qual das duas telas mostrar com duas condições que deveriam ser mutuamente exclusivas e não eram**: `role==="corretor"&&view==="atendimento"` desenhava `<Atendimento>` (a caixa simples, `myLeads = leads.filter(l=>l.assignedTo===session.id)`, SEM nenhuma exceção para quem supervisiona) e, ao MESMO TEMPO, `supervisor&&(view==="conversas"||view==="atendimento")` desenhava `<Conversas>` (a tela certa — busca TODOS os leads da conta, inclusive sem dono) — as duas no mesmo lugar da tela, ao mesmo tempo, para o dono autônomo.

  **Numa conta autônoma NASCIDA autônoma isso quase não dói**, e foi por isso que ninguém tinha notado em três semanas: todo lead novo já entra direto na mão do dono (`services/catraca.js` → `proximoAtendente`, regra de 02/09/2026), então a caixa simples (`myLeads`) nunca fica de fato vazia — só duplicada, o que passa despercebido. **A conta do Alberto era diferente**: tinha vindo de uma imobiliária de uma pessoa só, SEM atendente — e `proximoAtendente` para `tipo==='imobiliaria'` sem SDR nenhum devolve `null` (lead fica "na fila", sem dono; é a regra certa para uma imobiliária de verdade, onde sobrar lead sem dono é melhor que chutar em qual conta ele cai). Alberto via esses leads normalmente enquanto era `role==='adm'` (a tela dele era sempre `<Conversas>`, sem concorrência nenhuma). No instante em que o Ali converteu o tipo da conta, o papel dele virou `corretor` — CERTO, é a regra do recurso — e a caixa errada (que não enxerga `assigned_to` nulo) passou a competir pela tela com a certa. Do lado de quem usa isso não parece "duas telas brigando" — parece "as mensagens pararam".

  **O conserto é uma condição a mais, não uma tela nova**: `role==="corretor"&&!supervisor&&view==="atendimento"` — a caixa simples só aparece para quem é SÓ corretor. Quem supervisiona, seja gestor, atendente ou dono de conta autônoma, sempre cai em `<Conversas>`, exatamente como o comentário que já estava escrito ali em cima dizia que deveria ser ("quem supervisiona usa a tela completa... é a mesma aba") — a intenção sempre esteve certa, só a condição não a seguia à risca.

  Confirmado com Playwright reproduzindo o caminho INTEIRO do Alberto: uma org `imobiliaria` de uma pessoa só, sem SDR, com 3 leads sem dono (o mesmo estado que uma conta assim acumula na vida real); o master converte para `autonomo` pela rota de verdade (`POST /orgs/:id/tipo`); o titular loga de novo (o crachá antigo morre na conversão, regra de 22/09/2026) — e os 3 leads aparecem na hora, na tela de Atender, com a marca "na fila". Sem a correção, essa mesma sequência mostra zero. `npm run teste:autonomo` e `npm run teste:converter-tipo` continuam passando — o bug nunca esteve no backend, então nenhum teste de servidor podia pegá-lo.

- **"Cliquei em Desconectar e simplesmente não vai"** (24/09/2026, mesmo dia, relatado pelo Ali sobre a MESMA conta do Alberto — depois do conserto da tela acima, ele entrou de novo pelo hub para reconectar o WhatsApp e o botão Desconectar não fazia nada, mesmo com "DESCONECTAR" escrito certo na confirmação). A pista já estava no relato anterior: minutos antes, um envio de teste tinha voltado da Uazapi com o erro literal `"WhatsApp disconnected: session is not reconnectable"` — a sessão daquele número estava travada por dentro, do lado do próprio provedor.

  **NENHUMA chamada à Uazapi tinha teto de tempo** (`services/uazapi.js` → `call()`, usada por todo envio, e `instanceStatus()`) — o `fetch` nunca levava `signal`. Enquanto a Uazapi responde rápido (mesmo para dizer "não dá", como no envio que revelou a sessão travada), isso nunca doeu. Mas pedir para uma sessão NESSE ESTADO se desconectar é justamente o caso em que a API remota está mais provável de estar travada por dentro — e sem teto, o `fetch` fica pendurado esperando uma resposta que talvez nunca chegue. Do lado da tela isso é indistinguível de "não fez nada": o botão mostra "Desconectando…" e não volta — sem erro, sem sucesso, para sempre.

  **Provado, não só lido no código**: um servidor de mentira no lugar da Uazapi, programado para NUNCA responder ao pedido de desconexão, deixava a requisição de verdade pendurada por mais de 15 segundos sem sinal nenhum de vida — o teste precisou abortar ele mesmo, porque o servidor não tinha limite próprio nenhum.

  **O conserto é a mesma régua que `services/video.js` já usa para o `ffmpeg`** ("melhor um erro claro do que uma espera sem fim"): as duas chamadas de `services/uazapi.js` ganharam `signal: AbortSignal.timeout(20000)`. Passado o teto, o erro que chega à tela diz que a instância não respondeu e pode estar travada do lado da Uazapi, e sugere tentar de novo em alguns minutos ou conferir pelo painel da Uazapi — a mesma saída que a função já oferecia para quando nenhum dos três endereços de desconexão existe. **O caminho rápido (Uazapi responde na hora, mesmo com erro) continua exatamente igual** — o teto só entra quando ninguém do outro lado responde.

  Teste: `npm run teste:desconectar-timeout` (sobe o servidor e uma Uazapi de mentira com dois comportamentos — uma rota que erra na hora, para provar que o caminho comum não regrediu, e uma que nunca responde, para provar que o pedido volta dentro de um teto em vez de ficar pendurado pra sempre).

  **O timeout sozinho não resolvia — e o motivo de verdade eram mais dois defeitos** (24/09/2026, mesmo dia: *"eu clico em desconectar e não desconecta, não aparece o QR code"*). (1) **O selo "WhatsApp conectado" mentia**: a tela pintava verde quando `w.ok` — e `ok` só dizia que a Uazapi RESPONDEU, não o que ela respondeu. Uma instância desconectada respondia "disconnected" com 200, e a tela continuava dizendo "conectado"; depois do Desconectar, o clique funcionava e a tela desmentia. Agora `instanceStatus` devolve `conectado` (lido de `status.connected`/`loggedIn` ou do texto do status), e a tela só fica verde com ele. (2) **O CRM não tinha QR Code nenhum** — parear só no painel da Uazapi. Quem desconectava por aqui ficava sem caminho de volta. Agora `POST /config/conexao/conectar` (só gestor) chama `/instance/connect` e a tela mostra o QR, atualiza a imagem a cada 3s (a Uazapi troca o código) e vira "conectado" sozinha quando o celular lê. **Desconectar já abre o QR em seguida**, porque o próximo passo é sempre parear de novo. **E a sessão travada ganhou saída**: quando o Desconectar falha (o caso "session is not reconnectable" do Alberto), aparece "Forçar reconexão com novo QR Code", que tenta derrubar o que sobrou e pede o QR **ignorando a falha da derrubada** — numa sessão já quebrada, desconectar pode falhar justamente porque não há o que desconectar, e isso não pode bloquear a única saída. Junto: o diagnóstico `envio_sem_id` passou a olhar só `/send/*` — desconectar e conectar não devolvem id de mensagem e sujavam o diagnóstico da citação. Teste: `npm run teste:conexao-qrcode` (8 casos, com uma Uazapi de mentira com estado); conferido também no navegador, clicando como o gestor clica.

  **O selo do TOPO tinha o mesmo defeito, num segundo lugar** (24/09/2026, o Ali mandou o print: cartão "desconectado", topo "WhatsApp conectado"). Ele lia `ok` de `/integracoes` — a mesma mentira — e só era buscado ao entrar no sistema, então nada que acontecesse na tela de Conexão chegava até ele. Agora existe **uma regra só**, `whatsConectado(w)` em `app.jsx`, usada no topo e no cartão; e `acoes.conexao()` atualiza o selo do topo a cada leitura, inclusive a de 3 em 3s do QR. Junto: **o prazo de 3 minutos do QR saiu** — ele dizia "expirou" enquanto a Uazapi ainda mostrava "conectando… leia o QR Code". Quem decide que venceu é ela (volta a `disconnected` sem código); e **abrir a tela com um código já ativo mostra o QR direto**, sem pedir clique.

- **Portais de imóveis: o anúncio sai, o lead entra** (24/09/2026, pedido do Ali — "preciso entregar isso com urgência"; `services/portais.js`, `routes/portais.routes.js`, tabelas `portais_config`/`portais_leads`, colunas novas em `produtos`; botão **Portais** em Imóveis, só gestor). Vale para toda conta do ConHub, sem configuração nossa: cada imobiliária cola os próprios endereços no painel de cada portal.

  **Dois caminhos, nos dois sentidos.** (1) **Anúncio sai por FEED XML** — o portal busca o arquivo algumas vezes por dia; não existe "enviar para o portal". São dois formatos porque os portais não concordam: **VRSync** (`/feeds/:token/zap.xml`, Grupo OLX — ZAP, VivaReal e OLX) e o formato próprio do **Chaves na Mão** (`/feeds/:token/chavesnamao.xml`, as 53 tags em minúsculo, todas presentes mesmo vazias, só JPG, vídeo só do YouTube). (2) **Lead entra por webhook** (`POST /webhooks/portais/:token`, JSON ou formulário, `?portal=` para quem não diz de onde veio) e segue a MESMA regra do WhatsApp: atendente da vez pela catraca, funil de quem recebe, push para o dono. O imóvel e a mensagem do cliente viram uma **observação** — é a faixa âmbar que quem atende lê antes de falar.

  **DOIS tokens, e a separação é a trava.** O do feed é lido por qualquer um que tenha o endereço (o portal, o integrador, quem viu o painel); o dos leads ESCREVE no CRM. Com um token só, o endereço do feed vazado criaria lead falso. Cada um se troca sozinho na tela ("gerar endereço novo") sem derrubar o outro. Endereço errado responde **404, nunca 401** — não conta que existe um certo.

  **Só vai ao feed o que está PRONTO** (`pendencias()`): título, descrição de 50+ letras, valor, UF, cidade, bairro, uma foto e, para casa, área construída/quartos/banheiros. Anúncio incompleto o portal recusa ou publica mal, e a imobiliária só descobriria olhando o portal. Aqui ele fica fora, e a tela de Portais **começa pela lista do que falta em cada um** — "colei o endereço e não aparece nada" quase sempre é isso, e sem a lista a culpa iria para a integração. A regra é UMA: a mesma função monta o feed, a lista da tela e o aviso no formulário do imóvel (`produto.portal`) — cópia no navegador divergiria.

  **Publicar é decisão da gestão.** O corretor preenche tudo e vê o que falta, mas o `publicar_portais` só muda por quem supervisiona (o servidor ignora o que o corretor mandar). **A descrição do anúncio é um campo À PARTE das Observações**: aquelas são internas (chave com quem, condição com o proprietário), e reaproveitá-las publicaria na internet o que foi escrito para a equipe. **Endereço mostrado é escolha** (só bairro / rua / completo, padrão só bairro): endereço completo no anúncio leva o cliente direto ao proprietário.

  **Lead repetido não duplica** — nem reenvio do mesmo lead pelo portal (`portais_leads`, idempotente por `originLeadId`), nem a mesma pessoa pedindo contato em dois imóveis (casa pelo telefone/e-mail; o segundo interesse vira outra observação no lead que já existe).

  **O que NÃO está confirmado, e a tela diz:** o formato do **Imovelweb**. A documentação não estava alcançável daqui; a tela manda enviar o endereço VRSync ao suporte deles e perguntar se serve. Se pedirem outro formato, é um adaptador novo em `services/portais.js`. Os caminhos de menu dentro de cada portal também mudam sem aviso — por isso cada passo termina em "não achou? mande o endereço ao suporte".

  Teste: `npm run teste:portais` (18 casos, com o servidor de pé — incompleto fora do feed, aluguel com `RentalPrice`/`L`, PNG fora só do Chaves na Mão, corretor não publica, token do feed não cria lead, reenvio não duplica, isolamento entre imobiliárias).

- **O site da imobiliária: portal próprio e uma página por imóvel** (24/09/2026, pedido do Ali — *"capricha pra não ficar função demais e informação demais"*; `services/site.js`, `routes/site.routes.js`, tabela `sites`; botão **Site** em Imóveis, só gestor; card "Página no site" na ficha do imóvel, para todos). Cada imobiliária cliente ganha `/imoveis/<slug>` — a vitrine, com filtros (comprar/alugar, tipo, cidade, quartos, valor) — e `/imoveis/<slug>/<id>/<titulo>` para cada imóvel, na marca DELA (logo e cor da Identidade). O botão de contato vai SEMPRE para o WhatsApp da imobiliária, com a mensagem pronta citando o imóvel e o código; a conversa entra no CRM pelo caminho de sempre, pela catraca.

  **As páginas são montadas no SERVIDOR, não no navegador**: é o que faz o link colado no WhatsApp mostrar a prévia com foto e título. O WhatsApp lê o HTML que chega e não roda JavaScript — página montada no navegador chegaria vazia para ele, e o link iria sem foto justamente onde o corretor mais vai usá-lo.

  **O que o visitante vê é uma LISTA BRANCA** (`publico()`), nunca a linha do banco. O cadastro guarda coisa que é só da casa — construtora, comissão, captador, observações ("dono aceita 10% abaixo"). Montar a página da linha inteira seria confiar que ninguém nunca vai escrever `p.observacoes` num template; montar de um objeto que não TEM esse campo é a garantia. A descrição é a **do anúncio** (a mesma dos portais), nunca as observações. **O endereço segue "O que mostrar do endereço"**, e **o site não tem mapa** (decisão do Ali, no mesmo dia): a localização exata é o corretor quem manda, na conversa, quando achar que é a hora — mapa na página entregaria ao visitante, e a quem quer pular a imobiliária, o que a conversa existe para conduzir. O teste 11 procura cada um desses campos no HTML.

  **Nasce DESLIGADO**, pela régua do `publicar_portais`: o catálogo inteiro não vai para a internet porque uma versão subiu. Ligado, mostra **todo imóvel disponível** (`status='ativo'`) — sem marcação por imóvel, de propósito (menos função); para tirar um do site, tira-se do catálogo. **Aguardando aprovação e recusado não existem para o visitante** (404); **vendido, alugado e inativo** respondem "não está mais disponível" com o botão do WhatsApp (410, `noindex`), porque esse link pode estar num anúncio pago rodando agora. **Título trocado redireciona** (301) para o endereço novo. **Conta travada por falta de pagamento** não mantém vitrine grátis: vira "voltamos em breve" com o WhatsApp — não some com um 404, pelo mesmo motivo do anúncio pago.

  **Ligar exige o WhatsApp** (vitrine sem porta), e o endereço (`slug`) é **único na plataforma inteira** — duas imobiliárias no mesmo endereço seriam um site mostrando o catálogo da outra. Trocar o endereço com o site no ar mostra o aviso de que os links já enviados param de funcionar.

  **Pixel do Facebook, opcional**: PageView em toda página, ViewContent na página do imóvel (com código e valor) e **Lead no clique do WhatsApp** — que é a conversão que quem roda tráfego quer otimizar. O ID é conferido como **só dígitos** antes de entrar na página, então não há como ele carregar código para dentro do site. Com o Pixel ligado, o rodapé avisa que o site usa cookies do Facebook.

  **No computador a galeria é um MOSAICO** (24/09/2026, pedido do Ali depois de ver no ar): uma foto grande e até quatro menores ao lado, com "Ver as N fotos", numa altura de até 400px — a foto única de 600px ocupava a tela e empurrava preço e WhatsApp para baixo da dobra. Com uma foto só ela aparece inteira (sem corte), porque foto de celular em pé numa faixa larga mostraria só o meio do cômodo. No celular continua o carrossel. **Galeria do celular**: altura fixa e não `aspect-ratio` + `max-height` — a combinação transfere o teto de altura para a LARGURA e deixava uma faixa preta ao lado da foto no computador; achado na conferência no navegador. Toque na foto abre em tela cheia (Esc fecha, setas passam). Filtro de valor só aparece depois de escolher comprar ou alugar: "até R$ 2.000" num catálogo misturado cortaria todas as casas à venda sem ninguém entender. Teste: `npm run teste:site` (18 casos, com o servidor de pé — inclusive título com `<script>` saindo escapado e imóvel de outra imobiliária não abrindo no site desta).

## Core de gestão: pipelines, etapas, SLA e painel (28/08/2026)

O ConHub deixou de ser um CRM com um funil e passou a ser uma plataforma onde cada empresa monta a própria operação. O funil era uma lista de 11 nomes em `services/stages.js`, igual para todo cliente — servia enquanto o produto era o CRM de uma casa. Locação, lançamento e recaptação não têm as mesmas etapas, e nenhuma delas deveria precisar de mudança de código para existir.

**`leads.stage` (o nome) continua existindo e continua sendo escrito**, agora ao lado de `stage_id`. Os dois, sempre juntos, por `moverEtapa`. É duplicação de propósito: cerca de 40 pontos de leitura dependem do nome, e trocá-los de uma vez seria reescrever o sistema num commit com a operação rodando em cima. O preço é renomear etapa ter que atualizar os leads dela — e isso se paga num lugar só (`editarEtapa`).

**A migração é invisível.** No start, cada imobiliária ganha o pipeline padrão com exatamente as etapas que já usava, e cada lead é ligado pelo nome. Lead com etapa fora da lista fica com pipeline e sem `stage_id`: continua aparecendo pelo nome, em vez de ser descartado. Idempotente — reiniciar o servidor dez vezes faz o trabalho uma.

**Apagar e desativar são coisas diferentes.** Etapa com lead dentro não some (os leads ficariam órfãos, fora do funil e do relatório, sem ninguém perceber); ela se desativa. Apagar de vez só quando está vazia. Vale igual para pipeline.

**`moverLead` (`services/movimento.js`) é onde as regras moram**, e não nas rotas. Lead muda de etapa por cinco caminhos — rota manual, arrasto no kanban, confirmação da IA, venda registrada, reanálise em lote. Regra na rota valeria para um e não para os outros quatro, e regra que vale às vezes é pior que regra nenhuma. A ordem é: conferir campos, mover, rodar automação. `forcar` existe para o fato consumado (a venda aconteceu; segurá-la por um campo seria o CRM discordando do mundo).

**`automation_config`** entende `distribuir` (rodízio ou pessoa fixa), `mover_para_pipeline` e `limpar_responsavel`. É o caso central: o lead chega em "qualificado" no SDR e vai sozinho para o comercial, com dono. **Ninguém disponível vira aviso explícito** — lead sem dono parecendo distribuído é lead que ninguém atende. A automação nunca lança: configuração errada não pode virar etapa em que ninguém entra.

**SLA conta da ÚLTIMA INTERAÇÃO, não da entrada na etapa.** Lead que entrou ontem e conversou agora está saudável; lead que entrou hoje e ninguém tocou não está. Etapa sem `sla_minutes` devolve `null`, e **null não é "em dia"** — é "ninguém mandou medir". O painel mostra quantos leads estão fora de qualquer medição: sem isso, "3 vencidos" parece ótimo numa base em que 300 não têm prazo.

**`last_interaction_at` é mantido por GATILHO no banco**, não pelas rotas. Seriam seis lugares hoje e o sétimo que alguém escrever depois — e o esquecido não dá erro: só faz o lead parecer abandonado enquanto a conversa acontece. `MAX` impede que mensagem atrasada faça a data retroceder. Gatilho é código invisível; vale a troca porque a alternativa é um campo errado em silêncio.

**Funil de conversão x avanço operacional** (`services/painel.js`): o primeiro usa só as etapas com `counts_as_conversion`; o segundo mostra todas, com tempo mediano e atrasados. Etapa administrativa contada como conversão faz o relatório dizer que a operação converteu quando ela só juntou papel. **A taxa sequencial é uma INTERSEÇÃO** (dos que chegaram no degrau anterior, quantos chegaram neste) — dividir contagens independentes produzia "seq 300%", e um número impossível faz o gestor parar de confiar na tela inteira. Quem chegou sem passar pelo degrau anterior aparece como `+N` em âmbar.

**Os dados de campanha estavam se perdendo.** O webhook da Meta guardava só o `meta_lead_id`: a Graph API não manda campanha, conjunto, anúncio e formulário sem serem pedidos pelo nome. É dado que não volta — lead de ontem perdeu a atribuição para sempre. Por isso entrou junto da fundação, antes de qualquer tela que fosse usá-lo. O painel de campanhas informa a **cobertura** ("4 de 300 leads têm campanha"), senão quatro linhas parecem a operação inteira.

**O seletor de funil no Kanban aparece SEMPRE**, mesmo com um só — e criar outro sai dali. A primeira versão o escondia quando havia apenas um, com o raciocínio de que um seletor de uma opção não faz nada. O raciocínio estava errado: toda conta começa com um funil, então o seletor nunca aparecia para ninguém, e a possibilidade de ter vários não existia aos olhos de quem usa. Recurso sem porta na tela onde ele importa é recurso que não existe. O ajuste fino (SLA, campos, ordem) continua em Configurações — é trabalho de sentar, não de um clique no meio do atendimento. E o quadro passou a mostrar **só os leads do funil escolhido**: sem isso, escolher um funil novo dava colunas vazias com "60 lead(s)" no contador ao lado, duas afirmações contrárias na mesma barra.

**Mover os leads de uma pessoa para outro funil** (`services/lote.js` → `previaMoverFunil`/`moverParaFunil`, Base de leads → Arrumar a base, só ADM): é a operação que faz os múltiplos funis existirem de verdade — criar o funil do SDR não muda nada enquanto os leads que deveriam estar nele continuam no comercial, e mover trezentos um a um não é opção que se ofereça a alguém. **A prévia diz em que ETAPA eles caem**, e isso não é detalhe: os funis têm etapas diferentes por natureza, quase nada casa por nome, e o padrão é a primeira etapa do destino — mover a base para "Lead novo" sem avisar zeraria um funil que alguém passou meses construindo. **O responsável não muda**: mover de funil é trocar o fluxo, não tirar o lead da pessoa. Usa `moverEtapa` lead a lead, e não um UPDATE em massa, porque é ele que grava o histórico e a transferência — "por onde este lead passou" é justamente a pergunta que trocar de funil cria.

Testes: `npm run teste:pipelines` (55 casos).

## Segurança e LGPD — a auditoria de 02/09/2026

Pedido do Ali: *"encontre vulnerabilidades do sistema, e espaços que facilitem o acesso a informação interna de outras contas, e corrija todos, criptografe tudo e aumente a segurança seguindo a LGPD"*. O que segue é o que foi encontrado, o que foi corrigido e o que **continua aberto de propósito**.

**A regra que liga todas elas:** nenhuma dessas falhas dava erro. O CRM subia, a tela abria, os leads entravam — e a porta estava destrancada. É a mesma família de defeito que este arquivo já documentou três vezes (o `app.use` sem caminho, a busca do corretor, o robô mudo), e é por isso que agora existe `npm run teste:seguranca`: **23 casos que sobem o servidor inteiro** e conferem cada trava de fora, porque trava conferida por dentro do serviço não prova nada sobre a rota.

### As três que deixavam entrar ou escrever na conta alheia

**1. `JWT_SECRET` caía num valor escrito no código** (`auth.js`). Era `process.env.JWT_SECRET || "dev-secret"`. O `||` parece cuidado e era a pior linha do sistema: sem a variável, o segredo que assina os crachás virava uma palavra pública, e **qualquer pessoa fabricava um crachá de master** e lia todas as imobiliárias. A trava mudou de versão duas vezes depois disto — a versão 2 (derrubar o servidor sem a variável) chegou a tirar o CRM do ar duas vezes por uma chave só CURTA, não ausente; a versão 3 (03/09/2026), que vale hoje, **gera uma chave aleatória própria e a guarda no banco** quando a variável não existe, em vez de recusar subir — nada de errado com o sistema derruba a operação de uma imobiliária por uma variável esquecida no painel da hospedagem. O raciocínio completo das três versões está em `auth.js`, no bloco de comentário acima de `SECRET`.

**2. O webhook do WhatsApp aceitava o NÚMERO como identificação** (`canalDoWhatsapp`). O número de uma imobiliária é **público** — site, anúncio, fachada. Com ele, qualquer um mandava um POST e criava lead falso, escrevia na conversa de um cliente real e, com o atendimento automático ligado, **fazia a IA da casa mandar WhatsApp para um número escolhido por ele**. E um token ERRADO caía no mesmo caminho: a conferência não recusava, só "não achava". Agora o token é o único caminho; o número vira saída de emergência atrás de `UAZAPI_ACEITAR_POR_NUMERO=1`, porque a falha que este sistema mais teme é "parou de entrar lead" e religar tem que custar trinta segundos, não uma publicação. A recusa **diz qual das duas coisas faltou** — token que não bate e token ausente pedem remédios opostos.

**3. O webhook do Asaas falhava ABERTO.** `if (TOKEN_WEBHOOK && ...)`: sem a variável, a conferência era pulada e qualquer pessoa avisava "pagamento recebido" em qualquer conta. Agora falha **fechado** (503) e explica. O custo de errar para o lado seguro é o pagamento demorar a aparecer, que o "Verificar de novo" já resolve.

### O crachá durava 30 dias e não tinha como derrubar

`authRequired` só conferia a assinatura. Consequências, todas reais numa imobiliária: **corretor demitido continuava entrando por um mês** (o gestor clicava em "Remover", a pessoa sumia da tela e nada acontecia de fato); gestor rebaixado mantinha o poder; e **trocar a senha não derrubava ninguém** — o gesto universal de "me tira daqui" não fazia nada contra o intruso.

Agora o crachá é conferido contra o BANCO a cada chamada (um `SELECT` por id em SQLite dentro do processo — microssegundos): status, papel fresco e o carimbo `users.sessoes_desde`. **A comparação é exata, não "emitido antes de"**: a primeira versão comparava com o `iat` do JWT, que tem precisão de SEGUNDO, e o crachá emitido no mesmo segundo da troca sobrevivia — o teste 3 pegou isso na primeira execução, que num teste é o caso normal e na vida real é o caso raro, o pior tipo de defeito para uma trava. O carimbo viaja no crachá (`sd`); ausente vale zero, então **publicar não desloga a equipe**. E o front trata o 401: volta para o login **com o motivo escrito**, senão a pessoa concluiria que o CRM quebrou.

### O que a internet podia saber e o que ficava no log

- **`/integracoes` era público e contava tudo**: nome do cliente, quantas imobiliárias existem, quantos usuários, leads e mensagens, e o registro de tentativas de e-mail. Continua **abrindo sem login** — é o primeiro passo de "parou de chegar lead", e exigir login ali seria pedir o CRM justamente quando a dúvida é se o servidor está de pé —, mas os números e os nomes agora são só do master. Ganhou um bloco **`seguranca`** que responde "esqueci alguma variável?" em português.
- **O link de redefinição de senha ia para o log SEMPRE**, em texto puro. O log do Railway não é privado: era uma lista de contas prontas para serem tomadas, num lugar onde ninguém procuraria. Agora vale a régua do convite: **saiu o e-mail, o log só registra que saiu; não saiu, o link aparece** — porque aí ele é a única forma de a pessoa voltar a entrar.
- **Telefone e nome de cliente saíam no log** a cada lead. Dado pessoal fora do banco, sem prazo e fora de qualquer pedido de exclusão. Passou a `5587****01`: dá para achar a linha no banco, não dá para discar.
- **O token do link de senha era guardado em claro** (`users.invite_token`). Agora vai como **impressão digital** (`invite_hash`, sha-256) — resumo e não criptografia, porque segredo que o servidor consegue reverter é segredo que quem tem o banco também reverte. O campo antigo ainda é lido, para os links que já estão em caixas de entrada, e é apagado no primeiro uso.

### O resto

Sem freio nenhum em `/auth/login` — e como o `bcrypt.compareSync` PARA o servidor, o mesmo laço que adivinhava a senha derrubava o CRM junto: a porta servia de martelo. Agora **20 tentativas por IP em 15 min** e **50 por e-mail em 1 hora**, e a diferença é uma escolha: o freio por e-mail apertado viraria uma arma melhor que a doença (bastaria errar dez vezes a senha do colega para trancá-lo fora do trabalho a manhã inteira). A resposta **diz quantos minutos faltam** — "tente mais tarde" faz a pessoa tentar agora e renovar o próprio bloqueio. Junto: freio nas duas portas abertas de cadastro (cada uma dispara um e-mail nosso, e um laço transformaria o ConHub numa máquina de spam com o nosso domínio assinando).

Mais: **CORS deixou de ser `*`** (hoje a tela vem deste servidor — mesma origem não precisa de permissão); **o corpo de 30 MB ficou só onde sobe foto**, e o resto caiu para 1 MB (30 MB nas rotas abertas é o jeito mais barato de derrubar um CRM); **cabeçalhos de segurança** em toda resposta, escritos à mão em vez de instalar o `helmet` — cada dependência nova é mais uma coisa que pode quebrar o `npm install` numa hospedagem que o Ali administra sozinho; **`/arquivos` marca `nosniff` e `attachment`** (guarda arquivo que veio de fora, e "documento" com HTML dentro abriria como página no endereço do CRM, onde consegue ler o crachá de quem estiver logado); **o nome dos arquivos ganhou o UUID inteiro** — eram 8 caracteres mais o relógio, e ali moram os prints de simulação com renda e CPF; **`/push/cancelar` confere de quem é a inscrição** (qualquer pessoa logada desligava o aviso de lead de um corretor, e ele só descobriria nos leads que deixou esfriar); e o webhook da Meta confere a assinatura quando `META_APP_SECRET` existe — aqui o padrão erra para o lado de RECEBER, ao contrário do Asaas, porque lá aceitar sem conferir libera dinheiro e aqui recusar sem conferir para de entrar lead.

### O que foi criptografado, e o que não foi

`services/cofre.js`, AES-256-GCM com `CRYPTO_KEY` (64 hex), **separada do `JWT_SECRET` de propósito**: aquele pode e deve ser trocado quando houver suspeita — trocá-lo só derruba as sessões; se a mesma chave abrisse os dados, trocá-la destruiria o banco. Chave que não se pode trocar é chave que nunca vai ser trocada.

**A cópia de segurança diária passou a subir fechada.** É a criptografia que mais vale a pena aqui e a razão é o destino: a cópia SAI do nosso servidor para um armazenamento de terceiros, e é o CRM inteiro de todos os clientes num arquivo só. Compacta antes de fechar (texto cifrado não comprime). GCM porque **autentica além de cifrar**: um byte trocado faz a abertura falhar em vez de devolver lixo — num backup, é a diferença entre "está corrompido" e "restauramos um banco embaralhado sem ninguém perceber". Sem a chave o backup **continua acontecendo, em claro**, e o `/integracoes` diz isso em vermelho: o primeiro risco da lista é não ter cópia nenhuma, e proteção que se desliga em silêncio é pior que não existir.

**O que NÃO foi criptografado, e é decisão consciente: os tokens de WhatsApp na tabela `canais`.** O ganho seria real mas secundário (protege contra quem já tem o arquivo do banco — e a exposição principal disso era justamente a cópia sem criptografia, que acabou de ser fechada), e o risco é o maior de todos: a busca do webhook é `WHERE token = ?`, e cifra aleatorizada exige uma coluna de índice determinística, migração de todas as linhas e um caminho de volta. Fazer isso no mesmo deploy de treze outras mudanças de segurança, no CRM que a operação usa, é como se derruba o WhatsApp de uma imobiliária inteira. Fica para uma mudança isolada, com teste próprio. `cofre.abrir()` já devolve valor em claro como veio — a migração está pronta para acontecer sem janela de indisponibilidade.

### LGPD: os direitos do titular

O "titular" é o **cliente da imobiliária**, não o corretor. A lei dá a ele duas coisas que o CRM não tinha como atender (`services/lgpd.js`, `GET /leads/:id/lgpd`, `POST /leads/:id/lgpd/anonimizar`, cartão recolhido no fim da ficha, **só do gestor**):

**Acesso** (art. 18) — um arquivo com tudo: cadastro, conversas, ligações, observações, simulações, histórico de etapas e **as leituras que a IA fez sobre ele** (art. 20: esconder análise automatizada num pedido de acesso é o que a lei não permite).

**Eliminação** — e aqui **anonimizar, não apagar** (art. 12). São duas obrigações que puxam para lados opostos e as duas são reais: o titular tem direito de sumir, e a imobiliária precisa do registro do atendimento. Apagar a linha faria o relatório de agosto mudar em setembro, e a comissão de uma pessoa depender do pedido de outra. Então sai tudo que aponta para a PESSOA (nome, telefone, e-mail, o texto das conversas, arquivos, observações, simulações) e fica o ESQUELETO (datas, etapas, quem atendeu, **quantas** mensagens houve — que é o tempo de resposta do relatório). Irreversível, com confirmação escrita nos dois lados: na tela e no servidor.

**O que continua sendo decisão da imobiliária, e está escrito no `DEPLOY.md`:** prazo de guarda (hoje nada some sozinho), aviso de privacidade no anúncio da Meta, e o fato de as conversas passarem pela Uazapi — um fornecedor terceiro, fora do WhatsApp oficial.

Testes: `npm run teste:seguranca` (23 casos, com o servidor de pé). É o segundo da suíte, logo depois do `teste:webhook`, pela mesma razão: se a porta está destrancada, nada mais importa.

## Identidade visual

Verde-esmeralda (`#0E8F6E`), verde profundo (`#0A3D30`), base clara, coral (`#E1553A`) para urgência. Fontes: Sora (títulos), Inter (texto), IBM Plex Mono (números). Elemento-assinatura: cronômetro de espera que "esquenta" (verde→âmbar→vermelho) conforme o lead aguarda. Se o usuário mandar logo/cores oficiais da Conecta, aplicar.

## Estrutura

```
con-crm/
├── DEPLOY.md              # passo a passo de hospedagem, escrito para não-dev
├── render.yaml            # alternativa ao Railway
├── frontend/
│   ├── index.html         # versão compilada e deployável (Netlify). React embutido.
│   ├── build.mjs          # compila src/app.jsx e injeta no index.html (npm run build)
│   └── src/app.jsx        # fonte React (global React, sem imports)
└── backend/
    ├── public/            # cadastro.html e definir-senha.html (HTML puro, sem build)
    ├── railway.json
    ├── .env.example
    ├── src/server.js      # Express app + páginas públicas
    ├── src/bootstrap.js   # cria org e conta ADM no start (idempotente)
    ├── src/db.js          # SQLite + schema + migrações leves de coluna
    ├── src/auth.js        # JWT + middleware de papéis
    ├── src/seed.js        # SÓ testes: usuários fictícios (senha 123456)
    ├── src/routes/        # auth, leads, distribution, messages, meta.webhook, uazapi.webhook
    └── src/services/      # stages, uazapi, meta, mail (Resend)
```

## Ordem de montagem no server.js — a armadilha que já custou caro

`app.use(middleware, router)` **sem caminho** aplica o middleware a TODA rota
registrada depois dele. Em 13/08/2026 as tarefas entraram como
`app.use(cobrando, tarefasRoutes)`, e as rotas seguintes eram os webhooks da
Meta e da Uazapi: **todo lead que chegava pelo WhatsApp levou 401 e foi
descartado**. O CRM continuou de pé, a tela abria, nenhum erro aparecia — só
parou de entrar lead.

Regra: **todo `app.use` com middleware leva caminho explícito.** Rota nova nunca
entra depois dos webhooks sem prefixo próprio.

`npm run teste:webhook` sobe o servidor inteiro e confere de fora: os webhooks e
o painel `/integracoes` respondem SEM login, e `/leads`, `/reports`, `/config`,
`/tarefas` e `/distribution` respondem 401. É o primeiro teste da suíte — se o
webhook está fechado, nada mais importa. Nenhum teste de unidade pega isso:
cada rota isolada funciona; o que quebra é a ordem.

## "Parou de chegar lead" — por onde começar

1. **`SITE/integracoes`** → `ultima_entrada` diz há quantos minutos entrou o
   último lead, a última mensagem recebida e a última enviada. Mensagem
   recebida é o sinal mais sensível: ela chega pelo mesmo webhook do lead novo.
   Se as mensagens continuam entrando e leads não, o WhatsApp está de pé.
   Confira também `whatsapp.ok` (instância pareada?) e `banco.caminho`.
2. **`SITE/integracoes/webhooks`** → o que a Uazapi mandou desde que o servidor
   subiu, com o resultado de cada evento. A lista **zera a cada publicação** — e
   a própria resposta avisa isso, para lista vazia logo após um deploy não ser
   lida como "a Uazapi parou".

## Aviso de versão nova (conserto de 13/08/2026)

O `AvisoVersao` relia o `/index.html` para descobrir se havia versão nova. Isso
funcionava no Netlify — era o próprio arquivo do site. Depois que o CRM passou
a ser servido pelo backend, a página virou `/app` e o `index.html` **não existe
em `backend/public/`**: o pedido voltava 401, o erro era engolido pelo `catch` e
**a faixa de versão nova nunca apareceu na produção**. Cada publicação dependia
de alguém desconfiar sozinho e apertar Ctrl+Shift+R.

Agora confere pelo **`/versao.txt`**, que o build grava e o servidor entrega com
`no-store`. E em **Minha conta** aparecem as DUAS versões — a do aparelho e a
publicada no servidor — com botão de atualizar quando diferem. É o que separa
"não foi publicado" de "está no cache do aparelho": sem as duas lado a lado,
"consertei" e "aqui continua igual" são duas frases verdadeiras que ninguém
consegue reconciliar.

Diagnóstico rápido, sem abrir o CRM: **`https://www.conhubcrm.com.br/versao.txt`**.

## Build do frontend

```bash
cd frontend
npm install     # só na primeira vez (esbuild)
npm run build   # src/app.jsx -> index.html
```

`build.mjs` usa o `index.html` como molde: mantém `<head>`, `<style>` e os React/ReactDOM
embutidos, e troca **só o último `<script>`** pelo JSX recompilado. Ou seja: **JS mexe em
`src/app.jsx`; CSS global e `<meta>` mexem direto no `index.html`.** Sempre rode o build
depois de editar o JSX — senão o arquivo publicado fica velho.

Restrições do sandbox de artifact NÃO se aplicam aqui (é HTML puro). Hoje o app usa
**estilos inline** (sem Tailwind) e ícones SVG inline — mantenha esse padrão para não
depender de rede.

## Rodar o backend

```bash
cd backend
cp .env.example .env      # preencher
npm install
npm run seed              # opcional: usuários fictícios (senha 123456)
npm start                 # http://localhost:4000  (GET /health)
```

Banco: SQLite em `backend/concrm.db` (não commitar). Ao hospedar, aponte `DB_PATH` para
um disco/volume persistente ou migre para Postgres.

Cuidado com Node novo: `better-sqlite3` precisa ser uma versão com binário pré-compilado
para a versão do Node em uso (hoje `^12`), senão o `npm install` tenta compilar do zero e
exige Python + build tools. O npm 11+ também bloqueia os scripts de instalação — o
`package.json` já traz o campo `allowScripts` liberando o `better-sqlite3`.

## Integrações (a ligar)

- **Meta Lead Ads**: webhook `POST /webhooks/meta` (campo `leadgen`) → busca o lead na Graph API e joga na fila da catraca. Precisa `META_VERIFY_TOKEN` e `META_PAGE_ACCESS_TOKEN` (permissão `leads_retrieval`).
- **Uazapi (WhatsApp não-oficial)**: envio em `services/uazapi.js` (`/send/text`), recebimento em `POST /webhooks/uazapi`. Precisa `UAZAPI_HOST`/`UAZAPI_TOKEN`. ATENÇÃO: API não-oficial fere os termos do WhatsApp e tem risco de ban — usar número dedicado, sem disparo em massa idêntico. Os campos exatos de payload variam por provedor; ajustar conforme a conta.
- **WhatsApp Cloud API — API oficial da Meta (implementado, opcional; 03/09/2026)**: envio/recebimento em `services/whatsapp_oficial.js` e `routes/whatsapp-oficial.webhook.js` (webhook em `/webhooks/whatsapp-oficial`). Ao contrário da Uazapi e do Lead Ads, as credenciais **não são variável de ambiente do servidor** — cada imobiliária conecta a própria conta pela tela Configurações → Conexão, com Phone Number ID, WABA ID, token de acesso permanente e App Secret do PRÓPRIO aplicativo Meta dela. Passo a passo completo de conta (Gerenciador de Negócios, verificação de empresa, criação do app, token permanente) na tela mesma (tutorial embutido) e no guia publicado para o Ali. Ver a entrada de 03/09/2026 acima para as decisões de arquitetura.
- **E-mail (implementado, falta a conta)**: `services/mail.js` chama a API do Resend por HTTP puro (sem SDK). Precisa de `RESEND_API_KEY` e `MAIL_FROM` com domínio verificado da Conecta. Sem isso, `sendMail` devolve `{sent:false}` e o cadastro cai no modo manual (link na tela + no log) — de propósito, para não travar a operação.

## Relatórios e etapa por IA (feito em 13/08/2026)

Os quatro pontos da pauta do Ali, entregues. O que ficou:

- **Diagnóstico do funil** (`GET /leads/reanalise` → campo `diagnostico`; tela em
  "Base de leads" → Analisar a base). "A palavra-chave não funciona" são três
  doenças com remédios diferentes, e o painel separa: quantas conversas não têm
  NENHUMA das palavras, quantas são só áudio/foto (onde regra de palavra nunca
  vai alcançar) e em quantas conversas cada palavra aparece — marcando a que
  **nunca apareceu**. Sai um veredito escrito, em uma frase. Medir antes de
  mexer no regex é a regra aqui.
- **Etapa lida pela IA** (`POST /leads/:id/etapa-ia`, `services/ia.js` →
  `etapaDaConversa`; cartão na ficha do lead). A IA lê a conversa e diz a
  etapa, com confiança, motivo e o **trecho** que sustenta. **É SUGESTÃO**: quem
  grava é o corretor no botão, pela rota manual de sempre — a regra "a IA lê,
  nunca escreve" continua de pé, e etapa vira relatório que vira cobrança em
  reunião. Guardada em `leads.etapa_ia_json/_em/_msgs` para não pagar duas vezes;
  entra no Uso da IA como recurso `etapa`. Etapa fora do funil ou resposta fora
  do formato viram erro na tela, nunca lead movido.
- **Etapa confirmada por pessoa fica FORA da reanálise** por palavra-chave
  (`fora.confirmado_na_mao`). Sem isso as duas regras brigavam e o lead voltava
  para trás em silêncio.
- **Score fiel à tela**. Eram três descasamentos, todos corrigidos em
  `services/score.js`: venda agora conta pela `sale_date` (era etapa da coorte),
  conversão divide pelos **recebidos** (era pelos resolvidos) e o período é o
  **mesmo** que o gestor escolheu na tela (era 90 dias fixos). A função `pct`
  virou uma só, exportada do `score.js` e usada também pelo `reports.routes.js`
  — eram duas, uma arredondando para inteiro e outra com uma casa decimal
  (33% x 33,3%). O teste `npm run teste:score` compara as duas rotas número a
  número e quebra se voltarem a divergir.
- **A nota vem aberta**: cada parte com valor, régua, peso e quantos pontos
  contribuiu (`COMPONENTES_DO_SCORE`). Clicar no nome na tabela do score abre o
  detalhe. Vendas e ligações são notas **comparativas** (100 = o melhor da
  equipe no período) e a tela avisa isso.
- **Relatório para reunião** (botão na ficha do corretor em Relatórios). Abre em
  tela cheia e sai pela impressão do navegador → Salvar como PDF. Não calcula
  nada: busca as MESMAS duas rotas da tela, com o mesmo período. Traz o bloco
  "Como cada número é medido". O corretor imprime o próprio (sem o bloco da
  nota — ranking continua só da gestão). O CSS de impressão está no
  `index.html`; a folha usa a classe `folha`, e o que não vai ao papel usa
  `nao-imprimir`.

Ainda não existe **histórico de etapas**, então "quantos avançaram para X no
período" continua impossível — `agendamentos` é foto do momento, e o relatório
diz isso por escrito.

## Próximos passos (nesta ordem)

1. **Hospedar o backend** — guia pronto em `DEPLOY.md` (Railway; `render.yaml` como alternativa). Precisa de plano sempre-ligado (o free hiberna e atrasa os webhooks) e de disco persistente com `DB_PATH=/data/concrm.db`. Objetivo: obter a URL HTTPS pública e liberar o link de cadastro. **Domínio (11/08/2026): o endereço oficial é `https://www.conhubcrm.com.br`, COM `www`** — a raiz sem `www` não aponta e não vai apontar tão cedo (o Railway não dá IP fixo e o domínio está com DNSSEC no Registro.br; o porquê está em `DEPLOY.md → Domínio próprio`). Ao trocar o endereço, acompanham: `APP_URL`/`SITE_URL` no Railway (é o `APP_URL` que monta a URL pública das mídias enviadas ao WhatsApp), o webhook da Uazapi e a reinstalação do atalho na tela de início.
2. **Ligar o e-mail** (Resend): verificar o domínio da Conecta, preencher `RESEND_API_KEY` e `MAIL_FROM`. Enquanto isso não é feito, o link de confirmação aparece na tela e no log.
3. **Ligar o frontend ao backend**: trocar os dados de exemplo (lista fixa de login, leads em memória) por chamadas HTTP às rotas. Precisa da URL do passo 1. Aí os corretores fictícios saem e entram as contas reais, e a tela de login vira e-mail + senha.
4. **PWA**: manifest + service worker + ícone para instalar na tela de início. O layout responsivo já está pronto.
5. **Notificações push (Web Push)**: backend dispara push quando um lead é transferido para o corretor ou quando o lead responde. Requer HTTPS (do passo 1), VAPID keys e armazenar a subscription por usuário. Caveat iOS: só funciona se o corretor **adicionar o site à Tela de Início** (PWA) — aba aberta no Safari não recebe push. Android funciona direto.
6. **Tela de equipe para a ADM**: `GET /auth/users` já devolve quem está `pendente` / `ativo`; falta a tela no CRM.

## Decisões já tomadas

- Cadastro **com confirmação por e-mail** (opção b) — decidido em 27/07/2026.
- `ADM_CODE` **continua sendo a trava**, mas vai embutido no link (`?c=...`), então o corretor não digita nada.
- Provedor de e-mail: **Resend** (implementado; falta a conta e o domínio verificado).

## Decisões em aberto (perguntar ao usuário)

- Qual domínio da Conecta usar no remetente do e-mail.
- Nomes reais dos corretores / quem é SDR / quem é ADM (hoje são fictícios no frontend).

## Restrições / cuidados

- Não introduzir `localStorage`/`sessionStorage` no frontend do artifact original; no site hospedado é permitido, mas o padrão atual é estado em memória.
- O avanço de etapa vive só em `backend/src/services/stages.js`. Ao mexer nas palavras (`GATILHOS`), atualizar também o `PALAVRA_ETAPA` de `frontend/src/app.jsx` — é o texto que o corretor lê na ficha, e regra que ninguém sabe não é regra.
- O usuário (Ali) é de marketing/gestão, não é dev — explicar em passos claros, sem jargão, e nunca assumir que ele roda comandos avançados sem orientação.
