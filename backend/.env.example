# ─── Básico ───────────────────────────────────────────────────────────────────
PORT=4000

# URL pública deste backend, SEM barra no final. É com ela que o link do e-mail é montado.
# Local:      http://localhost:4000
# Hospedado:  https://con-crm.up.railway.app  (ou o domínio que a hospedagem te der)
APP_URL=http://localhost:4000

# De quais ENDEREÇOS o navegador pode chamar esta API.
# Hoje a tela é servida por este mesmo servidor (/app), então o normal é DEIXAR
# VAZIO — mesma origem não precisa de permissão nenhuma. Preencha só se um dia
# o CRM voltar a ser hospedado à parte; aceita vários, separados por vírgula:
#   FRONTEND_ORIGIN=https://www.conhubcrm.com.br,https://app.conhubcrm.com.br
# NÃO use "*": isso libera qualquer site do mundo a chamar a API.
FRONTEND_ORIGIN=

# ─── SEGURANÇA — as três variáveis que o servidor NÃO perdoa ──────────────────

# 1) JWT_SECRET — a chave que assina o crachá de quem entra.
# Quem souber este valor consegue FABRICAR um crachá de gestor e ler os leads de
# todas as imobiliárias. Em produção o servidor RECUSA A SUBIR sem ela (antes
# ele caía num valor padrão escrito no código, o que deixava tudo destrancado
# sem nenhum erro aparecer). Mínimo de 16 caracteres.
# Gere um com:  node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Trocar este valor desconecta todo mundo — é só entrar de novo.
JWT_SECRET=troque-este-segredo-em-producao

# 2) CRYPTO_KEY — a chave que fecha a CÓPIA DE SEGURANÇA e os segredos guardados.
# A cópia diária é o CRM inteiro de todos os clientes num arquivo só, e ela sai
# do nosso servidor para um armazenamento de terceiros (Cloudflare R2). Sem esta
# chave a cópia sobe EM CLARO, e /integracoes avisa isso em vermelho.
# Exatamente 64 caracteres (números e letras de a-f):
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ATENÇÃO: PERDER A CHAVE É PERDER O QUE ELA FECHA. Guarde uma cópia fora do
# Railway. Ela é SEPARADA do JWT_SECRET de propósito: aquele pode ser trocado
# quando quiser, este não pode.
CRYPTO_KEY=

# 3) ASAAS_WEBHOOK_TOKEN — está mais abaixo, na seção do Asaas. Sem ele o
# webhook de pagamento RECUSA tudo (antes ele aceitava, e qualquer pessoa da
# internet podia avisar "pagamento recebido" e liberar uma conta).

# META_APP_SECRET — assinatura do webhook do Facebook. Sem ele o CRM continua
# aceitando os leads (parar de receber lead é pior), mas sem conferir quem
# mandou. Está no painel do app da Meta, em Configurações > Básico.
META_APP_SECRET=

# UAZAPI_ACEITAR_POR_NUMERO — SAÍDA DE EMERGÊNCIA, deixe desligada.
# Se algum dia a Uazapi parar de mandar o token junto e os leads pararem de
# entrar, ponha "1" aqui para o CRM voltar a reconhecer a imobiliária pelo
# NÚMERO. É inseguro: o número da imobiliária é público, e com ele qualquer
# pessoa consegue mandar mensagem falsa para dentro do CRM. Use, resolva a
# causa e desligue. O diagnóstico em /integracoes/webhooks diz quando usar.
# UAZAPI_ACEITAR_POR_NUMERO=1

# Código que o corretor usa para vincular a conta à Conecta (vai no link de cadastro).
# Mudar aqui e reiniciar invalida os links antigos.
ADM_CODE=CONECTA-JAZ-2026
ORG_NAME=Conecta Imóveis

# Conta de administração criada no primeiro start (só se ainda não existir nenhuma
# com esse e-mail). Depois de criada, pode apagar estas três linhas.
ADM_NAME=Ali
ADM_EMAIL=
ADM_PASSWORD=

# Caminho do banco. Na hospedagem, aponte para o disco persistente (ex.: /data/concrm.db).
# DB_PATH=/data/concrm.db

# ─── E-mail de confirmação do cadastro (Resend) ───────────────────────────────
# Enquanto estiver vazio, o cadastro continua funcionando: o link de confirmação
# aparece na tela e no log do servidor, para a ADM repassar na mão.
# 1) Crie a conta em resend.com  2) Verifique o domínio da Conecta  3) Gere a API key.
RESEND_API_KEY=
MAIL_FROM=Conecta Imóveis <nao-responda@seudominio.com.br>

# ─── Fotos e vídeos dos imóveis ───────────────────────────────────────────────
# Sem as variáveis abaixo, os arquivos ficam no disco da hospedagem (ao lado do
# banco). Funciona, mas o volume é pequeno — bom para fotos, apertado para vídeo.
#
# Preenchendo as quatro, o sistema passa a usar o Cloudflare R2 sozinho, sem
# mexer em mais nada. Onde achar cada uma, no painel do R2:
#   R2_ACCOUNT_ID        → visão geral da conta (Account ID)
#   R2_ACCESS_KEY_ID     → Manage R2 API Tokens > Create API Token
#   R2_SECRET_ACCESS_KEY → aparece uma única vez, junto com a chave acima
#   R2_BUCKET            → nome do bucket que você criar (ex.: comhub)
#   R2_PUBLIC_URL        → Settings > Public access (r2.dev ou domínio próprio)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_URL=

# ─── Cópia de segurança do banco ──────────────────────────────────────────────
# O banco é UM ARQUIVO no disco da hospedagem, com os leads de todas as contas
# dentro. Todo dia de madrugada ele é copiado para o mesmo R2 acima — fora do
# servidor, que é o ponto: cópia guardada no mesmo disco some junto com ele.
#
# Por isso a cópia SÓ acontece com o R2 preenchido. Sem ele o sistema não grava
# no disco "para não ficar sem nada": ficaria com a impressão de estar protegido
# sem estar. O aviso aparece no hub, na seção Cópia de segurança.
#
# As duas variáveis abaixo são opcionais — os padrões servem.
BACKUP_HORA=3       # hora do dia (0-23) em que a cópia roda. Padrão: 3 da manhã
BACKUP_MANTER=30    # quantas cópias diárias ficam guardadas. Padrão: 30

# ─── Meta Lead Ads ────────────────────────────────────────────────────────────
META_VERIFY_TOKEN=
META_PAGE_ACCESS_TOKEN=

# ─── WhatsApp (Uazapi) ────────────────────────────────────────────────────────
UAZAPI_HOST=
UAZAPI_TOKEN=

# Endereço do SITE (o domínio que a equipe acessa). É ele que sai no link de
# definir senha enviado ao corretor. Diferente do APP_URL, que é o do servidor
# e continua servindo os arquivos das conversas.
SITE_URL=https://conhubcrm.com.br

# Leitura automática do print da simulação da Caixa (opcional). Sem a chave, o
# corretor registra a simulação na mão e nada quebra.
ANTHROPIC_API_KEY=

# Cobrança da mensalidade (Asaas). Sem a chave, a assinatura funciona no modo
# manual: o gestor marca "pago" e o vencimento anda um mês.
ASAAS_API_KEY=
ASAAS_SANDBOX=true
# Token que você define no painel do Asaas ao cadastrar o webhook.
# OBRIGATÓRIO: sem ele o webhook RECUSA todo evento (responde 503) e o pagamento
# não aparece sozinho no CRM. Até 02/09/2026 ele era opcional — e sem ele a
# conferência era PULADA, ou seja, qualquer pessoa da internet podia avisar
# "pagamento recebido" e ganhar mês de mensalidade em qualquer conta.
# Ponha aqui o MESMO valor que você digitou no painel do Asaas.
ASAAS_WEBHOOK_TOKEN=

# Armazenamento no Cloudflare R2 (recomendado). Sem estas variáveis os arquivos
# ficam no disco da hospedagem, no mesmo volume do banco — e disco cheio derruba
# o CRM inteiro, não só o envio de fotos.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# Domínio público do bucket. Sem ele o arquivo sobe mas não abre para ninguém.
R2_PUBLIC_URL=
